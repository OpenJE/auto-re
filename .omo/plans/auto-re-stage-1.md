# auto-re-stage-1 - Work Plan

## TL;DR (For humans)
<!-- Fill this LAST, after the detailed plan below is written, so it summarizes the REAL plan. -->
<!-- Plain English for a non-engineer: NO file paths, NO todo numbers, NO wave/agent/tool names. -->

**What you'll get:** An end-to-end reverse-engineering platform that takes a whole executable (your first target: Fallout: Van Buren), ingests every function/type/global/class/vtable it contains, schedules work for every one of those discoveries, drives a local LLM and the IDA debugger to reconstruct each one, builds a complete managed C++ project tree through your existing cmkr+CMake+Docker-MSVC2002 pipeline, and runs autonomous differential verification against the original — looping on failures through typed repair work until everything is either verified, blocked with a structured reason, or explicitly excluded. You get a CLI to drive a campaign and a Ratatui dashboard to watch it.

**Why this approach:** Your spec pinned the outcome down to decisions but left the FIRST-implementation toolchain/runtime portable. You told me you can take any language/toolchain later but you currently use cmkr+CMake+Docker-MSVC2002 and Wine+gdb — so every first-implementation in this plan plugs your existing workflow in behind a generic trait (BuildProvider, ExperimentDesign, DebuggerBackend, OpenAI-compatible LLM client) so swapping to clang-cl cross / native MSVC host / x64dbg-on-Windows / vLLM later never reshapes canonical entity ownership. This mirrors the spec's §23 architectural test: each "future backend" must slide into a trait, not bend the core.

**What it will NOT do:** Build Ghidra/Binary Ninja backends now (deferred behind the AnalysisProvider trait). Run remote workers, a package registry, a dependency solver, or any sandbox stronger than configured working directories and bounded runtimes — your dev box stays the execution surface. Symbolic/concolic execution, fuzzing, multiple generation languages (anything other than C++), or formal equivalence proofs. The whole-Van-Buren real-binary real-LLM run to "everything verified" is a separate OPERATOR acceptance you own — the plan delivers the platform that runs that campaign; the campaign's pass/fall to §20 is not a code-pass-bar the worker closes.

**Effort:** XL — 60 todos across 12 waves built directly from your spec's §18 Phase 1-12; each phase is one wave; each wave is 4-5 todos.
**Risk:** High — load-bearing invariants (canonical mutations only through ApplicationCommand, external providers never touch the project SQLite, LLM output enters as hypotheses-not-facts, generated source never edited directly by the LLM, whole-program coverage with SCC-using scheduler) are all independently testable on small fixtures but the cross-cut-through to the whole Van Buren safe binary volume tests how well the per-component invariants hold under scale; the schema is additive and Stage-0 backward-compat is hard-fact.
**Decisions to sanity-check:** (1) First build toolchain = cmkr+CMake+Docker-MSVC2002 behind the `BuildProvider` trait (you said this is your current workflow). (2) IDA debugger → GDB backend → IDA-Wine-bridge first-impl, `DebuggerBackend` trait admits x64dbg later. (3) First-class external OpenAI-compatible local server (`llama-server`/vLLM/Ollama/LM Studio) — operator-supplied endpoint; no hard-coded LLM provider. (4) Van Buren binary registered by operator at campaign start; binary bytes stay OUT of the auto-re repo (only content-hash). (5) All compliance rests on the existing Stage 0 atomic ApplicationCommand+ProjectEvent transaction discipline; no Stage 1 worker directly touches SQLite or artifact storage. (6) TDD per todo, every todo has happy+failure scenarios with files-of-record under `.omo/evidence/auto-re-stage-1/`; final acceptance is a 38-row spec §19 cross-check plus a 10-question §23 architectural-test proof file. (7) **All seven new Stage 1 crates are off `default-members`** — the three library crates (`autore-provider-protocol`, `autore-provider-runtime`, `autore-reconstruction`) must NOT include `tonic`/`prost`/`protoc` dependencies in the default `cargo build` surface; máquina: `cargo build` (no flags) must continue to compile the existing 7 Stage-0 default-members exactly as it does today, per Stage-0 backward-compat invariant.

Your move: approve the plan, OR request a high-accuracy dual review (Momus + independent Oracle) before delivery. Once approved I scaffold the executor (via `/start-work` elsewhere) — implementation is not run by the planner, full execution detail follows below.

---

> TL;DR (machine): XL, High-risk, whole-executable reverse-engineering pipeline with typed external providers (IDA/OpenAI-compatible/build), cmkr+CMake+Docker-MSVC2002 first build adapter, Wine+gdb first debug adapter, one managed C++ tree, deterministic SCC scheduler; 60 todos across 12 waves; acceptance = §19 38-row gate + §23 10-yes architectural proof + §22 final report.

## Scope
### Must have
The 23 sections of the supplied Stage 1 Vertical Slice Specification, mapped to one work plan:
- §3 architectural invariants (canonical ownership through `autore-app`, provider isolation, evidence-before-belief, typed machine communication, whole-program coverage)
- §5 Vertical Cut A — Provider substrate: versioned tonic+prost gRPC protocol; local package discovery+hashing+manifest validation; bootstrap with instance-ID + one-time secret over Unix-domain socket; ArtifactTransport abstraction with opaque scoped handles; fixture provider (echo/delay/fail/artifact/large-stream)
- §6 Vertical Cut B — IDA whole-executable ingestion: external IDA provider using `idax = 0.3.0`; capabilities `ida.binary.open`, `ida.binary.ingest`, `ida.program.refresh`, `ida.function.snapshot`, `ida.type.snapshot`, `ida.class.snapshot`, `ida.references.query`, `ida.reanalyze`, `ida.native-artifact.export`; whole-binary structural ingestion (segments, imports, exports, all functions, CFG, refs, strings, globals, RTTI, vtables, static initializers, decompiler output); two-layer storage (canonical structural index + immutable native-artifact snapshots); stable entity identity NOT based on IDA row ids; deterministic refresh with stale-marking invalidation
- §7 Vertical Cut C — Whole-program reconstruction graph: `ReconstructionCampaign`; 18 work-item kinds (ProgramSkeleton, ExternalDependency, Global, Enum, Structure, Class, Vtable, Function, FunctionCluster, StaticInitializer, Subsystem, Entrypoint, Investigation, Generation, BuildFailure, LinkFailure, VerificationFailure, ConflictResolution); dependency graph including SCC-collapsed recursive call cycles; deterministic priority policy; per-work-item input fingerprints; scheduler requests transitions through `autore-app` commands
- §8 Vertical Cut D — LLM semantic analysis: external OpenAI-compatible provider; 7 typed capabilities (`llm.analysis.function`, `llm.analysis.type`, `llm.analysis.class`, `llm.analysis.subsystem`, `llm.analysis.conflict`, `llm.analysis.failure`, `llm.experiment.design`); immutable investigation bundles; structured function-analysis result with proposed name/signature/claims/experiment-proposals; three-level import boundary (raw artifact → schema-validated result → canonical hypotheses/work items); bounded schema-repair policy (1 attempt, then retryable fail); provenance record without plaintext secrets
- §9 Vertical Cut E — Dynamic investigation through IDA/GDB: typed debugger scenario language subset (launch/attach/breakpoint/continue/step/capture-*/terminate); 7 `debug.*` capabilities; debug experiment flow (LLM proposes → core validates → scheduler creates Investigation work item → IDA provider executes with GDB backend → typed observations imported); `TargetRunner` abstraction with Wine+GDB first impl and explicit seams for x64dbg backends later
- §10 Vertical Cut F — Shared type and class recovery: canonical shared hypotheses (primitives/enums/structs/classes/vtables/virtual-methods/ctors/dtors); constraint model separate from LLM interpretation; conflict records + LLM arbitration; per-field verification separate from per-layout
- §11 Vertical Cut G — Complete C++ project generation: one managed `generated/openvb/` tree with cmkr-generated `CMakeLists.txt`; immediate skeleton+stubs after ingestion; generated-source mapping (entity → declaration artifact → definition artifact → generation operation → build result → verification records); 6 `llm.generation.*` capabilities; controlled staged patching (never direct LLM file edits); explicit stub policy
- §12 Vertical Cut H — Build and compiler feedback: `ConfigureProject`/`CompileUnits`/`LinkTarget`/`RunTest`/`CollectDiagnostics` abstractions; FIRST impl = cmkr + cmake + Docker-hosted MSVC 2002 (operator's existing workflow) wrapped as a build-provider run; structured diagnostic parsing; feedback classification taxonomy; bounded repair loop with detection of repeated equivalent failures
- §13 Vertical Cut I — Differential verification: 3 verification levels (function / cluster+subsystem / whole-program smoke); scenario capture+replay with explicit normalization rules; typed comparison records (equal / equivalent-under-normalization / different / inconclusive / not-observed / execution-failed); verification-driven repair tree; regression selection keyed on dependency fingerprints
- §14 Vertical Cut J — Autonomous coordinator: durable loop pseudocode from spec §14.1; no-progress detection (9 stagnation signals → blocked reason, not infinite loop); completion policy (Verified / Blocked / ExplicitlyExcluded for every required work item)
- §15 CLI: `auto-re reconstruct {start,status,pause,resume,stop,validate}`, `auto-re provider {refresh,list,show,start,stop,restart,health}`, `auto-re work {list,show,blockers,retry,dependencies}`, `auto-re generated {status,files,entity}` + `auto-re build latest` + `auto-re verification coverage`; all read commands support stable JSON output
- §16 TUI: extend existing 7-pane Ratatui dashboard with campaign coverage, work queue, active providers, recent compiler/verification failures, stagnation reasons; provider+storage ops off the render thread
- §17 Persistence and recovery: provider installations/instances/runs, capability descriptors, work items+deps+fingerprints+leases, static snapshots, dynamic observations, raw+parsed LLM responses, hypotheses, conflicts, generated-source mappings, build attempts, compiler diagnostics, verification scenarios+comparisons, repair attempts, blocked reasons, operations+events. On restart: mark old local provider instances unavailable, reconcile interrupted ops, expire/recover leases, drop uncommitted staging, preserve committed artifacts, recompute ready work, do not repeat non-idempotent ops automatically
- §18 implementation sequence — Phase 1..12 as this plan's execution waves 1..12
- §19 Stage implementation completion criteria — 38 items, the rubric for the final `F4. Scope fidelity` verification
- §20 Van Buren reconstruction completion criteria — 11 items, campaign-success bar (separate from implementation-success)
- §22 required final implementation report
- §23 final architectural test — 10 yes/no questions, all must be yes
- TDD per todo + agent-executed happy/failure QA per todo + 12-phase hardening (Phase 12) coverage

### Must NOT have (guardrails, anti-slop, scope boundaries)
Spec §21 explicit exclusions (do NOT implement; only preserve interfaces that admit them later):
- Ghidra provider / Binary Ninja provider / multi-backend consensus voting
- Public package registry / package dependency solver / network package installation / package signing authority
- Remote workers / distributed scheduling / general remote providers / remote TLS authorization
- Full sandbox enforcement / containers / microVMs
- Symbolic execution / concolic execution / general fuzzing / kernel debugging / multi-process distributed tracing / hardware tracing
- Multiple generation languages (Stage 1 ships C++ only; `ImplementationTargetId` is the polymorphism seam, not multiple generators now)
- Build systems beyond the cmkr+CMake+Docker-MSVC2002 first-impl behind the `BuildProvider` trait
- Arbitrary provider TUI plugins / automatic hot reload / Wasm providers / native dynamic-library providers
- Automatic prompt-training / fine-tuning pipelines / general cross-project knowledge packages / general autonomous tool use
- Formal whole-program equivalence proofs

Process guardrails (ulw-plan):
- No speculative implementations of any deferred item; only the interfaces required to add them later
- No human-intervention verification in the inner loop; no manual log interpretation in the repair loop
- No untyped text as an internal control interface (spec §3.4)
- Generated LLM code may never edit the working tree directly (spec §11.5)
- No Stage 1 worker writes directly to SQLite / artifact storage / hypotheses / evidence / work state — all through `autore-app` (spec §3.1)
- No IDA row IDs as canonical entity IDs (spec §6.5)
- No plaintext secrets persisted (spec §8.8)
- No `cargo test --workspace` without `--exclude autore-stage1` (Stage 0 rule, preserved — `autore-stage1` requires external SDKs)

## Verification strategy
> Zero human intervention - all verification is agent-executed.
- Test decision: **TDD per todo** (write the failing test first for new modules: provider runtime, schema validation, SCC scheduler, scenario interpreter, patch-control, verification diff, conflict reconciliation) + **tests-after** for additive refinery migrations (rollback + idempotency) + **agent-executed happy/failure QA per todo**, every todo. Frameworks: `cargo test`, `cargo clippy --workspace --exclude autore-stage1 --all-targets -- -D warnings`, `cargo fmt --all --check`; PTY integration test stays isolated (`cargo test -p autore-tui --test pty_integration -- --ignored --nocapture`); `cargo build -p autore-stage1 --no-default-features` for build-smoke-cross-cutting.
- Evidence: `.omo/evidence/ulw/auto-re-stage-1/<attempt>/task-<N>-auto-re-stage-1.<ext>` per todo; outside ulw-loop use `.omo/evidence/auto-re-stage-1/task-<N>-<slug>.<ext>`. Every todo's QA section names the exact command + assertion + the evidence file it writes.
- Final verification wave `F4. Scope fidelity` rubric = Spec §19 38-item completion gate + Spec §23 10-question architectural test (cross-referenced as a checklist table in `## Success criteria`).
- Spec §19 categories #35 ("Fixture and fault tests pass"), #36 ("Workspace tests pass"), #37 ("Formatting passes"), #38 ("Clippy passes with warnings denied") are the bottom-of-stack gates every wave must keep green.

## Execution strategy
### Parallel execution waves
> 12 waves mirroring Spec §18 Phase 1..12. Each wave ≤8 todos; Wave 6+9 share Cut G+H implementation todos; Wave 11 is integration-cross-cutting. Across-wave parallelism only between independent cuts (e.g. fixture subsystem Wave 2 must land before IDA-provider Wave 3, but openai-compatible LLM provider Wave 5 can develop in parallel with IDA-provider Wave 3 once Wave 2 protocol substrate exists).

### Dependency matrix
> 60 numbered todos + F1-F4. Wave = Phase one-to-one (Wave N == Spec §18 Phase N).

| Todo | Depends on | Blocks | Can parallelize with |
| --- | --- | --- | --- |
| 1-5 (Wave 1: repo reconciliation) | — | 6-10 (Wave 2); 11-15 (Wave 3) | sequential within wave |
| 6-10 (Wave 2: provider substrate+fixture) | 5 | 11-15, 21-25 (LLM), 26-30 (build), 41 (debug uses runtime) | sequential within wave |
| 11-15 (Wave 3: IDA ingestion) | 10, 3, 5 | 16-20 (persistence), 26-30 (skeleton from entities), 41 (debug extends IDA) | 21-25 (LLM provider can ship in parallel with Wave 3 after Wave 2) |
| 16-20 (Wave 4: work graph) | 5, 14, 3 | 31 (dynamic inverts), 36-40 (type recovery reads work items), 41 (LLM-driven generation references work kinds), 49 (coordinator composes scheduler) | 21-25 (LLM provider Wave 5) |
| 21-25 (Wave 5: LLM provider + analysis) | 10, 12 | 36 (reconciler feeds LLM analysis), 38 (LLM conflict-arbitration), 41 (generation capability handler) | 16-20 |
| 26-30 (Wave 6: skeleton + build + diagnostics) | 14 (entities from Wave 3), 9 (artifact transport for build artifacts), 13 (IDA ingestion feeds entities) | 41 (gen needs skeleton stubs), 45-48 (verify needs builds + diagnostics), 51 (panel consumption) | 21-25 (LLM provider Wave 5) |
| 31-35 (Wave 7: dynamic investigation via IDA+GDB) | 10, 27 (skeleton may invoke target), 18 (fingerprint invalidation on dynamic obs) | 36 (type recovery uses dynamic obs), 45 (scenario capture uses dynamic), 49 (Coordinator invokes dynamic pipeline) | 26-30 (build) |
| 36-40 (Wave 8: shared type + class recovery) | 33 (dynamic obs feeds layout constraints), 14, 12 (commands) | 41 (gen consumes accepted types), 47 (substitution regression), 49 (Coordinator flows conflict-resolution through here) | — |
| 41-44 (Wave 9: function/cluster generation) | 39 (decl from accepted types), 28 (build-provider), 30 (diag classification), 21 (LLM gen cpabilities) | 45-48 (verify genetically-generated code), 49 (Coordinator invokes Generation) | — |
| 45-48 (Wave 10: differential verification) | 33 (dynamic reuses), 44 (gen needs verification input), 38 (Type arbitration impact) | 49 (Coordinator consumes verify results), 51 (CLI surface exposes verify status) | — |
| 49-52 (Wave 11: coordinator + CLI + TUI + autonomous run) | 19 (scheduler-via-app), 25 (LLM e2e), 32 (debug e2e), 44 (gen e2e), 48 (verify e2e), 12 (Stage1 command handlers), 50 (CLI builds on coord) | 53-60 (Wave 12 hardening exercises waves 49-51) | — |
| 53-56 (Wave 12: fault tests + PTY) | 49-52 | 57 (workspace gate), 58-60 (reports consume deliverables+evidence) | — |
| 57 (workspace fmt/clippy/test gate) | 53-56 | 58-60, F1-F4 | — |
| 58 (Stage 1 final impl report) | 57 | 60 (gate report cross-checks §22) | 59 |
| 59 (§23 architectural-test answers) | 57, 58 | 60 | — |
| 60 (§19 completion gate cross-check + §20 reminder) | 58, 59 | F1-F4 | — |
| F1-F4 (Final verification wave) | 60 | — | run F1‖F2‖F3‖F4 in parallel |

## Todos
> Implementation + Test = ONE todo. Never separate.
<!-- APPEND TASK BATCHES BELOW THIS LINE WITH edit/apply_patch - never rewrite the headers above. -->

### Wave 1 — Repository reconciliation (Spec §18 Phase 1; §3.1, §4)

- [x] 1. Audit `autore-stage1` modules; produce retain/adapt/replace/remove table; move Stage 1 mutations behind `autore-app`
  What to do / Must NOT do: Inventory every `autore-stage1` source file (`engine.rs`, `engine/`, `analysis/`, `cli/`, `model/`, `scheduler/`, `storage/`, `store.rs`, `worker/`, `main.rs`, `lib.rs`). Output a `docs/stage1-audit.md` table with five columns per module: file path | retain (unmodified) | adapt (signature or trait updated, body preserved) | replace (signature removed) | remove (deleted). Classify `AnalysisBackend::analyze()->String` (backend.rs:44-48) as REPLACE; `WorkerRunner` direct Claim/EvidenceRepository writes (worker/runner.rs:42-80) as REPLACE; `model/router.rs:23` ModelRouter as ADAPT; `scheduler/scheduler.rs` Scheduler as ADAPT (priority score retained, transition requests moved through `autore-app`); `engine.rs` IDAGraph as REMOVE (replaced by external provider in Wave 3); `store.rs` REMOVE (in-process IDA store, replaced by external provider). Touch no source during this todo — output is a markdown report only.
  Must NOT do: delete or modify any source. Do not pre-implement commands.
  Parallelization: Wave 1 | Blocked by: — | Blocks: 2, 4
  References (executor has NO interview context - be exhaustive): autore-stage1/Cargo.toml:6-11 (features ida/gdb/llama/tui); autore-stage1/src/lib.rs:1-35 (module map, re-exports); autore-stage1/src/analysis/backend.rs:1-49 (`AnalysisBackend::analyze()->String` target at lines 44-48); autore-stage1/src/worker/runner.rs:42-80 (the `WorkerRunner` struct begins at line 59; lines 42-52 are the `WorkerOutput` struct that precedes it; both are the refactor target); autore-stage1/src/scheduler/scheduler.rs:77-99 (Scheduler struct only — `ModelRouter` lives in the separate file `autore-stage1/src/model/router.rs:23` and is held as the Scheduler's field); autore-stage1/src/model/router.rs:23 (ModelRouter); autore-stage1/src/engine.rs:1-43 (experimental IDA engine; `#[cfg(feature="ida")]`); `autore-stage1/src/store.rs` (read at classification time — apply retain/adapt/replace/remove cell); Stage 1 spec §4.1 retain list, §4.2 adapt list, §4.3 replace list, §4.4 add list.
  Acceptance criteria (agent-executable): `test -f docs/stage1-audit.md` and the file contains non-empty rows for every `autore-stage1/src/**/*.rs` file (assert via: `find autore-stage1/src -name '*.rs' | wc -l` equals count of unique file paths appearing in `docs/stage1-audit.md`). Each row has a non-empty retain/adapt/replace/remove cell. No source file modification during this todo (assert: `git diff --exit-code autore-stage1/ autore-*` is clean — only docs/stage1-audit.md and `.omo/` artifacts may be unstaged).
  QA scenarios (name the exact tool + invocation): happy — `bash -c 'cur=$(find autore-stage1/src -name '*.rs' | wc -l); rows=$(grep -cE '^\| `autore-stage1' docs/stage1-audit.md); [ "$cur" -le "$rows" ] && echo OK'` writes `OK` to stdout; failure — running the same on an audit missing one row echoes nothing. Evidence: `.omo/evidence/auto-re-stage-1/task-1-audit.txt`
  Commit: Y | docs(stage1): add Stage 1 retain/adapt/replace/remove audit table

- [x] 2. Add Stage 1a application commands + queries (persistence-side skeleton, no logic)
  What to do / Must NOT do: Extend `autore-app/src/application_service/requests.rs` ApplicationCommand enum (currently 14 variants per requests.rs:293-308) with new variants. The COMPLETE list of Stage 1 ApplicationCommand variants to add in this todo (.defaultsboundedlate-append expansions forbidden; downstream todos cite these canonical variants by name and MUST not append):
`CreateReconstructionCampaign`, `CreateWorkItems`, `RecordWorkDependency`, `PromoteWorkItem`, `LeaseWorkItem`, `RenewWorkLease`, `CompleteWorkItem`, `FailWorkItem`, `BlockWorkItem`, `InvalidateWorkItem`, `RequeueWorkItem`, `BlockWorkWithReason`, `RegisterProviderInstallation`, `RegisterProviderInstance`, `StopProviderInstance`, `ImportProviderRunResult`, `ImportDynamicObservation`, `RecordBuildAttempt`, `RunBuild`, `RecordVerificationComparison`, `RegisterGeneratedSourceMapping`, `InvalidateGeneratedSource`, `ImportGeneratedSourceCandidates`, `ScheduleVerificationRegression`, `RecordRepairAttempt`, `AcceptHypothesisPolicyDriven`, `PauseCoordinator`, `ResumeCoordinator`, `StopCoordinator` (29 command variants total — 3 coordinator-lifecycle commands added since coordinator loop todo 49 / CLI todo 50 / TUI todo 51 invoke them; this todo owns all Stage 1 enum expansion up-front to prevent late-append ordering violations flagged by dual review).
Extend ApplicationQuery (currently variants per application_service.rs:170-192) with the COMPLETE Stage 1 query-variant list: `GetCampaign`, `ListWorkItems`, `GetWorkItem`, `ListWorkItemDependencies`, `ListWorkItemBlockers`, `ListExpiredLeases`, `GetProviderInstallation`, `ListProviderInstallations`, `ListProviderInstances`, `GetProviderInstance`, `GetBuildStatus`, `ListBuildDiagnostics`, `GetVerificationCoverage`, `ListGeneratedSourceMappings`, `ListConflicts`, `ListBlockedReasons` (16 query variants).
Define request/response structs per variant with `#[derive(Debug, Clone, PartialEq, serde::Serialize)]` matching the existing style at requests.rs:292-310. Add no handler bodies in this todo — each new `execute` and `query` match-arm returns `Err(Error::Validation(format!("not yet implemented: {variant_name}")))`. Existing handlers (lines 149-166) are real handlers and are NOT a stub-template — this todo CREATES the first stub-template pattern for the new Stage 1 arms. Keep `LocalAutoReClient` (requests.rs:628-635) unaffected. Run `cargo test -p autore-app` (existing 614 tests still pass; new variants covered by an added roundtrip test).
  Must NOT do: do NOT add SQLite storage or migration logic. Do NOT wire handlers. Do NOT touch `autore-cli` or `autore-tui` (those land in Wave 11).
  Parallelization: Wave 1 | Blocked by: 1 | Blocks: 4, 5, 16 (Wave 3 persistence migrations reference IDs); 21 (Wave 4 scheduler commands)
  References: autore-app/src/application_service/requests.rs:292-308 (existing ApplicationCommand enum), :510-538 (existing request structs), :610-648 (AutoReClient trait + LocalAutoReClient); autore-app/src/application_service.rs:146-193 (execute+query dispatch match); autore-app/src/application_service/tests.rs (test pattern); Stage 1 spec §7.6 (transition-request commands), §17 (durable persistence list), §3.1 (canonical ownership rule)
  Acceptance criteria (agent-executable): `cargo build -p autore-app` succeeds with zero new warnings. `cargo test -p autore-app -- --nocapture` — all pre-existing tests pass AND a new test `application_command_stage1_variants_roundtrip` asserts at minimum the work-item lifecycle variants (Create, Promote, Lease, Renew, Complete, Fail, Block, Invalidate, Requeue) can be serialized via serde_json and deserialized back to equal values. `cargo clippy -p autore-app --all-targets -- -D warnings` clean.
  QA scenarios: happy — `cargo test -p autore-app application_command_stage1_variants_roundtrip -- --nocapture` reports `1 passed`; failure — flip one variant's serialize tag at edit time, the test fails with a serialization mismatch. Evidence: `.omo/evidence/auto-re-stage-1/task-2-app-commands.txt`
  Commit: Y | feat(app): add Stage 1 ApplicationCommand/Query variant skeletons

- [x] 3. Add Stage 1 schema types (records, kinds, IDs) — additive only
  What to do / Must NOT do: Extend `autore-schema` with new domain records mirroring §17 persistence list: `ReconstructionCampaign`, `ReconstructionWorkItem` (generalized from existing `Task` per autore-schema/src/domain/task/mod.rs:79-301 — keep existing `Task` state machine, add 18 work-item-kind enum per §7.2), `WorkDependency`, `WorkFingerprint`, `WorkLease` (extends existing `TaskLease`), `ProviderInstallation`, `ProviderInstance`, `ProviderRunRecord` (extends existing `ProviderRun`), `CapabilityDescriptor`, `NativeArtifactSnapshot` (extends existing `NativeArtifact`), `DynamicObservation`, `RawLlmResponse`, `ParsedLlmResult`, `ConflictRecord` (extends existing `Contradiction`), `GeneratedSourceMapping`, `BuildAttempt`, `BuildDiagnostic`, `VerificationScenario`, `VerificationComparison`, `RepairAttempt`, `BlockedReason`. Add namespaced kind strings: `recon.campaign`, `recon.work-function`, `recon.work-type`, ..., `provider.installation`, `provider.instance`, `debug.observation`, `llm.raw-response`, `llm.parsed-result`, `build.attempt`, `build.diagnostic`, `verify.scenario`, `verify.comparison`, `recon.mapping`. Add typed IDs via the existing `define_id!` macro pattern from autore-schema/src/ids.rs for: `ReconstructionCampaignId`, `WorkItemId`, `ProviderInstallationId`, `ProviderInstanceId`, `CapabilityDescriptorId`, `DynamicObservationId`, `RawLlmResponseId`, `ParsedLlmResultId`, `ConflictRecordId`, `GeneratedSourceMappingId`, `BuildAttemptId`, `BuildDiagnosticId`, `VerificationScenarioId`, `VerificationComparisonId`, `RepairAttemptId`. Use UUIDv7 (consistent with autore-schema IDs — workspace Cargo.toml:38 already has `uuid = { features = ["v4", "v7", "serde"] }`). Commit serialization fixtures for every new record using the existing `serde_json::to_string` fixture pattern referenced by Stage 0 plan (Stage 0 had `#[serde(tag=..., content=...)]` requirement — apply the same here).
  Must NOT do: do NOT remove or rename existing IDs/records (Stage 0 backward-compatibility invariant). Do NOT modify the existing `Task` type — extend it via composition (a `WorkItem` that wraps `Task` plus new fields). Do NOT change `ContentHash` (still BLAKE3 per Stage 0). Do NOT add migrations.
  Parallelization: Wave 1 | Blocked by: 1 | Blocks: 16, 6 (Wave 2 provider runtime references CapabilityDescriptor + ProviderInstallation), 11 (Wave 3 IDA provider returns snapshot records)
  References: autore-schema/src/domain/task/mod.rs:79-301 (existing Task); autore-schema/src/ids.rs (define_id! macro per Stage 0 plan); autore-schema/src/domain/mod.rs (ContentHash, EvidenceValue, NamespacedId structure); Stage 0 plan §3.7 + §26 (serde fixtures requirement); Stage 1 spec §6.4 (two-layer storage), §6.5 (identity NOT based on IDA row ids), §7.1 (ReconstructionCampaign fields), §7.2 (18 work-item kinds), §8.5 (raw-llm-response separate from parsed result), §11.3 (generated-source mapping model), §17 (full persistence inventory)
  Acceptance criteria (agent-executable): `cargo build -p autore-schema` succeeds. `cargo test -p autore-schema` passes (existing tests + new fixture tests for each new record). `cargo clippy -p autore-schema --all-targets -- -D warnings` clean. `grep -c '^fn ' autore-schema/src/ids.rs` shows at least +15 new `define_id!` invocations pre-blind-build.
  QA scenarios: happy — `cargo test -p autore-schema -- --nocapture` — all green including new `reconstruction_campaign_fixture`, `work_item_state_transitions_match_task`, `parsed_llm_result_fixture`. failure — changing a serde tag on one new record at edit → corresponding fixture test fails with diff. Evidence: `.omo/evidence/auto-re-stage-1/task-3-schema-records.txt`
  Commit: Y | feat(schema): add Stage 1 domain records, IDs, and serialization fixtures

- [x] 4. Replace direct WorkerRunner writes with autore-app commands (rep H4.3 §4.3)
  What to do / Must NOT do: In `autore-stage1/src/worker/runner.rs:42-80`, remove the `claims: Arc<dyn ClaimRepository>` and `evidence: Arc<dyn EvidenceRepository>` fields. Replace `WorkerRunner::new` with a constructor taking `Arc<dyn AutoReClient>` (from autore-app/src/application_service/requests.rs:610) instead. Reroute the runner's commit path so that on each successful analysis it issues `ApplicationCommand::AddHypothesis` (existing, requests.rs:300) with the model's claims as `candidate` EvidenceValue, the supporting evidence as `supporting_evidence` list, confidence derived from `FunctionAnalysisOutput.confidence`; it issues `ApplicationCommand::AddEvidence` (existing, requests.rs:299) for each referenced tool observation with provenance pointer to the provider-run-id. Mark the analysis-backed `Task` complete via `ApplicationCommand::CompleteWorkItem` (added by todo 2) — NOT directly through TaskRepository::complete (which scheduler/scheduler.rs:554 used). Preserve the existing `WorkerOutput` struct semantics (claims, evidence, analysis) for in-memory return value, but writes go through AutoReClient.
  Must NOT do: do NOT remove the in-memory return value shape — downstream code still reads `WorkerOutput.claims`. Do NOT delete the `ClaimRepository`/`EvidenceRepository` traits yet (other types still reference them — remove in Wave 11 cleanup). Do NOT remove scheduler.rs's `loader/internal` use of TaskRepository — only the worker's commit path.
  Parallelization: Wave 1 | Blocked by: 2, 1 | Blocks: 21 (Wave 4 scheduler must run via commands)
  References: autore-stage1/src/worker/runner.rs:42-80 (the WorkerRunner being refactored); autore-app/src/application_service/requests.rs:299-302 (AddHypothesis, AddEvidence commands), :610 (AutoReClient trait); autore-stage1/src/scheduler/scheduler.rs:251-257 (dispatch_tasks uses TaskRepository::lease_next — leave alone for this todo); Stage 1 spec §3.1 (+ §4.3 replace direct worker writes)
  Acceptance criteria (agent-executable): `cargo build -p autore-stage1 --no-default-features` succeeds. After refactor, `grep -n 'ClaimRepository' autore-stage1/src/worker/runner.rs` returns nothing. `grep -n 'EvidenceRepository' autore-stage1/src/worker/runner.rs` returns nothing. New test `worker_routes_writes_through_application` uses a `RecordingClient` (pattern from autore-tui/src/tui.rs:2174-2279) and asserts that a completed analysis pushes exactly one `AddHypothesis` + N `AddEvidence` + one `CompleteWorkItem` command. `cargo test -p autore-stage1 --no-default-features worker_routes_writes_through_application -- --nocapture` passes.
  QA scenarios: happy — the new test sees `[AddHypothesis{..}, AddEvidence{..}, CompleteWorkItem{..}]`. failure — leave an old `Arc<dyn EvidenceRepository>` field in place → test asserts the count of `AddEvidence` commands falls below the expected N and fails. Evidence: `.omo/evidence/auto-re-stage-1/task-4-worker-via-app.txt`
  Commit: Y | refactor(stage1/worker): route writes through ApplicationCommand; remove direct repos

- [x] 5. Stage-0 regression gate — ensure all 614 tests + fmt + clippy stay green
  What to do / Must NOT do: Run the full pre-existing Stage 0 verification suite exactly once at the wave boundary. Commands: `cargo test --workspace --exclude autore-stage1`, `cargo clippy --workspace --exclude autore-stage1 --all-targets -- -D warnings`, `cargo fmt --all --check`, `cargo build -p autore-stage1 --no-default-features`. Record outputs to evidence files. If any failures, file issues (do NOT fix them here — Wave 1 todos 2, 3, 4 introduced them; fix forward in W2 todo).
  Must NOT do: do NOT modify any source to fix a regression in this todo. Do NOT skip the `--exclude autore-stage1` flag on test/clippy — required by Stage 0 plan §32 #1. Do NOT run heavy PTY integration test in this gate (it's separate per README:87).
  Parallelization: Wave 1 | Blocked by: 2, 3, 4 | Blocks: 6 (Wave 2 starts fresh)
  References: README.md:84-91 (Stage 0 verification commands); Stage 0 plan §32 #1, #44, #45, #46 (gates preserved by Stage 1)
  Acceptance criteria (agent-executable): exit code 0 from each of the four commands above. The evidence file contains the `test result: ok. 614 passed;` line (count may rise >614 due to Todo 2 + 3 additions; assert no `failed: 0` mismatch). `cargo fmt --all --check` produces nothing on stdout and exits 0.
  QA scenarios: happy — all four commands return 0 with the asserted output strings present. failure — leave a `dbg!` macro from todo 4 in place → fmt check fails with `unexpected` diff, exit nonzero, evidence file contains the diff. Evidence: `.omo/evidence/auto-re-stage-1/task-5-wave1-gates.txt`
  Commit: N | (gate-only todo, no source change)

### Wave 2 — Provider protocol and fixture (Spec §18 Phase 2; §5)

- [x] 6. Add `proto/` schema tree + tonic+prost build pipeline; `autore.provider.v1` versioned package
  What to do / Must NOT do: Add `proto/autore/provider/v1/provider.proto` and child protos for: `bootstrap.proto`, `capability.proto`, `execution.proto`, `event.proto`, `health.proto`, `package.proto`. Define service `autore.provider.v1.Provider` with RPCs: `Negotiate(NegotiateRequest) returns (NegotiateResponse)` (unary), `DiscoverCapabilities(DiscoverRequest) returns stream (CapabilityDescriptor)`, `Execute(ExecutionRequest) returns stream (ExecutionEvent)`, `Health(HealthRequest) returns (HealthResponse)`, `GracefulShutdown(ShutdownRequest) returns (ShutdownResponse)`. Define `ExecutionEvent` as a `oneof` over `Accepted`, `Progress`, `Diagnostic`, `ObservationProduced`, `ArtifactProduced`, `Completed` per spec §5.1; every variant carries `provider_instance_id`, `request_id`, `operation_id`, `capability_id`, `capability_version`, monotonic `sequence`. Define `ArtifactDescriptor` (package_id, version, content_hash, relative_path, size). Define `CapabilityDescriptor` (capability_id, version, name, request_schema + response_schema as embedded `google.protobuf.Struct` or `bytes` per the crate's choice — JSON Schema `bytes` to match the existing `jsonschema = 0.33` dependency). Define `RequestDeadline` (absolute timestamp + relative budget). Add `autore-provider-protocol` crate with `build.rs` invoking `prost-build` + `tonic-build` to compile protos and emit Rust module tree `autore_provider_protocol::v1::*`. Add `tonic`, `prost`, `tonic-build`, `prost-build` to workspace dependencies in root `Cargo.toml`. The crate exposes client+server stubs and shared types but no runtime. Pin tonic at `0.12` and prost at `0.13` (or current tonic-compatible pair per Context7 query result in todo 6). **Add `autore-provider-protocol` to workspace `members` but NOT to `default-members`** — `cargo build` (no flags) must remain free of the `prost-build`/`protoc` dependency requirement, preserving the existing 7 Stage-0 default-members surface. Failure to register this exclusion regresses the Stage-0 backward-compat invariant (§G success criterion).
  Must NOT do: do NOT include business logic. Do NOT implement the runtime (that's todo 7). Do NOT lock the schema to a single provider; keep it capability-generic. Do NOT use a non-versioned package name (`autore.provider.v1` MUST appear in the `option java_package`-equivalent and `package` declaration; add a build-time test asserting the `v1` suffix).
  Parallelization: Wave 2 | Blocked by: 5 | Blocks: 7, 8, 9, 10
  References: Cargo.toml:27-50 (workspace deps to extend); existing migrations/V1..V13 (additive migration discipline reference); Stage 1 spec §5.1 (full RPC event list + monotonic sequence requirement), §5.4 (ArtifactTransport handle abstraction), §5.2 (package manifest schema), §5.3 (bootstrap with one-time secret, NO secrets in args). PRE-IMPL CHECK: invoke `skill name="context7_resolve-library-id"` query "tonic gRPC" → then `context7_query-docs` to identify the current tonic+prost compatible version pair (the plan names `tonic 0.12 + prost 0.13` but worker MUST verify that's the current compatible pair; if not, swap to the published current pair before pinning in Cargo.toml). Same for `prost 0.13`. Then use `libraryId: '/tokio-rs/tonic'` for current tonic 0.12 build.rs idioms and `libraryId: '/tokio-rs/prost'` for prost-build API.
  Acceptance criteria (agent-executable): `cargo build -p autore-provider-protocol` succeeds. `cargo test -p autore-provider-protocol` includes `version_suffix_present` which greps the generated `OUT_DIR` for `package autore.provider.v1;` strings and asserts presence. `cargo clippy -p autore-provider-protocol --all-targets -- -D warnings` clean.
  QA scenarios: happy — generated module contains `pub mod v1` with `provider.rs`, `event.rs`, etc. failure — change proto `package` line to `autore.provider` (no v1) → version_suffix test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-6-proto.txt`
  Commit: Y | feat(proto): add versioned autore.provider.v1 gRPC schema and codegen crate

- [x] 7. Implement `autore-provider-runtime` crate (bootstrap, auth, lifecycle, cancellation, limits)
  What to do / Must NOT do: New crate `autore-provider-runtime` depending on `autore-provider-protocol`. Add `autore-provider-runtime` to workspace `members` but NOT to `default-members` (same rule that keeps `autore-stage1` out of the default build surface; `cargo build` no-flags must remain free of provider-runtime's transitive `tonic`/`prost` build requirement, per Stage-0 backward-compat invariant §G). Implement: `CoordinatorBootstrap` struct that generates a `ProviderInstanceId` (UUIDv7), a one-time `BootstrapSecret` (32 random bytes from `getrandom`), launches a child process via `tokio::process::Command` (env-var-only bootstrap — `AUTORE_BOOTSTRAP_SOCKET`, `AUTORE_BOOTSTRAP_SECRET`, `AUTORE_BOOTSTRAP_INSTANCE_ID` environment variables; nothing in argv per spec §5.3), binds a Unix-domain-socket listener first, falls back to `127.0.0.1:0` loopback TCP if UDS unavailable (spec §5.3 local endpoint), accepts the inbound gRPC connection, performs `Authenticate` (provider must echo the secret as the first message over the established channel — byte-equal compare; failed compare → `AuthenticationError`), `Negotiate` protocol range (request declares `min_supported`, `max_supported`; coordinator accepts if `min_supported <= v1 <= max_supported`), verifies provider package identity (package ID + version + content_hash matches the manifest the coordinator started from per §5.2 identity rule), enforces `max_concurrency` (per capability), per-RPC `tokio::time::timeout` driven by the request's `RequestDeadline`, propagates `CancellationToken` (tokio-util) into inbound streams and the child process. Implements `ProviderRuntime::spawn(manifest, config_bundle, deadline) -> ProviderInstanceHandle` returning a handle holding the pid, instance_id, secret, channel, child process handle, capabilities cache, and a `JoinHandle<()>` for graceful shutdown. Implement `GracefulShutdownSeq`: send `GracefulShutdown` RPC, await child exit with 10s budget, on timeout `child.kill()` + reap. Implement crash detection: `ChildEvent::Exited(code)` / `ChildEvent::Signal(n)` surfaced via a stream on the handle; coordinator can subscribe.
  Must NOT do: do NOT put secrets in command-line arguments. Do NOT launch a provider that opens the project SQLite DB — providers receive only staged inputs. Do NOT implement remote providers (TCP listener stays on loopback). Do NOT implement the package dependency solver or registry (§5.6 deferred). Do NOT bind on a non-loopback interface in the TCP fallback path.
  Parallelization: Wave 2 | Blocked by: 6 | Blocks: 8, 9, 10
  References: tonic 0.12 docs (Context7) for server lifecycle; `tokio-util` CancellationToken (already a workspace dep); spec §5.3 (bootstrap procedure with 8 steps), §5.5 (fixture provider unit infra), Stage 0 Cargo.toml:30 (tokio with all features)
  Acceptance criteria (agent-executable): `cargo build -p autore-provider-runtime` succeeds. New test `bootstrap_secrets_never_in_argv` spawns a fixture echo provider, captures the launched child's cmd via `tokio::process::Command::as_std` introspection in a test stub, asserts no env var name appears in `std::env::args_os()`. Test `negotiate_rejects_unsupported_protocol` returns `NegotiateError::UnsupportedVersion`. Test `authentication_rejects_wrong_secret` closes the channel and surfaces `AuthenticationError`. Test `graceful_shutdown_within_10s` measures wall-clock. `cargo test -p autore-provider-runtime -- --nocapture` all pass.
  QA scenarios: happy — `bootstrap_secrets_never_in_argv` writes `[OK] secret not in argv` to evidence file. failure — insert `cmd.arg(&secret)` in `CoordinatorBootstrap::spawn` → test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-7-runtime-bootstrap.txt`
  Commit: Y | feat(providers/runtime): bootstrap, sealed auth, lifecycle, cancellation, limits

- [x] 8. Implement local provider package discovery, manifest validation, content-hash verification
  What to do / Must NOT do: New module `autore-provider-runtime::package`. Read a configured local provider roots list (operator config in `project.auto-re/provider_roots.toml` or default `<parent>/providers/` per spec §5.2). Each provider package is a directory containing `manifest.toml` with fields per §5.2: `schema_version`, `package_id` (namespaced), `version` (semver parse), `content_hash`, `entrypoint` (relative path, must resolve inside the package root), `protocol_range` (min/max supported), `capabilities` (list of CapabilityDescriptor), `max_concurrency` (per capability), `configuration_schema` (JSON Schema bytes). Validation per §5.2 list: manifest syntactic validity (toml parse + serde); namespaced package_id (regex `^[a-z0-9-]+\.[a-z0-9-]+$`); entrypoint exists; entrypoint containment (canonical path within package root, no `..` escape, no symlink escape per spec §5.2); package content hash (walk all files in package dir excluding `manifest.toml`, BLAKE3 each, feed sorted (path, hash) list into BLAKE3 again, compare to `manifest.content_hash`); protocol compatibility (coordinator's declared `v1` falls inside `protocol_range`); capability declaration is non-empty and each descriptor is well-formed (version ≥1, JSON Schema parses via `jsonschema = 0.33`); `configuration_schema` parses. Output a `PackageManifest` record + a `PackageInstallationIntent` returned to the coordinator. On validation failure return `PackageValidationError` with which-check-failed code.
  Must NOT do: do NOT implement network installation (§5.6 deferred). Do NOT implement package signing (§5.6 deferred). Do NOT implement the dependency solver (§5.6 deferred). Do NOT recurse into symlinked subtrees during hash walk — reject (not traverse) them.
  Parallelization: Wave 2 | Blocked by: 7 | Blocks: 9, 10
  References: Stage 0 README.md:34-45 (project layout: `project.auto-re/` root pattern — mirror for provider roots), Cargo.toml:41 (blake3 already a dep), :45 (jsonschema 0.33 already a dep); spec §5.2 (manifest fields + validation list), §3.1 (provider isolation invariant)
  Acceptance criteria (agent-executable): `cargo test -p autore-provider-runtime package::` runs `valid_manifest_loads`, `missing_entrypoint_fails`, `symlink_escape_rejected` (craft a temp package dir with a symlink to /etc/passwd), `protocol_range_outside_v1_rejected`, `content_hash_mismatch_fails`, `namespaced_id_rejects_uppercase`. `cargo clippy -p autore-provider-runtime --all-targets -- -D warnings` clean.
  QA scenarios (name the exact tool + invocation): happy — `valid_manifest_loads` returns a `PackageManifest` whose `package_id == "fixture.echo"`. failure — flip one byte in a fixture data file → `content_hash_mismatch_fails` returns `PackageValidationError::ContentHashMismatch`. Evidence: `.omo/evidence/auto-re-stage-1/task-8-package-validation.txt`
  Commit: Y | feat(providers/runtime): local package discovery + manifest validation + content hashing

- [x] 9. Implement `ArtifactTransport` abstraction with core-created local staging directories + scoped handles
  What to do / Must NOT do: New module `autore-provider-runtime::artifact`. Define trait `ArtifactTransport` (async) with methods: `stage_inbound(bytes: Bytes) -> ArtifactHandle` (core writes provider output to a staging dir it creates under `project.auto-re/staging/<instance>/<uuid>/`); `stage_outbound(handle: &ArtifactHandle) -> PathBuf` (core exposes a staged file to a provider read-only via a handle, never the canonical artifacts path); `commit_inbound(handle: &ArtifactHandle, kind: NamespacedId, hash: ContentHash) -> ArtifactId` (core computes its own BLAKE3 of the staged bytes; if mismatch fails, handle dropped); `discard(handle: &ArtifactHandle)` (sweeps unfinished staging; called on incomplete imports and restart reconciliation per §17). `ArtifactHandle` is opaque to providers — only `PathBuf` ever crosses the provider boundary via `stage_outbound`. FIRST impl `LocalStagingTransport` rooted at `project.auto-re/staging/`. Staging dir layout: `<project>/staging/<instance_id>/<request_id>/<artifact_uuid>/data`. Provider receives the `PathBuf` from `stage_outbound`; it does NOT know the canonical `artifacts/<algo>/<prefix>/<digest>/data` layout (spec §5.4). Commit math: core independently recomputes the hash; providers cannot choose canonical destinations. Build a `StagingReconciler::sweep()` that on startup walks `<project>/staging/` and removes entries with no associated in-flight `ImportProviderRunResult` operation.
  Must NOT do: do NOT pass canonical artifact paths to providers. Do NOT trust provider-supplied hashes. Do NOT commit an artifact before the surrounding result transaction commits (spec §5.4 "Artifact import occurs only with the surrounding result transaction"). Do NOT implement remote object storage (§5.4 explicit the seam admits it later — abstraction stays trait-shaped). Do NOT make `stage_outbound`
return bare `PathBuf` — make it return `ArtifactLocation` (an enum over `Local(PathBuf)` + `Remote(Uri)`) so the trait admits remote transports without changing capability semantics per §23 architectural test Q4. Do NOT bake lioka physical-path assumptions into provider capability contracts.
  Parallelization: Wave 2 | Blocked by: 8 | Blocks: 10
  References: Stage 0 README.md:34-45 (canonical artifacts/<algo>/<prefix>/<digest>/data layout — must NOT leak to providers); Cargo.toml:41 (blake3); spec §5.4 (ArtifactTransport requirements), §17 (staging cleanup on restart)
  Acceptance criteria (agent-executable): `cargo test -p autore-provider-runtime artifact::` runs `staging_layout_is_instance_scoped`, `handle_never_exposes_canonical_path`, `commit_recomputes_hash_independently`, `commit_rejects_mismatched_hash`, `discard_removes_staging_dir`, `reconciler_sweeps_orphans_on_startup`. Assertions include string comparison that the `PathBuf` returned to a mock provider does NOT contain the canonical `artifacts/sha256` or `artifacts/blake3` segment.
  QA scenarios: happy — `commit_recomputes_hash_independently` writes a 1KB blob then calls commit; the recorded `ContentHash` equals BLAKE3 of the blob. failure — provider supplies a wrong hash → `commit_rejects_mismatched_hash` returns `ArtifactError::HashMismatch`. Evidence: `.omo/evidence/auto-re-stage-1/task-9-artifact-transport.txt`
  Commit: Y | feat(providers/runtime): ArtifactTransport staging abstraction with scoped handles

- [x] 10. Implement fixture provider binary with 5 capabilities (echo/delay/fail/artifact/large-stream)
  What to do / Must NOT do: New workspace member `providers/fixture/` (off `default-members` in root Cargo.toml, mirroring the autore-stage1 exclusion pattern at Cargo.toml:12-20). `main.rs` reads bootstrap env (`AUTORE_BOOTSTRAP_SOCKET`, `AUTORE_BOOTSTRAP_SECRET`, `AUTORE_BOOTSTRAP_INSTANCE_ID`) per spec §5.3, establishes UDS connection to the coordinator, performs `Authenticate` (echo the secret), enters `Negotiate`, then serves the `Provider` gRPC service. Implements 5 capabilities as `CapabilityDescriptor` entries: `fixture.echo` (returns the input bytes as ObservationProduced), `fixture.delay` (sleeps `request.deadline.relative` then emits Progress + Completed), `fixture.fail` (returns `Diagnostic{severity=Error}` + `Completed{status=Failed}`), `fixture.artifact` (writes a 64KiB deterministic blob to a path obtained via `stage_outbound` handle and emits `ArtifactProduced` with descriptor), `fixture.large-stream` (emits 1024 `Progress` events then `Completed`, exercises flow control). Manifest at `providers/fixture/manifest.toml` declares `package_id = "fixture.echo"`, `version = "0.1.0"`, `content_hash = "<blake3 of package>"`, `entrypoint = "bin/fixture-provider"`, `protocol_range = [1, 1]`, `capabilities = [...]`, `max_concurrency = 4`, `configuration_schema = <empty JSON schema>`. The provider honors per-RPC `RequestDeadline` (aborts on overrun) and a `CancellationToken` privileged on `GracefulShutdown` (terminates within 5s).
  Must NOT do: do NOT make the fixture provider part of the workspace default-members. Do NOT use it as the production deliverable — it's test infrastructure per §5.5. Do NOT depend on the project SQLite store. Do NOT spawn subprocesses outside the fixture's own pid.
  Parallelization: Wave 2 | Blocked by: 9 | Blocks: 11 (Wave 3 IDA), 26 (Wave 5 LLM), 36 (Wave 6 build)
  References: root Cargo.toml:1-20 (workspace member discipline); spec §5.5 (fixture capability list), §5.3 (bootstrap), §5.1 (event emission order — Accepted → Progress → ... → Completed)
  Acceptance criteria (agent-executable): `cargo build -p fixture-provider --no-default-features` succeeds. New integration test `fixture_provider_five_capabilities` in `autore-provider-runtime/tests/fixture.rs` uses `CoordinatorBootstrap::spawn` with `PackageManifest = fixture.echo`, dispatches one request per capability, asserts observed event ordering for each: echo → `[Accepted, ObservationProduced, Completed]`; delay → `[Accepted, Progress, Completed]`; fail → `[Accepted, Diagnostic, Completed{Failed}]`; artifact → `[Accepted, ArtifactProduced, Completed]`; large-stream → `[Accepted, Progress × 1024, Completed]`. Asserts every event carries matching `provider_instance_id`, `request_id`, `sequence` strictly monotonic per spec §5.1.
  QA scenarios: happy — `fixture_provider_five_capabilities` completes each capability in <2s wall-clock and sequences match. failure — fixture emits Progress with a non-monotonic sequence → test detects the regression in the monotonicity assertion. Evidence: `.omo/evidence/auto-re-stage-1/task-10-fixture-provider.txt`
  Commit: Y | feat(providers/fixture): external fixture provider with 5 capabilities

### Wave 3 — IDA whole-program ingestion (Spec §18 Phase 3; §6)

- [x] 11. Additive refinery migrations V14..V23 for reconstruction records (campaigns, work items, deps, fingerprints, leases, provider installations/instances/runs, capability descriptors, native snapshots, dynamic observations)
  What to do / Must NOT do: Add new migration files under `migrations/` following the existing V1..V13 naming pattern — `V<n>__<snake_case_name>.sql` with the double-underscore separator immediately after the version number (as the existing `V1__initial_schema.sql`, `V2__stage0_projects.sql`, ... per `ls migrations/`). Each todo that introduces migrations uses this exact format. V14 `V14__reconstruction_campaigns.sql` (reconstruction_campaigns table: id BLOB PK, project_id BLOB FK, binary_artifact_id BLOB FK, binary_revision_id BLOB, output_target_id BLOB, provider_policy TEXT json, model_policy TEXT json, build_policy TEXT json, verification_policy TEXT json, completion_policy TEXT json, state TEXT, created_at, updated_at). V15 `__work_items.sql` (reconstruction_work_items: id BLOB PK, campaign_id BLOB FK, kind TEXT, subject_entity_id BLOB, subject_address INT, subject_kind TEXT, priority INT, state TEXT, dependencies_summary TEXT, required_capabilities TEXT json, preferred_worker BLOB, preferred_model_class TEXT, maximum_attempts INT, attempt_count INT, input_revision INT, fingerprint BLOB, blocked_reason TEXT). V16 `__work_dependencies.sql` (id BLOB PK, predecessor BLOB FK, successor BLOB FK, edge_kind TEXT). V17 `__work_fingerprints.sql` (id BLOB PK, work_item_id BLOB FK, fingerprint BLOB, computed_from TEXT json, computed_at). V18 `__work_leases.sql` (extending the proven V1 leases table pattern; references Wave-1 records). V19 `__provider_installations.sql` (id BLOB PK, package_id TEXT, version TEXT, content_hash BLOB, capabilities TEXT json, max_concurrency_per_cap TEXT json, configuration_schema TEXT json, root_path TEXT). V20 `__provider_instances.sql` (id BLOB PK, installation_id BLOB FK, instance_id BLOB, status TEXT, started_at, ended_at, exit_code INT NULL). V21 `__provider_runs.sql` (id BLOB PK, instance_id BLOB FK, request_id BLOB, operation_id BLOB FK to operations, status TEXT, started_at, ended_at, error_message NULL). V22 `__capability_descriptors.sql` (id BLOB PK, installation_id BLOB FK, capability_id TEXT, version INT, request_schema BLOB json, response_schema BLOB json). V23 `__native_artifact_snapshots.sql` (id BLOB PK, work_item_id BLOB NULL, provider_run_id BLOB FK, kind TEXT, artifact_id BLOB FK to artifacts, captured_at). Use `BLOB` for all UUIDv7 ids (per existing Stage 0 migration practice). Every migration is `CREATE TABLE IF NOT EXISTS` plus forward-only additive indexes (`CREATE INDEX IF NOT EXISTS`). DO NOT modify V1..V13.
  Must NOT do: do NOT rewrite V1..V13 (Stage 0 invariant). Do NOT use DDL that drops columns. Do NOT use DB-generated ids (Stage 0 §3.8 invariant — all ids are uuid-v7 application-supplied). Do NOT include foreign-key cascade deletes (waves 17 reconciliation rules out destructive cascades).
  Parallelization: Wave 3 | Blocked by: 3, 5 | Blocks: 12, 13, 14, 15
  References: migrations/V1..V13 (the additive discipline reference); autore-store/src/storage/database.rs (refinery embed_migrations!), autore-store/src/storage/event_store.rs:81,146,358 (atomic commit pattern); Stage 1 spec §17 (persistence list), §6.4 (two-layer storage — the Snapshot rows stay heavy enough for scheduling, low-frequency records)
  Acceptance criteria (agent-executable): `cargo test -p autore-store` runs `migrations_apply_clean`, `migrations_v14_to_v23_idempotent` (run `cargo run -p autore-cli -- project migrate` twice — second invocation is a no-op), `migrations_v14_to_v23_rollback_safe` (begin transaction, apply V14..V23, ROLLBACK, db unchanged from clean V13 state). Existing 614 tests pass. `cargo clippy -p autore-store --all-targets -- -D warnings` clean.
  QA scenarios: happy — `cargo test -p autore-store migrations` green; second `project migrate` reports no-op. failure — edit V14 to `DROP TABLE reconstruction_campaigns` — `migrations_v14_to_v23_rollback_safe` fails since on subsequent apply the table is missing. Evidence: `.omo/evidence/auto-re-stage-1/task-11-migrations-v14-v23.txt`
  Commit: Y | feat(store): additive V14..V23 migrations for reconstruction records

- [ ] 12. Wire Stage 1 application commands to storage (handlers for campaign/work-item/provider records)
  What to do / Must NOT do: Replace the stub arms added in todo 2 in `autore-app/src/application_service.rs:149-167` with real handlers. Each handler does: validate request (existing validate_not_empty, validate_id_present patterns); open a single SQLite transaction via `Database::begin_transaction` (per autore-store/src/storage/database.rs:111-114); insert/update the matching row in V14..V23 tables; construct a `ProjectEvent` matching the variant; emit via the existing atomic `EventStore::with_event` (autore-store/src/storage/event_store.rs:81); commit. Variants with handlers in this todo: `CreateReconstructionCampaign` (INSERT row, emit `ProjectEvent{kind=recon.campaign-created}`), `CreateWorkItems` (batch INSERT, emit single aggregation event), `PromoteWorkItem`, `LeaseWorkItem`, `RenewWorkLease`, `CompleteWorkItem`, `FailWorkItem`, `BlockWorkItem` (+`BlockWorkWithReason` writes BlockedReason), `InvalidateWorkItem`, `RequeueWorkItem`, `RegisterProviderInstallation`, `RegisterProviderInstance`, `StopProviderInstance`, `ImportProviderRunResult` (atomic import of observations + artifacts in the surrounding transaction per spec §5.4), `ImportDynamicObservation`, `RecordBuildAttempt`, `RecordVerificationComparison`, `RecordRepairAttempt`, `RegisterGeneratedSourceMapping`, `InvalidateGeneratedSource`, `AcceptHypothesisPolicyDriven`. **Handler ownership for the 7 command variants whose handler body is deferred to a later wave** (per Oracle round-2 residual — these handlers are owned by their first-using wave, NOT by todo 12 — they stay as `Err("not yet implemented")` stubs until the wave that first invokes them adds the real implementation): `RecordWorkDependency` → handler wiring owned by todo 17 (Wave 4) where the `WorkGraphBuilder` first issues it; `RunBuild` → handler wiring owned by todo 39 (Wave 8) where the `DeclarationGenerator` first issues it; `ImportGeneratedSourceCandidates` → handler wiring owned by todo 42 (Wave 9) where the patch-control pipeline first issues it; `ScheduleVerificationRegression` → handler wiring owned by todo 47 (Wave 10) where the regression selector first issues it; `PauseCoordinator`, `ResumeCoordinator`, `StopCoordinator` → handler wiring owned by todo 49 (Wave 11) where the coordinator loop first drives them. Each first-using-wave todo MUST add a sub-step "wire the `execute` handler for the `X` command, replacing its todo-2 stub with a real atomic state+event transaction per the same pattern used by todo 12." Use the existing `with_event` (autore-store/src/storage/event_store.rs:81) to keep the state-write + event-emit atomic (Stage 0 §3.3 invariant).
  Must NOT do: do NOT split state-write and event-emit into separate transactions (atomic-or-fail, full stop). Do NOT call any storage layer other than the autore-app handlers. Do NOT introduce provider-runtime or any business logic in here — handlers are thin write-shells.
  Parallelization: Wave 3 | Blocked by: 11, 2 | Blocks: 13, 21 (Wave 4 scheduler will invoke these)
  References: autore-app/src/application_service.rs:146-193 (existing execute dispatch), autore-store/src/storage/event_store.rs:81,146,358 (with_event atomic pattern), autore-store/src/storage/database.rs:111-157 (Transaction); Stage 1 spec §3.1 (+ §4.4 the "Phase 1 exit criterion is one canonical mutation path"), §7.6 (transition command names)
  Acceptance criteria (agent-executable): `cargo test -p autore-app` includes new tests: `create_reconstruction_campaign_persists_row_and_emits_event`, `create_work_items_batch_inserts`, `lease_work_item_transitions_state`, `complete_work_item_lease_removed`, `block_work_with_reason_records_blocked_reason`, `import_provider_run_result_atomic_rolls_back_on_partial_fail` (inserts a 3-observation ImportProviderRunResult where the 2nd observation's referenced artifact handle was discarded mid-tx → entire tx rolls back, no rows persist). All pre-existing autore-app tests pass.
  QA scenarios: happy — the atomic-rollback test asserts zero rows added on partial fail. failure — break atomicity by moving event emit out of the tx → the rollback test sees rows persist after the failure and fails. Evidence: `.omo/evidence/auto-re-stage-1/task-12-app-handlers.txt`
  Commit: Y | feat(app): Stage 1 command handlers with atomic state+event commits

- [ ] 13. Implement external IDA provider binary `providers/ida/` over `idax = 0.3.0`
  What to do / Must NOT do: New workspace member `providers/ida/` (off `default-members`). Binary `bin/ida-provider` reads the same bootstrap env as fixture (todo 10). On `Negotiate` declares capabilities: `ida.binary.open`, `ida.binary.ingest`, `ida.program.refresh`, `ida.function.snapshot`, `ida.type.snapshot`, `ida.class.snapshot`, `ida.references.query`, `ida.reanalyze`, `ida.native-artifact.export` (spec §6.2). `ida.binary.open` calls `idax::database::init()` then `idax::database::open(binary_path, read_only=true)` (per existing pattern at autore-stage1/src/engine.rs:36-42 — preserve the init/open call shape). `ida.binary.ingest` walks the whole binary (per §6.3 list) and emits stream events: `Progress` per stage (segments, imports, exports, functions, refs, strings, globals, rtti, vtables, static initializers, decompiler), `ObservationProduced` per batch of canonical entities discovered, `ArtifactProduced` for each snapshot file (disassembly, decompilation, CFG, instruction-list, native type dumps, IDA db snapshot). At end emits `Completed`. Write snapshot files to the `stage_outbound(handle)` PathBuf from the runtime — never to canonical artifacts. `ida.program.refresh` re-walks the binary and emits only deltas — known entities unchanged: `Progress` marker only; new entity: `ObservationProduced`; removed/changed: `Diagnostic{severity=Warning, code=stale}` (mark-stale, do not delete per §6.6). `ida.function.snapshot` returns per-function snapshot artifact (decompilation + disassembly + CFG). `ida.type.snapshot`, `ida.class.snapshot`, `ida.references.query` similar — typed responses, native artifacts via staged handles. `ida.reanalyze` calls the IDA analyze-c API with the supplied address range. `ida.native-artifact.export` exports an IDA-native artifact (raw IDA database snapshot, .idareg, or similar) by writing to the staged handle.
  Must NOT do: do NOT let the IDA provider open the project SQLite. Do NOT use IDA row ids as canonical entity ids (todo 14 owns identity). Do NOT collapse ingestion — the high-level `ida.binary.ingest` MUST cover the entire executable per §6.2. Do NOT bundle IDA in the binary — assume IDA Pro is installed on the conductor's machine; the provider process binding comes from `idax`. Do NOT depend on the `gdbstub` crate — debugger integration lands in Wave 7; this provider is static-only for Wave 3.
  Parallelization: Wave 3 | Blocked by: 12, 9 | Blocks: 14, 41 (Wave 7 extends with debug capabilities)
  References: autore-stage1/Cargo.toml:36 (idax = 0.3.0); autore-stage1/src/engine.rs:36-42 (existing idax open pattern); spec §6.1 (IDA provider owns all IDA-native state, core never imports IDA SDK types), §6.2 (capability list), §6.3 (extraction list), §6.4 (two-layer storage), §6.6 (refresh semantics)
  Acceptance criteria (agent-executable): `cargo build -p ida-provider --no-default-features` succeeds. `cargo test -p ida-provider -- --nocapture` includes `binary_open_succeeds_on_valid_idb` (uses a committed tiny .idb fixture from `autore-stage1/tests/fixtures/` — if no fixture exists yet, create one in this todo from `samples/hello.idb`), `binary_ingest_emits_progress_per_stage`, `binary_ingest_writes_snapshot_artifacts_to_staging` (asserts PathBuf from `stage_outbound` does NOT contain `artifacts/sha256`), `program_refresh_emits_only_deltas_for_unchanged`, `program_refresh_marks_removed_as_stale` (Diagnostic code=stale, never deletes), `function_snapshot_returns_typed_result`. The provider must NOT crash if idax init failure (returns `Diagnostic{Error}` + `Completed{Failed}`).
  QA scenarios: happy — `binary_ingest_writes_snapshot_artifacts_to_staging` returns at least 6 artifact descriptors (one per snapshot kind). failure — change ida-provider to mutate the IDA DB at refresh → assertion `program_refresh_emits_only_deltas_for_unchanged` sees unexpected `ObservationProduced` events for unchanged entities. Evidence: `.omo/evidence/auto-re-stage-1/task-13-ida-provider.txt`
  Commit: Y | feat(providers/ida): external IDA provider over idax 0.3.0 with whole-binary ingestion

- [ ] 14. Build canonical entity identity scheme (no IDA row ids) + import observations through autore-app
  What to do / Must NOT do: New module `autore-reconstruction::identity`. **First appearance of the `autore-reconstruction` crate — register it in workspace `members` but NOT `default-members`** (it transitively depends on `autore-provider-runtime` → `auto-provider-protocol` → `tonic`/`prost` build-time deps; `cargo build` no-flags must remain free of `protoc`, per Stage-0 backward-compat invariant §G). Implement `CanonicalEntityKey` constructed from: `binary_revision_id || address_space || entry_address || entity_kind || provider_native_extension` (per spec §6.5). For IDA-sourced entities `provider_native_extension` carries IDA's `ea` (effective address) and ida-function-row-uuid as a namespaced extension column on `semantic_entities` (new additive column in V14 migration append: `external_identities TEXT NULL` storing a JSON map of `provider_package_id → {native_id, native_kind}`). Implement `import_ingestion_observations` that takes a batch of `ObservationProduced` events from the IDA provider and issues (via the autore-app commands from todo 12) `ApplicationCommand::RegisterEntity` per new entity (existing command, requests.rs:296) with a `StableEntityKey` derived from the CanonicalEntityKey; for entities that already exist by key, it issues `ApplicationCommand::ImportProviderRunResult` linking the existing entity to the new observation artifact. For removed entities (Diagnostic code=stale), it does NOT delete — it issues `BlockWorkItem` with reason `ProviderObservedStaleEntity` and creates a follow-up `Investigation` work item via `CreateWorkItems` (per spec §6.6 "Mark removed or changed observations as stale rather than deleting history"). Classify the work item kind for each entity (function→Function; type→Structure; etc.).
  Must NOT do: do NOT use IDA's `ea` or function-row-uuid AS the canonical entity id — those go ONLY into `external_identities`. Do NOT delete entities on refresh (mark stale + spawn investigation only). Do NOT invent a `CanonicalEntityKey` that includes mutable provenance (only structural identity goes in the key).
  Parallelization: Wave 3 | Blocked by: 13, 12 | Blocks: 15, 21, 41
  References: Stage 0 autore-schema/src/domain for `SemanticEntity` and `StableEntityKey` (committed fixtures per Stage 0 plan §3.7); spec §6.5 (identity rule), §6.6 (refresh + invalidation), §3.1 (canonical ownership — all writes through autore-app)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction identity::` runs `canonical_key_is_stable_across_refresh`, `canonical_key_excludes_ida_row_id`, `import_observations_creates_entities_with_stable_keys`, `import_observations_rematches_by_key_on_refresh`, `import_observations_does_not_delete_on_stale` (asserts the entity remains in semantic_entities and a BlockWorkItem command was issued), `import_observations_creates_investigation_for_stale`. `cargo clippy -p autore-reconstruction --all-targets -- -D warnings` clean.
  QA scenarios: happy — `canonical_key_excludes_ida_row_id`: an IDA-generated `ea=0x401000` and an ea-after-relocation `0x5001000` for the same logical function at the same binary_revision/entity_kind map to the same canonical id because the key uses `address_space + entry_address` plus `external_identities` carries both eas. failure — include `ea` field in the canonical key itself → the test on `canonical_key_is_stable_across_refresh` fails because two refresh feeds at different eas produce two canonical ids for one logical entity. Evidence: `.omo/evidence/auto-re-stage-1/task-14-canonical-identity.txt`
  Commit: Y | feat(reconstruction/identity): canonical entity key without IDA row ids + observation importer

- [ ] 15. End-to-end IDA importer on a small .idb; integration test asserts canonical graph is stable
  What to do / Must NOT do: New integration test `tests/ida_full_ingest.rs` (in autore-reconstruction). Steps exercised in the test: (1) `cargo run -p autore-cli -- project create --name test-binary`; (2) `auto-re artifact add --file samples/hello.exe --kind core.binary`; (3) build a `ProviderInstallation` from `providers/ida/manifest.toml`; (4) bootstrap the provider via `CoordinatorBootstrap::spawn`; (5) dispatch `ida.binary.open` with the ArtifactId, wait for Completed; (6) dispatch `ida.binary.ingest`, collect all events; (7) on `Completed`, run `import_ingestion_observations` from todo 14 — assert it issues at least N entity registrations (N = sample-binary's true function count, asserted); (8) re-run `ida.program.refresh` against the SAME .idb, assert no new entity registrations issued (only `Progress` events observed); (9) close the .idb externally (simulate IDA-side change by re-saving a slightly modified .idb under a new file, refreshing, asserting the changed entity issued a stale Diagnostic + a new investigation work item). Assert throughout that no canonical mutation happened except through ApplicationCommand (grep on the test's RecordingAutoReClient to verify all canonical writes are commands — zero direct SQL strings).
  Must NOT do: do NOT skip any of the 9 steps. Do NOT use a fixture-only IDA substitute — must be the real idax binding against a real (small) .idb. (If samples/hello.idb is missing, create one in this todo by running IDA once on samples/hello.exe — instructions in the test preamble; commit the resulting ~few hundred KB .idb under `autore-reconstruction/tests/fixtures/`.) Do NOT count specific canonical rows (this test asserts structural stability, not exact counts — declare N as a fixture constant).
  Parallelization: Wave 3 | Blocked by: 14, 13 | Blocks: 21, 41
  References: README.md (existing CLI commands — Stage 0 CLI is reused); samples/hello.exe or a small fresh fixture; spec §6.8 (Wave 3 exit criterion: whole Van Buren exe ingested — at this todo we only prove the slice; the full Van Buren exe runs in Wave 11)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test ida_full_ingest -- --nocapture --ignored` (ignored-tag for slow integration suite, gated from default `cargo test`). The test prints its 9 phase markers; final assertion: `apply count of canonical mutations went through ApplicationCommand == apply count of canonical mutations total`. `cargo clippy -p autore-reconstruction --all-targets -- -D warnings` clean.
  QA scenarios: happy — end-to-end runs green, prints `[OK] refresh stable; 0 unexpected new entities`. failure — refactor todo 14 to write SQL directly → the assertion `all canonical mutations were ApplicationCommand` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-15-ida-end-to-end.txt`
  Commit: Y | test(reconstruction): end-to-end IDA ingest + refresh stability on small .idb fixture

### Wave 4 — Whole-program reconstruction graph (Spec §18 Phase 4; §7)

- [ ] 16. Additive migrations for conflict records, generated-source mappings, blocked reasons (Wave 4 tail-set; complements Wave 3's campaigns/work-items)
  What to do / Must NOT do: V24 `V24__conflict_records.sql` (id BLOB PK, work_item_id BLOB FK, kind_a TEXT, kind_b TEXT, evidence_a BLOB json, evidence_b BLOB json, status TEXT, created_at, resolved_at NULL). V25 `V25__generated_source_mappings.sql` (id BLOB PK, work_item_id BLOB FK, entity_id BLOB FK, declaration_artifact_id BLOB, definition_artifact_id BLOB, generation_operation_id BLOB FK to operations, build_attempt_id BLOB NULL, verification_comparison_id BLOB NULL, last_change_hash BLOB, last_change_at). V26 `V26__blocked_reasons.sql` (id BLOB PK, work_item_id BLOB FK, reason_kind TEXT, detail TEXT json, created_at). V27 `V27__repair_attempts.sql` (id BLOB PK, work_item_id BLOB FK, build_attempt_id BLOB NULL, attempt_seq INT, kind TEXT, sponsor TEXT, status TEXT, created_at, ended_at NULL). Keep refinery additive discipline; IF NOT EXISTS; no destructive cascades. File-name format `V<n>__<name>.sql` exactly per V1..V13 reference.
  Must NOT do: do NOT modify V14..V23. Do NOT introduce cascade deletes (Wave 17 invariants forbid). Do NOT bump the project schema_version (still 2.0).
  Parallelization: Wave 4 | Blocked by: 5 | Blocks: 17, 18, 19, 20
  References: migrations/V1..V13 (additive discipline); autore-store/src/storage/database.rs (refinery embedded migrations), autore-store/src/storage/event_store.rs:81 (atomic event commit); spec §7.6 (transition commands), §10.3 (conflict records), §11.3 (generated-source mapping model)
  Acceptance criteria (agent-executable): `cargo test -p autore-store` includes `migrations_v24_to_v27_idempotent`, `migrations_v14_to_v27_rollback_safe`. `cargo run -p autore-cli -- project migrate` on a fresh project must succeed and the second invocation must be a no-op. Existing autore-store tests remain green.
  QA scenarios: happy — V14..V27 clean apply on a fresh DB; second `project migrate` is a no-op (refinery version unchanged). failure — add a DROP COLUMN to V24 → rollback-safe test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-16-migrations-v24-v27.txt`
  Commit: Y | feat(store): additive V24..V27 migrations for conflicts, mappings, blocked reasons, repair attempts

- [ ] 17. Implement 18 work-item-kind enum + dependency graph construction with SCC collapse
  What to do / Must NOT do: In `autore-reconstruction::work_graph`. Define enum `WorkItemKind` per spec §7.2: `ProgramSkeleton`, `ExternalDependency`, `Global`, `Enum`, `Structure`, `Class`, `Vtable`, `Function`, `FunctionCluster`, `StaticInitializer`, `Subsystem`, `Entrypoint`, `Investigation`, `Generation`, `BuildFailure`, `LinkFailure`, `VerificationFailure`, `ConflictResolution`. Implement `WorkGraphBuilder` that consumes the imported canonical structural graph (entities from todo 14) and emits `CreateWorkItems` commands: one `Function` work item per `core.function` entity (issued via `ApplicationCommand::CreateWorkItems` per todo 12); one `Structure` per `core.type` entity; one `Class` per discovered class; one `Vtable` per discovered vtable; one `Global` per `core.global` entity; one `Enum` per discovered enum; `ExternalDependency` per imported external function (kind `core.external-function`); `StaticInitializer` per static-init/destruct observation; `ProgramSkeleton` singleton; `Entrypoint` per address-entrypoint. Implement `DependencyEdgeKind` enum per spec §7.3 (DirectCall, IndirectCallHypothesis, TypeUsage, GlobalAccess, VtableMembership, CtorDtor, StaticInit, GeneratedDeclRequirement, BuildDependency, VerificationDependency). Construct the dependency graph using `petgraph` (already workspace dep): nodes = WorkItemIds, edges = typed by DependencyEdgeKind. After initial graph is built, run `petgraph::algo::kosaraju_scc` and collapse recursive call cycles into a single `FunctionCluster` work item per SCC of size >1 (spec §7.3 "Collapse recursive call cycles into strongly connected function clusters"). The collapsed cluster's dependencies are the union of its members' outgoing non-cycle edges. The members become "sub-items" of the cluster (linked row in `work_dependencies` with `edge_kind = ClusterMember`).
  Must NOT do: do NOT pretend the call graph is acyclic — SCC collapse is REQUIRED. Do NOT collapse singleton SCCs (size==1 with no self-loop) — they stay as plain Function work items. Do NOT collapse an SCC containing mixed kinds (Function + Vtable) — those belong in separate cluster kinds; reject the construction if encountered. Do NOT store the graph in the coordinator memory — write edges to `work_dependencies` immediately via `CreateWorkItems` + `RecordWorkDependency` (the `RecordWorkDependency` command is already present in the todo-2 canonical list).
  Parallelization: Wave 4 | Blocked by: 16, 14 | Blocks: 18, 19
  References: autore-schema/src/domain for `core.function`, `core.type`, etc. namespaced kinds (README.md:65-72); Cargo.toml:46 (petgraph = 0.8); autore-schema/src/domain/task/mod.rs:294-301 (`Task::dependencies_satisfied` — the base pattern stays); spec §7.2 (work kinds), §7.3 (dependency construction with SCC)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction work_graph::` runs `work_graph_builder_creates_function_per_entity`, `work_graph_builder_collapses_recursive_cycles_to_cluster` (sample: f_a calls f_b calls f_a; assert one FunctionCluster formed, both Functions become ClusterMember edges), `work_graph_builder_leaves_singleton_sccs_alone`, `work_graph_builder_rejects_mixed_kind_scc`, `work_graph_builder_records_dependencies_via_command` (asserts the inserted `work_dependencies` rows match the RecordingAutoReClient's `CreateWorkItems` + `RecordWorkDependency` call tally).
  QA scenarios: happy — the 2-function cycle test prints `[OK] 1 cluster formed, 2 ClusterMember edges inserted`. failure — skip the kosaraju step → cluster test asserts `fn_clusters.len() == 1` fails (size 0). Evidence: `.omo/evidence/auto-re-stage-1/task-17-work-graph.txt`
  Commit: Y | feat(reconstruction/work_graph): 18 work-item kinds, dependency edges, SCC collapse

- [ ] 18. Implement per-work-item input fingerprints + bounded invalidation
  What to do / Must NOT do: Module `autore-reconstruction::fingerprint`. Implement `WorkItemFingerprint::compute(work_item, deps_context) -> ContentHash` over a deterministic serialization of: static-analysis inputs (artifact content_hashes of relevant ingested snapshots — per spec §7.5), accepted hypotheses (references via `AcceptHypothesisPolicyDriven` from todo 12), relevant generated declarations (last_change_hash from `generated_source_mappings` — belonging to upstream entity kinds), dynamic observations (artifact content_hashes of `debug.observation` artifacts — created in Wave 7), prompt-template version (string, derive from a `prompt_versions.toml`), model configuration hash (OpenAI-compatible provider's sampling params + endpoint-config hash — provider-run provenance per spec §8.8), build configuration (the build-profile hash from project), verification policy (string). Fingerprint is BLAKE3 of the canonicalized byte-serialization. Implement `InvalidateDownstream::propagate(fingerprint_change, graph)` that, given a work item whose input fingerprint changed, finds dependent work items via `work_dependencies`, recomputes their effective_fingerprint, compares to their stored `work_fingerprints.fingerprint`, marks the mismatched ones `Invalidated` via `ApplicationCommand::InvalidateWorkItem`, and recursively ENUMERATES their transitive downstream: invalidation ends at work items whose own fingerprint still matches (a downstream item whose inputs didn't change isn't invalidated even if a predecessor's fingerprint changed — but the spec says only 돌아 affected downstream — recompute, and stop at no-change). Bounded per spec §7.5: invalidation propagates only through `GeneratedDeclRequirement` and `BuildDependency` edges, not pure `DirectCall` edges (calls don't invalidate a caller's source).
  Must NOT do: do NOT include LLM raw response hashes in the fingerprint (the LLM is non-deterministic — only the prompt+config enter the fingerprint). Do NOT invalidate proto-state work items only because a hypothesis changed status from Pending to Accepted (status-change of an upstream hypothesis is captured by recompute, not by prior fingerprint). Do NOT cascade invalidation through edges not tagged in §7.5.
  Parallelization: Wave 4 | Blocked by: 17 | Blocks: 19
  References: Cargo.toml:41 (blake3); autore-schema/src/domain/task/mod.rs:108-110 (input_revision field — existing Stage 0 schema already carries per-task invalidation bit, just bump it Stage-1-wise via `WorkItemFingerprint`); spec §7.5 (fingerprint content list), spec §8.8 (provenance without plaintext secrets — fingerprint never carries secrets)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction fingerprint::` runs `fingerprint_is_deterministic_for_same_inputs`, `fingerprint_changes_when_upstream_declaration_changes` (rebuild fp after mutating last_change_hash → different fp), `fingerprint_does_not_change_on_only_hypothesis_status_transition` (acceptance timestamp changes, fp unchanged), `invalidation_propagates_through_generated_decl_edges_only` (sample: A→B GeneratedDeclRequirement; A→C DirectCall; mutate A; assert B invalidated, C left alone), `invalidation_stops_at_matching_fingerprint`.
  QA scenarios: happy — happy-path test prints `[OK] 1 invalidated downstream, 1 left alone`. failure — propagate invalidation through DirectCall edges → `invalidation_propagates_through_generated_decl_edges_only` fails seeing C invalidated. Evidence: `.omo/evidence/auto-re-stage-1/task-18-fingerprint.txt`
  Commit: Y | feat(reconstruction/fingerprint): work-item input fingerprint + bounded downstream invalidation

- [ ] 19. Adapt the deterministic scheduler to request transitions through autore-app commands
  What to do / Must NOT do: Edit `autore-stage1/src/scheduler/scheduler.rs:77-288` (the existing `Scheduler` with `priority_score`, `run_campaign`, `evaluate`, `recover_expired_leases`, `promote_ready_tasks`, `dispatch_tasks`, `evaluate_state`). Keep the `priority_score` formula (lines 115-129) and the `CampaignEvaluation` enum (lines 60-70). Remove the direct repository calls in `run_campaign` (calls to `repos.queries.find_expired_leases`, `repos.tasks.lease_next`, `repos.queries.update_task_state`, `repos.tasks.fail`) — replace the `RepositorySet` argument with `Arc<dyn AutoReClient>` and issue `ApplicationQuery::ListExpiredLeases` (already in the todo 2 query list), `ApplicationCommand::RenewWorkLease`, `ApplicationCommand::LeaseWorkItem`, `ApplicationCommand::CompleteWorkItem`, `ApplicationCommand::FailWorkItem` (with the lease-id recover semantics per spec §7.6). Expand `PriorityFactors` (scheduler.rs:28-53) — keep existing 5 weights + add 5 per spec §7.4 priority policy: `dependents_unblocked_weight` (unblocked-many bonus), `high_impact_conflict_weight`, `removes_build_blocker_weight`, `verified_coverage_weight`, `evidence_strength_weight`. Each weight defaults to the existing sub-scoring defaults; deterministic per existing invariant. Keep default `max_dispatch_per_tick = 4` (uneendet by Wave 4 — tunable via run config in Wave 11).
  Must NOT do: do NOT introduce an LLM-controlled scheduler (§7.4 explicit). Do NOT remove the deterministic invariant — same inputs must still produce same priority scores (preserve existing tests at scheduler.rs:366-419). Do NOT call storage directly anywhere in the scheduler file post-edit. Do NOT collapse `Lease`/`Ready`/etc states into fewer (the autore-schema Task state machine stays).
  Parallelization: Wave 4 | Blocked by: 18 | Blocks: 20
  References: autore-stage1/src/scheduler/scheduler.rs:1-304 (existing scheduler); autore-app/src/application_service/requests.rs (AutoReClient trait at :610); spec §7.4 (priority policy bullet list), §7.6 (scheduler mutation boundary), §4.2 (adapt — not replace — the scheduler)
  Acceptance criteria (agent-executable): `cargo build -p autore-stage1 --no-default-features` succeeds. `cargo test -p autore-stage1 --no-default-features scheduler::` runs `priority_score_is_stable` (existing retention), `priority_score_unblocked_dependents_bonus` (new), `priority_score_build_blocker_bonus` (new), `scheduler_evaluates_complete_unchanged_after_refactor`, `scheduler_recovers_expired_lease_via_command` (asserts the lease recovery path went through `ApplicationCommand::FailWorkItem` or `RenewWorkLease` via a RecordingAutoReClient double), `scheduler_does_not_touch_storage` (test stubs storage; if any storage call leaks, the test fails with "scheduler called unexpected RepositorySet method"). Existing tests `campaign_tests::*` ported to use ApplicationCommand assertions still green.
  QA scenarios: happy — `scheduler_does_not_touch_storage` records a list of application commands issued; storage-accessed counts are 0 across the tick. failure — leave one `repos.queries.update_task_state(task.id, TaskState::Ready)` in promote_ready_tasks → the test stub's "any storage access fails" assertion trips. Evidence: `.omo/evidence/auto-re-stage-1/task-19-scheduler-via-app.txt`
  Commit: Y | refactor(stage1/scheduler): request transitions via AutoReClient; expanded priority factors

- [ ] 20. Whole-executable work-graph generation end-to-end (large-IDB-scale smoke)
  What to do / Must NOT do: Integration test `autore-reconstruction/tests/whole_program_work_graph.rs`. Steps: (1) ingest a moderately complex binary (use the same binary as Wave 3's end-to-end or a fresh `samples/factorial.exe` fixture); (2) run `WorkGraphBuilder` from todo 17; (3) assert at least one `ProgramSkeleton`, one `Entrypoint`, N `Function` work items, M `ExternalDependency` work items (where N/M derived from fixture constants); (4) assert dependency graph include some `DirectCall` edges; (5) induce a synthetic cycle (manually append f_a→f_b→f_a edge records via `RecordWorkDependency` commands) — assert the scheduler in this build has renamed those work items as members of one `FunctionCluster` and added a new `FunctionCluster` work item (asserts SCC collapse); (6) compute fingerprints (todo 18), simulate a Stage-1 gen event that marks a function as implemented (set last_change_hash on its generated_source_mapping row), assert downstream work items have their fingerprints changed appropriately (Generation/work-items that point through `GeneratedDeclRequirement` edges on the changed one invalidated); (7) run one scheduler tick; assert `CreateProgramSkeleton`/`ExternalApiDeclaration` priority is high and dispatched first (deterministic ordering — assert by appending the dispatched kind sequence to evidence). Use RecordingAutoReClient throughout; assert every mutation is an ApplicationCommand. Exit-criterion: §7.8 — the scheduler can traverse the executable without manual function selection.
  Must NOT do: do NOT call this test for whole-Van-Buren (that's Wave 11). Do NOT skip the cycle-injection step — actually create the cycle. Do NOT skip the invalidation assertion.
  Parallelization: Wave 4 | Blocked by: 19 | Blocks: 41 (Wave 7 dynamic obs create new evidence that requires invalidation cycle)
  References: Wave 3's `tests/ida_full_ingest.rs` (todo 15) for the bootstrap pattern; autore-reconstruction modules built in todos 14, 17, 18, 19; spec §7.8 (exit criterion), §7.4 (priority ordering sample)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test whole_program_work_graph -- --nocapture --ignored` ends with `[OK] traversed executable without manual selection; priority ordering matched spec §7.4 sample`. Exit code 0. `cargo clippy -p autore-reconstruction --all-targets -- -D warnings` clean.
  QA scenarios: happy — the 7 Assertion markers print; specimen priority check confirms `ProgramSkeleton` first. failure — comment-out the SCC collapse in todo 17 — step 5 sees the cycle uncollapsed → assertion fails with `expected 1 FunctionCluster, found 0`. Evidence: `.omo/evidence/auto-re-stage-1/task-20-whole-program-work-graph.txt`
  Commit: Y | test(reconstruction/work_graph): end-to-end ingestion → work graph → cycle → invalidation

### Wave 5 — OpenAI-compatible LLM analysis (Spec §18 Phase 5; §8)

- [ ] 21. Implement external OpenAI-compatible provider binary `providers/openai-compatible/`
  What to do / Must NOT do: New workspace member off default-members. Binary `bin/llm-provider` reads the same bootstrap env (same runtime from Wave 2). On `Negotiate` declares 7 capabilities per spec §8.2: `llm.analysis.function`, `llm.analysis.type`, `llm.analysis.class`, `llm.analysis.subsystem`, `llm.analysis.conflict`, `llm.analysis.failure`, `llm.experiment.design`. Provider config (`/etc/autore/providers/openai-compatible/configuration.toml` or env vars) carries: `endpoint_url` (default `http://127.0.0.1:8080/v1` to hit a local `llama-server` or Ollama/vLLM/LM Studio — same protocol), `api_key_ref` (NOT the secret itself — reference to a path or env var from which the runtime fetches per spec §8.8 "never persist plaintext secrets"). On Execute(capability_id=llm.analysis.X, request): the request contains the bounded investigation bundle (todo 22) as bytes (JSON). The provider renders the capability-specific prompt template (template file at `providers/openai-compatible/prompts/<capability>.handlebars` — committed in this todo with placeholder preamble for each capability; content refined later), submits to the configured OpenAI-compatible REST endpoint with `response_format: json_schema` (OpenAI structured-output API shape) where the schema is the capability's result schema (todo 23). The provider parses the model HTTP response: read safe-route (parse → validate vs schema locally via jsonschema = 0.33 — provider may pre-validate before submitting to the canonical commit), retry on malformed output once with a schema-repair prompt per spec §8.7 (provider-level retry, bounded to 1). On success emits `ObservationProduced{kind=llm.raw-response, content=<raw HTTP body bytes as staged artifact via stage_inbound>}` and `ObservationProduced{kind=llm.parsed-result, content=<JSON validated result as staged artifact>}`. On persistent invalid output emits `Diagnostic{severity=Error, code=invalid-llm-output}` + `Completed{status=Failed}`. Track token + request metrics per spec §8.8 in provenance metadata on the Completion event.
  Must NOT do: do NOT persist plaintext API keys — only key-reference, fetched live per run. Do NOT let the LLM auto-call arbitrary tools — no `tool_use` is enabled in the wrapped OpenAI call; raw text in/out only. Do NOT bypass the provider's local schema validation (provider must validate before committing `llm.parsed-result`). Do NOT collapse all capabilities into one `analyze-this` prompt (§8.2 explicitly forbids). Do NOT include raw HTTP request body in any persisted record (only response goes to llm.raw-response artifact).
  Parallelization: Wave 5 | Blocked by: 10 | Blocks: 22
  References: spec §8.1 (provider/core ownership division), §8.2 (capability list), §8.7 (invalid-output policy), §8.8 (provenance — never persist secrets); tonic 0.12 server plumbing ( reused from todo 10's fixture provider); Context7 for OpenAI-compatible client Rust crate (resolve `libraryId: '/async-graphql/async-graphql'` no — try `/tokio-rs/axum` for the http client; or use `ureq` / `reqwest` per existing workspace deps — context7 resolve-library-id on "reqwest" before designing the http client call shape)
  Acceptance criteria (agent-executable): `cargo build -p openai-compatible-provider --no-default-features` succeeds. `cargo test -p openai-compatible-provider` runs: `negotiate_advertises_seven_capabilities`, `provider_renders_capability_specific_prompt` (asserts rendered prompt differs per capability), `provider_submits_with_response_format_json_schema`, `provider_retries_once_on_malformed_output` (mock LLM endpoint returning `"{"` ensures parser fails and provider issues 1 retry then `Completed{Failed}`), `provider_never_persists_plaintext_key` (asserts the staged artifact bytes do not contain the key string). `cargo clippy -p openai-compatible-provider --all-targets -- -D warnings` clean.
  QA scenarios: happy — first attempt malformed; retry test parses once then gives up; emits Completed{Failed} and the artifact bytes do NOT contain `sk-...`. failure — drop the retry count check → test asserts retry count must be exactly 1 and fails on count=2. Evidence: `.omo/evidence/auto-re-stage-1/task-21-openai-compatible-provider.txt`
  Commit: Y | feat(providers/openai-compatible): external LLM provider with 7 capabilities + bounded retry

- [ ] 22. Implement bounded investigation-bundle builder + capability schemas
  What to do / Must NOT do: New module `autore-reconstruction::analysis` (or extend existing one). Implement `InvestigationBundle` per spec §8.3 fields: subject_identity (WorkItemId), static_structural_snapshot (artifact_handle to ida-function-snapshot or ida-type-snapshot), decompilation_artifact (artifact_handle), disassembly_artifact (artifact_handle), cfg_summary (artifact_handle), callers_and_callees (list of WorkItemIds with brief summaries), relevant_types (list of EntityIds for types referenced in the subject), relevant_globals, strings_and_constants (sample snippets), dynamic_observations (artifact_handle list — set to empty in Wave 5; populated by Wave 7), accepted_hypotheses (list of hypothesis ids), unresolved_conflicts (list of conflict ids), prior_generated_candidate (artifact_handle if regeneration now; None on first attempt), compiler_diagnostics (list of build-diagnostic records — empty in Wave 5, populated by Wave 6/9), verification_failures (list of verification_comparison ids — empty in Wave 5), requested_output_schema (the JSON Schema for the target capability's parsed result). Every artifact covered by an artifact_handle uses `ArtifactId` (no raw bytes shipped). Every WorkItemId, EntityId is stable (an entity registered via todo 14). The bundle is JSON serialization of these refs (the LLM never gets artifact bytes directly — it gets handles and the static textual/structural summaries that have been pre-rendered into the bundle by reading the canonical snapshots). Implement `request_payload_for(bundle, capability)` that fetches each referenced artifact from canonical storage (autore-store reads bytes), extracts textual summaries, and assembles the final JSON payload sent in the Execute request. Implement JSON Schemas per capability (committed under `autore-reconstruction/schemas/analysis/*.schema.json`): `function-analysis.schema.json` (proposed_name, proposed_signature, behavior_claims[], side_effect_claims[], preconditions[], postconditions[], type_proposals[], relationship_proposals[], external_dependencies[], uncertainties[], evidence_references[], proposed_debugger_experiments[], recommended_follow_up_work[]); `type-analysis.schema.json` (size, alignment, fields[], base_classes, methods[], union_kind, confidence_per_field[]); `class-analysis.schema.json` (class_name, primary_vtable_id, size, alignment, bases[], virtual_slots[], ctors[], dtors[], overrides[]); `subsystem-analysis.schema.json` (functions[], shared_state, init_order, external_dependencies[], lifecycle_phases[]); `conflict-analysis.schema.json` (contradictions[], explanations[], recommended_resolution[], confidences_per_resolution[]); `failure-analysis.schema.json` (error_kind, passing_to_failing_diff, candidate_cause[], recommended_repair[], recommended_follow_up_work[]); `experiment-design.schema.json` (proposed_scenario, captured_observations[], normalizations[], significance_confidence).
  Must NOT do: do NOT copy raw artifact bytes into the bundle (only refs + cook summaries). Do NOT include all project entities in the bundle (only relevant per spec §8.3 — "bounded context"). Do NOT let the bundle reference an artifact by anything other than its canonical ArtifactId. Do NOT make schemas ad-hoc — use JSON Schema 2020-12 (compatible with jsonschema = 0.33 already in deps).
  Parallelization: Wave 5 | Blocked by: 21 | Blocks: 23
  References: jsonschema = 0.33 (workspace dep); serde_json; spec §8.3 (bundle fields), §8.4 (function-analysis result fields), §8.5 (3-level import boundary), §8.6 (validation list)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction analysis::` runs `bundle_serializes_via_serde_json` (round-trip crate), `bundle_excludes_artifact_bytes` (each artifact field is `ArtifactId`, not bytes — deserialize and assert fielded types), `bundle_only_relevant_calls_in_callers_list` (subject f_a has callers f_b and f_c recorded, but irrelevant callees of f_c excluded), `function_analysis_schema_rejects_missing_required_field` (jsonschema-0.33-based discriminator).
  QA scenarios: happy — the bundle test produces a JSON document ≤ 64KiB (bounded). failure — embed raw decompilation bytes into bundle struct → `bundle_excludes_artifact_bytes` asserts ArtifactId field has type `Option<ArtifactId>`, fails seeing `Vec<u8>`. Evidence: `.omo/evidence/auto-re-stage-1/task-22-bundle.txt`
  Commit: Y | feat(reconstruction/analysis): bounded InvestigationBundle + JSON-Schema per capability

- [ ] 23. Implement 3-level import boundary (raw → schema-validated → canonical) + bounded schema-repair
  What to do / Must NOT do: Module `autore-reconstruction::analysis::import`. When the LLM provider emits `ObservationProduced{kind=llm.raw-response}` and `ObservationProduced{kind=llm.parsed-result}` (todo 21), the core-side importer does THREE things: (1) it ALWAYS persists the raw response (an `ApplicationCommand::ImportArtifact`) using `core.native-provider-output` kind, then the parsed result also persisted as a typed artifact. Then validates the parsed result against the same JSON Schema the provider used (independent re-validation per spec §8.5: "the core independently validates provider results"; Wave 5 §19 #17 enforced at F4). Validation invariants per spec §8.6: referenced entities exist (query ApplicationQuery::GetEntity per id in `evidence_references` — and reject if a referenced entity was not in the original bundle's relevant list — guards LLM hallucinating entities it didn't see), referenced evidence was in the request bundle, type sizes and offsets within `1..4096` for structs, confidence values `0.0..=1.0`, relationships well-formed (src+dst EntityIds exist, edge-kind known), experiment proposals use `debug.*` capabilities only — never arbitrary text, result size ≤ 1 MiB parsed JSON, capability name matches request subject's capability, schema version field equals expected. On first validation failure, construct a `SCHEMA_REPAIR_PROMPT` (committed under `prompts/schema-repair.handlebars`) inserting the failed validation errors, re-dispatch to the provider's `failure-analysis` capability (or the same capability with a `repair-context` extension — design choice: use `failure-analysis` to preserve capability boundaries per §8.2) BUT only if `attempt_count == 0` (bounded-1 spec §8.7). On second failure, emit `ApplicationCommand::FailWorkItem{kind=InvalidLlmOutput}` + a `BlockedReason{kind=InvalidOutput}`. On success, issue `ApplicationCommand::AddHypothesis` per behavior claim, side-effect claim, type proposal, etc. (each becomes one Hypothesis with `predicate` like `proposes-behavior`, `proposes-type-layout`, ... and `candidate` a JSON representation; `confidence` from model output). Issue `ApplicationCommand::CreateWorkItems` for `recommended_follow_up_work` (these become `Investigation` / `ConflictResolution` work items). Update `parsed_llm_results` table linking raw→parsed→work_item_executed via AddEvidence (existing command). Preserve provenance: the AddHypothesis `derived_from` field lists the raw + parsed artifact IDs, the provider-run-id, the prompt-template version, the model id, endpoint-config hash, sampling params, duration, token usage (per spec §8.8 — never secrets).
  Must NOT do: do NOT let the LLM model output directly create DatabaseRecord instances — only the ApplicationService does that. Do NOT partially import semantic output per §8.7 — all-or-fail. Do NOT persist secrets in any provenance field. Do NOT allow the same raw response to be re-parsed if schema changes between parses (tag the parsed result with the schema version it was validated against).
  Parallelization: Wave 5 | Blocked by: 22, 12 | Blocks: 24
  References: autore-app/src/application_service/requests.rs:300-301 (AddHypothesis already exists), :299 (AddEvidence), spec §8.5 (3-level boundary), §8.6 (validation rules list), §8.7 (bounded repair), §8.8 (provenance); jsonschema 0.33
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction analysis::import::` runs `raw_response_always_persisted_irrespective_of_validation`, `parsed_result_validated_against_schema`, `referenced_entities_must_exist_in_original_bundle`, `confidence_outside_0_to_1_rejected`, `arbitrary_text_in_experiment_proposal_rejected`, `no_partial_import_on_second_failure` (mock provider returns invalid result twice; assert zero Hypothesis rows, `FailWorkItem` issued once, `BlockedReason` recorded), `bounded_retry_attempt_count_one`, `provenance_records_no_plaintext_secrets` (grep on emitted evidence for any of a known secret-string; assert zero).
  QA scenarios: happy — partial-import test reads `INSERT INTO evidence` count: 0 after attempt-2 failure. failure — allow a partial import via `continue` after one bad hypothesis — test asserts the count is >0 and fails. Evidence: `.omo/evidence/auto-re-stage-1/task-23-import-boundary.txt`
  Commit: Y | feat(reconstruction/analysis): 3-level import boundary + bounded schema-repair + provenance

- [ ] 24. Capability-specific parser tests for all 7 LLM capabilities
  What to do / Must NOT do: Per capability (function/type/class/subsystem/conflict/failure/experiment-design), commit a fixture raw response (`autore-reconstruction/tests/fixtures/llm/<capability>.raw-response.json`) and corresponding parsed (`<capability>.parsed.json`). Each fixture goes through a per-capability test that calls the import pipeline of todo 23 against a stub LLM provider returning the fixture. Asserts that the imported Hypotheses match a hand-checked list provided in the test (the test encodes its expected assertions). For each capability, commit also a SECOND malformed fixture (`<capability>.malformed.json`) — asserts the bounded-repair-and-final-failure path is taken exactly.
  Must NOT do: do NOT write fixture raw responses that violate the capability's schema in unexpected ways (each malformed fixture violates on PURPOSE exactly one rule — the rule's name appears in the test name).
  Parallelization: Wave 5 | Blocked by: 23 | Blocks: 25
  References: Wave-5 schemas (`autore-reconstruction/schemas/analysis/*.schema.json`); spec §8.6 (rule list — match the violation-per-test to the rule)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test llm_capability_imports` runs all 7 happy + 7 malformed cases, named per capability per rule (`function_ref_entities_exist`, `type_offsets_within_range`, `class_vtable_id_well_formed`, ...). Exit 0. `cargo test -p autore-reconstruction --test llm_capability_imports -- --nocapture` prints `[OK] capability function-analysis: 1 happy, 1 malformed-import-rejection; capability type-analysis: ..."`.
  QA scenarios: happy — all 14 sub-test pass. failure — drop paragraph a schema rule → fixture previously malformed now appears "happy"; test asserts the malformed path fails to take. Evidence: `.omo/evidence/auto-re-stage-1/task-24-llm-capability-fixtures.txt`
  Commit: Y | test(reconstruction/analysis): per-capability parser happy + malformed fixtures

- [ ] 25. End-to-end LLM analysis slice — best-effort local LLM run (mocked fallback if no LLM available)
  What to do / Must NOT do: Integration test `tests/llm_analysis_e2e.rs`. Build a `ProviderInstallation` for `openai-compatible`, bootstrap via `CoordinatorBootstrap::spawn`, drain the IDA ingestion slice from Wave 3 (to populate entities), build an InvestigationBundle for one Function work item, dispatch `llm.analysis.function`, capture all events, run the import pipeline (todo 23). Assert: the raw-llm-response artifact is committed (query via `auto-re artifact list` filtered), the parsed-llm-result artifact is committed, at least one `AddHypothesis` command was issued, at least one `recommended_follow_up_work` resulted in a `CreateWorkItems`/`Investigation` work item. Use a mock LLM at `127.0.0.1:<port>` running in tokio that returns a tested canned response with `proposed_name: sub_<address_x>`, `behavior_claims: []` etc — placeholder minimal valid response; IF a real OpenAI-compatible local server URL is provided via `AUTORE_TEST_REAL_LLM_ENDPOINT`, forward the request there instead of the mock. This keeps the test runnable on dev machines without a real LLM.
  Must NOT do: do NOT require a real LLM in the test path (the test must pass on a clean dev machine with no LLM endpoint). Do NOT let the test mutate the AUTO-RE repository source tree — only the project.auto-re SQLite store + artifacts. Do NOT burn tokens during integration tests (mock by default; opt-in real LLM via env var).
  Parallelization: Wave 5 | Blocked by: 24 | Blocks: 31 (Wave 9 generation uses llm provider)
  References: existing mock-test pattern at autore-stage1/src/analysis/mock.rs:115-150 (deterministic canned strings); tonic in-process test fixtures; spec §8.10 (exit criterion: invalid output cannot mutate canonical state)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test llm_analysis_e2e -- --nocapture --ignored` prints `raw=persisted; parsed=persisted; hypotheses=N; follow-up-work-items=M` with N≥1 and M≥1. Mock LLM endpoint port is randomized, command-line grep the test's stdout for `mock-listening-on=127.0.0.1:`.
  QA scenarios: happy — the canned response triggers import; canonical state mutated through ApplicationCommand only (RecordingAutoReClient assertion). failure — bypass the import boundary by having the test stub write directly to evidence table → assertion `all mutations were ApplicationCommand` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-25-llm-analysis-e2e.txt`
  Commit: Y | test(reconstruction/analysis): end-to-end LLM function-analysis slice with mock fallback

### Wave 6 — Program skeleton + initial build (Spec §18 Phase 6; §11, §12)

- [ ] 26. Additive migrations for generated-source mappings relationships + llm.generation capability pointers + build attempts/diagnostics (complement Wave 4)
  What to do / Must NOT do: V28 `V28__generated_declaration_artifacts.sql` (id BLOB PK, source_mapping_id BLOB FK → generated_source_mappings, kind TEXT, declaration_artifact_id BLOB FK → artifacts, last_change_hash BLOB). V29 `V29__generated_definition_artifacts.sql` (id BLOB PK, source_mapping_id BLOB FK, kind TEXT, definition_artifact_id BLOB FK). V30 `V30__build_attempts.sql` (id BLOB PK, work_item_id BLOB NULL, configuration_blob BLOB json, status TEXT, exit_code INT NULL, stdout_artifact_id BLOB NULL, stderr_artifact_id BLOB NULL, log_artifact_id BLOB NULL, started_at, ended_at NULL). V31 `V31__build_diagnostics.sql` (id BLOB PK, build_attempt_id BLOB FK, diagnostic_code TEXT, severity TEXT, file_path TEXT, line INT NULL, column INT NULL, related_entity_id BLOB NULL, message TEXT, candidate_cause TEXT NULL, suggested_work_kind TEXT NULL). V32 `V32__verification_scenarios.sql` (id BLOB PK, name TEXT, level TEXT, scenario_blob BLOB json, original_execution_blob BLOB NULL, captured_observations_blob BLOB NULL). V33 `V33__verification_comparisons.sql` (id BLOB PK, scenario_id BLOB FK, candidate_run_id BLOB NULL, comparison_kind TEXT, equal_count INT, equivalent_count INT, different_count INT, inconclusive_count INT, not_observed_count INT, failed_count INT, evidence_artifact_id BLOB NULL, generated_at). Idempotent + forward-only. File-name format `V<n>__<name>.sql` exactly per V1..V13 reference.
  Must NOT do: do NOT modify migrations added by Wave 3 / Wave 4 (V14..V27). Do NOT use DB-generated ids.
  Parallelization: Wave 6 | Blocked by: 5 | Blocks: 27, 28, 29
  References: migrations directory naming; autore-store embed_migrations!; spec §11.3 (mapping model), §13.2 (scenario model), §12.2 (build diagnostics records)
  Acceptance criteria (agent-executable): `cargo test -p autore-store migrations_v14_to_v33_idempotent` and `migrations_v14_to_v33_rollback_safe` pass. All existing autore-store tests remain green.
  QA scenarios: happy — second `project migrate` reports no-op. failure — make V32 perform `ALTER TABLE` → rollback-safe test fails as SQLite ALTER behavior mismatches. Evidence: `.omo/evidence/auto-re-stage-1/task-26-migrations-v28-v33.txt`
  Commit: Y | feat(store): additive V28..V33 migrations for generation, build, verification records

- [ ] 27. Implement generator skeleton builder + stub policy + entity-to-source mapping
  What to do / Must NOT do: New module `autore-reconstruction::generation`. Implement `ProjectSkeletonBuilder` that on completion of Wave-3 work (semantically: when running against a project that has at least the IDA-imported entities) creates a managed `generated/openvb/` tree at `<output_path>` with the layout per spec §11.1 (`CMakeLists.txt`, `reconstruction.toml`, `include/{recovered,platform,external}`, `src/{runtime,generated,subsystems,entrypoints}`, `tests/{unit,differential,scenarios}`, `reports`). The `CMakeLists.txt` is GENERATED by cmkr (since the operator's workflow uses cmkr + cmake — todo drafting answer) — meaning the skeleton writes a `CMakeLists.txt` plus a `CMakePresets.json` configured for Docker + MSVC 2002 cross-build (operator's existing setup, todo clarification). For each canonical entity (per spec §11.2 in-order: module + namespace layout; external API declarations; placeholder type declarations; declarations for every discovered function; explicit temporary stubs; build configuration; earliest possible complete build), issue `RegisterGeneratedSourceMapping` and `RegisterArtifact` (existing Stage 0 command) for each generated stub file (kind `core.generated-candidate`). Each stub file is named after its canonical entity: `include/recovered/<entity_namespace_path>.hpp` / `src/generated/<entity_namespace_path>.cpp`. Each stub implements §11.6 explicit stub policy: header comment naming the entity id, reason stubbed, and a `[[reconstruction_status = "stubbed"]]` attribute marker + a `static_assert(false, "reconstruction-stub: <entity id>")` if the symbol would otherwise compile (or instead an empty body with a TODO comment if compile-contamination would break the skeleton build). Produce a deterministic order: program skeleton first; external API declarations second; constants/enums; globals with clear layouts; leaf functions; etc. (per spec §7.4 sample, applicable for skeleton generation ordering).
  Must NOT do: do NOT let the LLM anywhere NEAR the skeleton's C++ source — the skeleton is deterministic from canonical identities (only the Phase-7-onward generation is LLM-driven). Do NOT skip the skeleton because "the LLM will generate it later" (§11.2 strict — earliest complete build BEFORE any LLM generation). Do NOT give two functions the same source path (id-stable paths). Do NOT use entity-content-derived names (only canonical ids) — keep logical identity stable independent of current content hash (spec §11.3).
  Parallelization: Wave 6 | Blocked by: 26, 13 | Blocks: 28, 29, 36 (Wave 9 patches on top)
  References: spec §11.1 (project layout), §11.2 (initial skeleton algorithm), §11.3 (mapping model), §11.6 (stub policy); CMake/cmkr doc reference via Context7 (resolve `libraryId: '/cmkr/cmkr'`); cargo-existing Stage 0 artifact model
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction generation::skeleton` runs `skeleton_produces_layout_from_spec`, `skeleton_generates_stub_per_discovered_function` (asserts count of stubs equals baseline fixture function count), `skeleton_stubs_marked_explicit_status` (asserts each stub file contains `reconstruction_status = "stubbed"` substring), `skeleton_source_paths_derived_from_canonical_entity_id_not_content` (rename entity+rebuild skeleton → path unchanged), `skeleton_no_llm_involved` (asserts zero ApplicationCommand with `AddEvidence` of `llm.raw-response` type emitted during skeleton generation).
  QA scenarios: happy — the skeleton test generates for a 3-function fixture binary and produces 4 files (header + cpp + CMakeLists + reconstruction.toml). failure — name a stub by its `display_name` instead of `entity_id` → stability-on-rename test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-27-generator-skeleton.txt`
  Commit: Y | feat(reconstruction/generation): deterministic project skeleton with explicit stub policy

- [ ] 28. Implement build-provider trait + first impl: cmkr + cmake + Docker-hosted MSVC 2002 wrapper
  What to do / Must NOT do: New module `autore-reconstruction::build`. Define `BuildProviderTrait` (async) with methods: `configure_project(generator_manifest: &GeneratorManifest, project_root: &Path) -> Result<BuildConfigured>`; `compile_units(units: &[CompileUnit]) -> Result<CompileResult>`; `link_target(target_artifacts: &[PathBuf]) -> Result<LinkResult>`; `run_test(test_target: &str) -> Result<RunTestResult>`; `collect_diagnostics(build_logs: &BuildLogs) -> Result<Vec<BuildDiagnostic>>`. FIRST impl `DockerMsvc2002BuildProvider` (operator's existing workflow per user clarification): wraps `cmkr gen` → `cmake -S . -B build -G Ninja` (operating inside a container), `cmake --build build` compiling each cpp through the container's MSVC 2002 + Ninja (or NMake, depending on operator's existing container config), linking into `bin/openvb.exe`, parsing stdout/stderr to typed `BuildDiagnostic` records per spec §12.2 record (diagnostic_code, severity, file, line, column, related_entity_id — matched by parsing the file path against the generated_source_mappings table, message, candidate_cause, suggested_work_kind). The provider is an EXTERNAL provider binary `providers/build/` using the Wave-2 runtime; capabilities: `build.configure`, `build.compile`, `build.link`, `build.run-test`, `build.collect-diagnostics`, `build.abort`. The provider shells out to docker (via `tokio::process::Command`) — docker host is configured via the provider's configuration.bundle. Build logs (stdout, stderr, build_log) are committed to staging via ArtifactTransport (todo 9); provider emits `ArtifactProduced` for each log; emitting final `Completed{status=Passed|Failed}` + parsed diagnostics as a JSON observation.
  Must NOT do: do NOT bake the operator's Docker image name or CMake generator hard into the trait — `BuildConfigured` MUST contain them as configured via the provider's configuration_bundle; the trait stays generic (spec §23 #5 invariant). Do NOT default to system clang/g++. Do NOT skip diagnostic parsing — typed diagnostics flow onward to Wave 9. Do NOT generate docker commands blindly — every command-line invocation must pass validation against an allowlist (e.g. binary path within the supplied `/generated/openvb/` root, container image-name set in configuration).
  Parallelization: Wave 6 | Blocked by: 27, 9 | Blocks: 29, 30, 36 (Wave 9's repair loop uses diagnostics)
  References: user clarification: "cmkr + cmake + a Docker container hosting the MSVC 2002 compiler"; spec §12.1 (abstractions list), §12.2 (records), §12.3 (classification taxonomy); Context7 for cmake command-line / cmkr manifest reference (resolve-library-id `cmkr`)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction build::docker_msvc2002` runs `build_provider_invokes_cmkr_gen_then_cmake_build_inside_docker` (uses a mock docker binary path via env var to assert the subprocess call sequence), `build_provider_parses_compiler_diagnostics_to_typed_records` (MSVC error `error C2065: 'foo': undeclared identifier` samples feed → `BuildDiagnostic{code=C2065, severity=Error, file=.., line=.., message=undeclared identifier 'foo', suggested_work_kind=MissingDeclaration}`), `build_provider_propagates_fail_status_to_completed_with_failed`, `build_provider_never_uses_unconfigured_image_names`, `trait_traits_admit_other_adapters` (smoke test that a second impl compiling-only trait with a stub `CmakeNinjaBuildProvider` returns `Result<()>` for each method — keeps the trait surface pluggable).
  QA scenarios: happy — smoke test sequence `cmkr gen` then `cmake --build` happens in mock docker. failure — bake image name into trait → `build_provider_never_uses_unconfigured_image_names` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-28-build-provider.txt`
  Commit: Y | feat(providers/build): cmkr+cmake+Docker-MSVC2002 build provider with typed diagnostics

- [ ] 29. Run skeleton through build-provider → produce earliest complete-build state
  What to do / Must NOT do: Integration test `tests/skeleton_first_build.rs`. Run `ProjectSkeletonBuilder` (todo 27) on the small-fixture binary, then `DockerMsvc2002BuildProvider::configure_project`, `compile_units` on all generated cpps, `link_target` of the executable. Record build outcome as `RegisterArtifact` for the produced exe (or `RecordBuildAttempt{status=Failed}` if it fails — which it should NOT with all-stubs since stubs are minimum-viable, but the test asserts the failure path is graceful if invoked). On success → `RecordBuildAttempt{status=Passed}` + `RegisterArtifact{kind=executable, ...}` for the generated binary, plus a `RegisterGeneratedSourceMapping` updating `build_attempt_id` per generated source row (Wave 4 V25). On failure → `RecordBuildAttempt{status=Failed}` + structured diagnostics for each `parse-diagnostics` outcome. Round-trip repeat the test on a sanity-modified skeleton (drop one stub from the tree) — assert `Failed` outcome + the parse-diagnostics `RelatedEntityId == the missing entity_id` + the BuildDiagnostic.suggested_work_kind set to `Generate` (or appropriate). The `ConfigureProject` abstraction must NOT use any system-level cmake — only the configured Docker container.
  Must NOT do: do NOT use a system gcc/clang as a path-of-least-resistance. Do NOT short-circuit `RecordBuildAttempt` if the build fails — pass it through. Do NOT mutate canonical state outside ApplicationCommand.
  Parallelization: Wave 6 | Blocked by: 28 | Blocks: 30
  References: Wave 3 end-to-end loop; spec §11.2 "earliest possible complete build"; §12.4 (build loop)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test skeleton_first_build -- --nocapture --ignored` ends with one of `BUILD PASSED, exe committed` or `BUILD FAILED with N typed diagnostics -> BuildFailures work items created` (forced by the missing-stub mutation branch). On the happy branch asserts `RegisterArtifact`+`RecordBuildAttempt` for exe and build rows, AND a `RegisterGeneratedSourceMapping` updating each generated source's `build_attempt_id` to point at the happy attempt.
  QA scenarios: happy — happy branch prints `BUILD PASSED`. failure — drop the missing-stub mutation step → mutated branch still reports `PASSED`; test asserts it should report `FAILED`. Evidence: `.omo/evidence/auto-re-stage-1/task-29-skeleton-first-build.txt`
  Commit: Y | test(reconstruction/build): skeleton first complete build round-trip

- [ ] 30. Build-feedback classification taxonomy + initial-vs-repair feedback classifier (bounded by spec §12.3)
  What to do / Must NOT do: Module `autore-reconstruction::build::classification`. Define enum `BuildFailureKind` per spec §12.3: `Syntax`, `MissingDeclaration`, `UnknownType`, `IncompleteType`, `LayoutMismatch`, `CallingConventionMismatch`, `Linkage`, `MissingSymbol`, `DuplicateSymbol`, `Abi`, `UnsupportedPlatformApi`, `GeneratedCodeDefect`, `BuildEnvironmentDefect`. Implement `classify(diagnostic: &BuildDiagnostic) -> BuildFailureKind` with rules: MSVC code C2065 → MissingDeclaration; C2061 → UnknownType; C2079 (incomplete-type use) → IncompleteType; C2440 (cannot-convert) → Abi or LayoutMismatch (decide based on `candidate_cause` field); C2027 (incomplete-type) → IncompleteType; LNK2019 → MissingSymbol; LNK2005 → DuplicateSymbol; C3861 → MissingDeclaration; C2385 (ambiguous function call) → CallingConventionMismatch; C2065 with stdlib context → UnsupportedPlatformApi; C1010 fatal → GeneratedCodeDefect; environment errors (cmake not found, docker daemon down) → BuildEnvironmentDefect. Implement `select_repair_strategy(failure_kind, diagnostic) -> RepairStrategy`: for `MissingDeclaration` create `BuildFailure` work item kind `Generation` for the required declaration; `UnknownType` → `Generation` for the type; `Linkage` → `ConflictResolution`; `BuildEnvironmentDefect` → `BlockWorkItem` (operator-fixable); `GeneratedCodeDefect` → goes to LLM via `llm.analysis.failure` capability (todo 21); `LayoutMismatch` → request layout investigation (Wave 8); other kinds → routed to `BuildFailure` work items with their suggested_follow_up_work set.
  Must NOT do: do NOT bundle diagnostics by file (one diagnostic → one work item or one append to existing work item via dedup-by-diagnostic-code-and-line). Do NOT auto-repair without the bounded retry policy (Wave 9 enforces bounds). Do NOT let the LLM analyze every diagnostic — only `GeneratedCodeDefect` and diagnostics that pass a "deterministic repair not applicable" check pipeline go to the LLM (spec §12.3 explicit: "Use deterministic repairs where possible. Only send bounded relevant diagnostics to the LLM.")
  Parallelization: Wave 6 | Blocked by: 28, 26 | Blocks: 41 (Wave 9 repair loop)
  References: MSVC error-code reference (via Context7 or websearch); spec §12.3 (full taxonomy)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction build::classification::` runs at least 13 tests — one per `BuildFailureKind` — each with a fixed diagnostic fixture and expected kind. Plus `classify_routes_generated_code_defect_to_llm_analysis_failure`, `classify_routes_environment_defect_to_block_work_item` (not LLM-bound), `classify_routes_layout_mismatch_to_investigation_kind`.
  QA scenarios: happy — C2065 → MissingDeclaration; assert `select_repair_strategy` returns `CreateWorkItems{kind=Generation, predicate=missing_declaration}`. failure — misclassify C2065 as `GeneratedCodeDefect` → C2065 test asserts `BuildFailureKind::MissingDeclaration` and fails. Evidence: `.omo/evidence/auto-re-stage-1/task-30-build-classification.txt`
  Commit: Y | feat(reconstruction/build): classified diagnostic taxonomy + repair-strategy routing

### Wave 7 — IDA/GDB dynamic investigation (Spec §18 Phase 7; §9)

- [ ] 31. Implement typed debugger scenario language + scenario verifier
  What to do / Must NOT do: New module `autore-reconstruction::dynamic`. Define a `DebuggerScenario` AST per spec §9.2 subset: `LaunchTarget{exe_artifact: ArtifactId, env: HashMap<..>, working_dir: PathBuf}`, `AttachTarget{pid}`, `SetBreakpoint{entity: EntityId}`, `RemoveBreakpoint{}`, `Continue{}`, `Step{}`, `Finish{}`, `CaptureRegisters{}`, `CaptureArguments{entity: EntityId}`, `CaptureReturnValue{entity: EntityId}`, `CaptureMemoryRegion{addr: u128, size: usize}`, `CaptureMemoryDelta{addr, size}`, `CaptureGlobalValue{entity: EntityId}`, `CaptureCallTarget{}`, `CaptureExternalCall{api: NamespacedId}`, `CaptureException{}`, `StopAfterInvocationCount{count: u32}`, `StopAfterTimeout{ms: u64}`, `TerminateTarget{}`. Define a top-level `Scenario{setup: Vec<SetupOp>, body: Vec<Step>, stop_conditions: Vec<StopOp>}`. Implement `ScenarioVerifier::validate(&scenario, entities_by_id, allowed_external_apis)` (per spec §9.2 "Do not allow the model to emit arbitrary debugger scripts") — asserts: every EntityId used in steps exists; every Address referenced in CaptureMemoryRegion falls within a known mapped segment (cross-reference IDA's segment snapshot from Wave 3); every api referenced in CaptureExternalCall is in the operator-supplied allowlist; CaptureMemoryDelta's size ≤ 64 KiB. Invalid scenario → `ScenarioValidationError` (NOT executed).
  Must NOT do: do NOT allow free text / arbitrary script injection in scenarios — only the typed AST kinds above. Do NOT permit addresses outside mapped segments. Do NOT allow API-hook against arbitrarily-named APIs (must be from the allowlist). Do NOT support multi-process or remote debugging yet (§9.8 deferred).
  Parallelization: Wave 7 | Blocked by: 10, 27 | Blocks: 32, 33, 34
  References: spec §9.2 (scenario language + safe subset), §9.4 (flow: LLM proposes → core validates → IDA provider executes), §3.4 (typed machine communication); IDA-segment snapshot from Wave 3 todo 13
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction dynamic::verifier` runs `valid_scenario_passes`, `scenario_rejects_unknown_entity_id`, `scenario_rejects_unmapped_address`, `scenario_rejects_disallowed_external_api`, `scenario_rejects_too_large_memory_delta`. `cargo clippy -p autore-reconstruction --all-targets -- -D warnings` clean.
  QA scenarios: happy — valid LaunchTarget+SetBreakpoint+Continue+CaptureArguments+StopAfterInvocationCount passes verifier. failure — swap CaptureArguments{entity: EntityId::nonexistent} → scenario_rejects_unknown_entity_id fails verifier. Evidence: `.omo/evidence/auto-re-stage-1/task-31-scenario-lang.txt`
  Commit: Y | feat(reconstruction/dynamic): typed debugger scenario AST + verifier with allowlist

- [ ] 32. Extend IDA provider with 7 debug capabilities: `debug.target.launch`, `debug.target.stop`, `debug.scenario.execute`, `debug.function.capture`, `debug.function.trace`, `debug.memory.capture`, `debug.calls.capture`
  What to do / Must NOT do: Append to `providers/ida/` (Wave 3) a new capability group. Configure IDA's debugger backend via `idax::debugger` API (to GDB backend — IDA's debugger module chain (Linux — remote GDB)) per spec §9.1. Implement on the existing IDA provider binary by reusing Wave 2 runtime + Wave 3 hang: handlers for: `debug.target.launch{exe_artifact, env, working_dir}", `debug.target.stop{}`, `debug.scenario.execute{serialized_scenario}` runs full Scenario AST, `debug.function.capture{entity, run_count}` is a convenience sub-flow launching + setting breakpoint + running + capturing arguments/return/registers, `debug.function.trace{entity, depth}`, `debug.memory.capture{addr, size}`, `debug.calls.capture{entity}`. The IDA debugger backend is GDB attached to the Wine-launched EXE (use a stub script `tools/wine-launch-vanburen-gdb.sh` written in this todo — wraps `wine <exe>` and `gdbserver :2345 --attach <pid>` per spec §9.2 / §9.6 user resolution). The provider emits `ObservationProduced{kind=debug.observation, content=<observation JSON via stage_inbound>}` for each captured artifact, `Progress` per scenario step, finally `Completed{status=Passed if executed}]`. Hardware/memory capture and hook bypass failures emit `Diagnostic{severity=Error}` + `Completed{status=Failed}`. The `TargetRunner` abstraction stays pluggable: define `TargetRunner` trait (per spec §9.6) — `WineGdbRunner` first impl, plus a stub `WindowsGdbServerRunner` trait impl that the future x64dbg-direct path will implement — those backends later add only an implementation, not a coordinator change.
  Must NOT do: do NOT spawn scenario execution without going through ScenarioVerifier (todo 31) first — for security per spec §9.7. Do NOT bypass the ConfigurationArtifactTransport (todo 9) for `debug.observation` artifacts. Do NOT capture memory regions the verifier rejected ("core validates" per §9.4). Do NOT bundle the wine script as bash-only — write it IDA-provider-owned artifact at runtime launch. Do NOT implement multi-process or VM isolation (§9.8 deferred).
  Parallelization: Wave 7 | Blocked by: 31, 13 | Blocks: 33, 34
  References: idax 0.3.0 debugger API (PRE-IMPL CHECK: call `skill` with `name="context7_resolve-library-id"` query "idax rust IDA debugger API", then `context7_query-docs` to verify `idax::debugger` exposes GDB backend + Linux + Wine attach capability — if not documented, downgrade Wave-7 first-impl to use IDA's external debugger subprocess directly via `idax::debugger::start_process` + `idax::debugger::set_debugger_options` and log the limitation as an architectural note; SPEC DOES NOT require IDA-bind-binding specifically, only "IDA debugger with GDB backend" which can come from IDA's CLI debugger invoker if the Rust binding lacks it); spec §9.1 (IDA debugger with GDB backend chain), §9.3 (capability list), §9.6 (TargetRunner + Wine first impl), §9.7 (safety boundary)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction dynamic::ida_provider capabilities::` runs `ida_provider_advertises_debug_capabilities` (asserts 7 new capabilities within the discovery response), `launch_then_stop_lifecycle`, `scenario_execute_returns_completed_status_passed` (uses a tiny launch-test fixture scenario with `StopAfterInvocationCount{1}`), `capture_arguments_recorded_for_target_function`, `cancellation_terminates_target_and_emits_diagnostic` (test injects a CancellationToken after launch + before Continue, asserts target terminated and Diagnostic{Warning,cancellation} emitted), `unsupported_runner_blocks_in_verifier` (WindowsGdbServerRunner stub trait impl exists as a different backend; verify Scenario doesn't change shape when used by both runner implementations).
  QA scenarios: happy — `scenario_execute_returns_completed_status_passed` returns `Completed{Status=Passed}` within runtime+grace. failure — fail to verify scenario before invoking provider (regression on todo 31 boundary) → launch an unmapped address; IDA returns Diagnostic{Error}; test asserts verifier already refused. Evidence: `.omo/evidence/auto-re-stage-1/task-32-ida-debug.txt`
  Commit: Y | feat(providers/ida debug): 7 debug capabilities + scenario interpreter via GDB+Wine

- [ ] 33. Build dynamic observation canonical importer (Route observations → canonical records through autore-app, invalidate dependent work)
  What to do / Must NOT do: Module `autore-reconstruction::dynamic::import`. When the IDA provider emits an `ArtifactProduced` for `debug.observation`, the importer does: (1) `RegisterArtifact{kind=trace, content=staged_bytes}` (existing Stage 0 command); (2) `ImportDynamicObservation{observation_kind, captured_artifact_id, target_entity_id, scenario_id, timestamp_range, recorded_at}` (Wave-1 command from todo 12); (3) the imported observation becomes an authoritative evidence record for the target entity — issue `AddEvidence{kind=Verification, ...}` linking the target entity to the new artifact (existing command requests.rs:299); (4) compute updated `WorkItemFingerprint` (todo 18) for the work item that owns this entity; (5) if fingerprint changed, propagate invalidation via `InvalidateWorkItem`/`InvalidateGeneratedSource` commands; (6) emit a `ProjectEvent` of kind `debug.observation-imported` so TUI updates later (Wave 11). Invalidities (e.g. observation scenario token Sequence mismatches that look like replay) emit `ProjectEvent{Diagnostic{Warning,observation-mismatch}}` but DO NOT fail the importer — they are flagged for LLM review via `CreateWorkItems{kind=Investigation}` to detect nondeterminism (relate to spec §13.4 case).
  Must NOT do: do NOT partially import a multi-capture observation — atomic per §3.1. Do NOT persist observation bytes outside canonical artifacts. Do NOT silently swallow nondeterministic observation-flag — must spawn an Investigation. Do NOT cache imported observations without referencing their originating scenario record.
  Parallelization: Wave 7 | Blocked by: 32, 18 | Blocks: 34, 36 (Wave 8 type recovery reuses observations)
  References: spec §9.4 (6-step flow: validates → schedules → executes → validates → imports → invalidates or promotes), §13.4 (no-progress / nondeterminism detection branches), §3.1 (atomic ownership)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction dynamic::import::` runs `observation_importer_emits_three_commands_in_atomic_transaction` (RegisterArtifact + ImportDynamicObservation + AddEvidence in same ApplicationCommand-issued tx via todo-12 atomicity), `importer_recomputes_target_fingerprint`, `importer_propagates_invalidation_to_downstream_work`, `nondeterministic_observation_flags_create_investigation_work_item`. `cargo clippy -p autore-reconstruction --all-targets -- -D warnings` clean.
  QA scenarios: happy — RecordingAutoReClient sees `[RegisterArtifact, ImportDynamicObservation, AddEvidence, InvalidateWorkItem]` for 1 entity. failure — skip the invalidation-propagation → assert sees only first 3 commands; test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-33-observation-import.txt`
  Commit: Y | feat(reconstruction/dynamic): canonical observation importer + downstream invalidation

- [ ] 34. Experiment flow: LLM-proposed scenario → core-validated → scheduled → executed (end-to-end smoke)
  What to do / Must NOT do: Integration test `tests/dynamic_llm_proposed_scenario.rs`. Steps: (1) bootstrap LLM provider (Wave 5); (2) request `llm.experiment.design` for one canonical function (Wave 3 fixture); (3) obtain a `proposed_debugger_experiments[0]` parsed-result containing a typed Scenario AST; (4) ScenarioVerifier (todo 31) validates the LLM-generated scenario — if LLM produces a scenario that violates allowlist etc, assert the bounded schema-repair path (todo 23) tries once then `FailWorkItem`; (5) on validated, schedule via `CreateWorkItems{kind=Investigation, subject=function-entity, experiment_proposal=scenario_ast}`; (6) Coordinator dispatches `debug.scenario.execute` (todo 32) — fixture EXE under Wine; (7) capture observations imported per todo 33; (8) assert dependent analysis work items (the originating Function analysis) were invalidated via fingerprint propagation.
  Must NOT do: do NOT skip validation — if LLM hallucinates an unmapped address, the verifier must reject (proves §9.4 security boundary). Do NOT bypass scheduler—investigation work item must be scheduled, not free-floating. Do NOT execute any non-validated scenario.
  Parallelization: Wave 7 | Blocked by: 33 | Blocks: 35
  References: Wave 5 LLM import pipeline, Wave 7 todos 31-33; spec §9.4 full flow
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test dynamic_llm_proposed_scenario -- --nocapture --ignored` ends with `[OK] experiment proposed, validated, scheduled, executed, observed+imported+op invalidated dependent analysis`.
  QA scenarios: happy — happy-path captures 1+ observation and propagates invalidation. failure — bypass verifier; mock LLM proposes scenario with unmapped address; provider would accept+execute → security-breach; test asserts verifier rejects and `BlockedReason` recorded with `kind=UnvalidatedScenarioRejection`. Evidence: `.omo/evidence/auto-re-stage-1/task-34-llm-experiment-flow.txt`
  Commit: Y | test(reconstruction/dynamic): LLM-proposed scenario → validated → executed end-to-end

- [ ] 35. Be home end-to-end "IDA debugger uses GDB" exit-criterion check + TargetRunner trait seam test
  What to do / Must NOT do: Verify Wave 7's exit-criterion per spec §9.9: coordinator can automatically request and execute structured experiments against the original executable. Build standalone test `tests/wave7_exit_criterion.rs`: (1) launch the same fixture binary used by Wave-7's previous tests; (2) dispatch a bundled scenario that performs launch + setBreakpoint + Continue + CaptureArguments + StopAfterInvocationCount; (3) verify the IDA debugger attached via GDB backend (assert via the provider's Negotiate response namespace `ida.debugger.backend = gdb-wine`); (4) compile-test a `WindowsGdbServerRunner` stub impl of the `TargetRunner` trait showing the trait is backend-agnostic — assert the Scenario struct created for the fixture binary does NOT change shape when serializable for both runners. Lower-bound: scenario's serializable surface must be ≤ 16 KiB JSON for the fixture function.
  Must NOT do: do NOT delete the stub `WindowsGdbServerRunner` after the test — keep committed (provides the future-x64dbg-on-Windows adapter seam).
  Parallelization: Wave 7 | Blocked by: 34 | Blocks: 41 (Wave 9 generation consumes outputs), 46 (Wave 8 type recovery reuses observations), 45 (Wave 10 verification consumes observations via scenario replay)
  References: spec §9.6 (TargetRunner trait), §9.9 (Wave 7 exit-criterion), §23 #2 (final architectural test — "Can a future direct GDB provider implement the debugger scenarios without changing the coordinator?")
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test wave7_exit_criterion -- --nocapture --ignored` ends `[OK] coordinator can schedule + execute structured experiments; backend seams documented`.
  QA scenarios: happy — happy path emits 1 captured observation against the fixture function in <30s. failure — remove the GDB backend stub → `ida.debugger.backend = gdb-wine` missing from provider Negotiate response → test asserts the response's `backend` field == `gdb-wine` and fails. Evidence: `.omo/evidence/auto-re-stage-1/task-35-wave7-exit.txt`
  Commit: Y | test(reconstruction/dynamic): wave-7 exit-criterion + runner-trait seam

### Wave 8 — Shared type and class recovery (Spec §18 Phase 8; §10)

- [ ] 36. Implement deterministic layout constraint model + reconciliation
  What to do / Must NOT do: Module `autore-reconstruction::types`. Define `LayoutConstraintKind` enum per spec §10.2: `FieldObservedAtOffset{entity: EntityId, offset: usize}`, `ReadWidth{entity, width_bytes: usize}`, `WriteWidth{entity, width_bytes: usize}`, `ObjectAllocationSize{entity, size_bytes: usize}`, `VtablePointerLocation{entity, offset}`, `VirtualSlotTarget{slot_idx, vtable: EntityId, called: EntityId}`, `BaseAdjustment{entity, base_entity, offset}`, `FunctionParameterUsage{func, param_idx, kind}`, `ReturnValueUse{func, kind}`, `ArrayStride{entity, stride_bytes}`, `AlignmentRequirement{entity, alignment}`. Implement `LayoutConstraintStore` (canonical via ApplicationCommand::AddEvidence{kind=LayoutConstraint, content=JSON}) — deterministic. Implement `Reconciler::reconcile(constraints: &Vec<LayoutConstraint>) -> ReconciledLayout` (THIS is deterministic pre-LLM step per spec §10.2): merge constraints by entity; check compatible sizes/offsets; record "deterministic-reconciled layout" candidate as one hypothesis via `AddHypothesis{predicate=proposes-deterministic-layout, candidate=ReconciledLayout, confidence=1.0, derived_from=evidence-list}`. For conflicting constraints (e.g. obj_size=32 vs field_offset=64) → emit `CreateWorkItems{kind=ConflictResolution, subject=entity}` per spec §10.3 path (instead of forking duplicate types §10.3 explicit "Never silently fork duplicate types to avoid resolving a conflict").
  Must NOT do: do NEVER call LLM in this todo — deterministic only. Do NOT record a hypothesis as a fact — confidence=1.0 means deterministic-reconciled; promote via `AcceptHypothesisPolicyDriven` later (Wave 11). Do NOT create new entity types silently. Do NOT skip conflict detection.
  Parallelization: Wave 8 | Blocked by: 33, 14 | Blocks: 37, 38
  References: spec §10.2 (constraint kinds), §10.3 (conflict handling 7-step procedure); existing Stage 0 evidence records
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction types::reconciler` runs `reconciler_produces_layout_hypothesis_for_compatible_constraints`, `reconciler_creates_conflict_work_item_for_incompatible_size_offset`, `reconciler_handles_array_stride`, `reconciler_does_not_fork_duplicate_types_on_conflict` (asserts ONE `AddHypothesis` per entity, zero for conflicting — ConflictResolution work item instead). Clippy clean.
  QA scenarios: happy — 3 compatible FieldObservedAtOffset → 1 ReconciledLayout hypothesis, confidence=1.0. failure — emit both sizes simultaneously + accept first as canonical → conflict-not-forked assertion fails seeing 2 hypotheses. Evidence: `.omo/evidence/auto-re-stage-1/task-36-types-reconciler.txt`
  Commit: Y | feat(reconstruction/types): deterministic constraint model + reconciler

- [ ] 37. Implement shared canonical type/class hypothesis store + per-field-vs-per-layout verification tracking
  What to do / Must NOT do: Add `CanonicalTypeHypothesis` record in autore-schema (per §10.4 layout-confidence split). Per-field verification tracked separately: `VerifiedSize`, `VerifiedAlignment`, `VerifiedIndividualFieldOffset{field_name}`, `VerifiedFieldInterpretation`, `VerifiedInheritanceRelation`, `VerifiedVtableSlot`, `VerifiedCallingConvention`. Implement `MarkVerified(field: &VerificationField, canonical: &CanonicalTypeHypothesis) -> Result<()>` via `AddVerification` command (existing Stage 0 command, requests.rs:303) — and update confidences accordingly. A class is NOT fully-verified only because its total size is known.
  Must NOT do: do NOT collapse all "verified" bits to a single boolean. Do NOT mark `VerifiedInheritanceRelation` until base hypothesis is also verified (dependency). Do NOT enable deposit without upstream verification.
  Parallelization: Wave 8 | Blocked by: 36 | Blocks: 38, 41 (Wave 9 generation requires types)
  References: spec §10.4 (per-element verification split), §10.1 (shared canonical hypothesis list); existing AddVerification command
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction types::verification_split` runs `size_verified_does_not_implicitly_verify_offset`, `offset_verified_does_not_implicitly_verify_field_interpretation`, `inheritance_relation_requires_base_verified`. Clippy clean.
  QA scenarios: happy — record `VerifiedSize` on a Type → assert only `VerifiedSize` flag set; `VerifiedAlignment` still false. failure — collapse into "verified" boolean → per-field tally assertion fails seeing all-or-nothing. Evidence: `.omo/evidence/auto-re-stage-1/task-37-types-verification.txt`
  Commit: Y | feat(reconstruction/types): canonical hypothesis store + split per-field verification

- [ ] 38. Implement LLM conflict-arbitration flow (llm.analysis.conflict capability)
  What to do / Must NOT do: When a `ConflictResolution` work item is processed by the coordinator (Wave 11), the worker uses `llm.analysis.conflict` (Wave 5 todo 21). Provide the bundle including `accepted_hypotheses` for layout-X and layout-Y, the conflicting constraints, and supporting/detracting evidence ids. The LLM returns per §8.4 capability result: `recommended_resolution`, `explanations`, `confidences_per_resolution`. The core (per spec §10.3 step 6) calls `AcceptHypothesisPolicyDriven{hypothesis_id, policy_decision=Accept/Reject/Supersede, justification=explanation-LLM-bundle, superseding_hypothesis_id}` (Wave-1 command added in todo 2 — handler in todo 12). On supersede, `InvalidateGeneratedSource` for any generated declarations using the superseded hypothesis — rerun build via Wave 9.
  Must NOT do: do NOT auto-accept LLM arbitration as a fact — must be policy-driven transition (§3.3 invariant). Do NOT let LLM create new canonical types — only Accept/Reject/Supersede existing hypotheses. Do NOT skip downstream-code invalidation.
  Parallelization: Wave 8 | Blocked by: 37, 21 | Blocks: 41 (Wave 9 generation consumers of accepted types)
  References: spec §10.3 (conflict resolution loop); Wave 5 LLM provider import boundary; §3.3 (evidence before belief)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction types::conflict::` runs `llm_arbitration_produces_accept_or_reject_or_supersede_command`, `supersede_invalidates_affected_generated_sources`. Use a mock LLM returning canned conflict-analysis result. Clippy clean.
  QA scenarios: happy — mock returns "accept layout-Y reject layout-X"; assert `AcceptHypothesisPolicyDriven` and `InvalidateGeneratedSource` issued. failure — bypass policy → no `AcceptHypothesisPolicyDriven` issued; test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-38-conflict-arbitration.txt`
  Commit: Y | feat(reconstruction/types): LLM conflict-arbitration flow + generated-source invalidation on supersede

- [ ] 39. Generated shared declarations (hpp) and vtable scaffolding from accepted hypotheses (deterministic — NOT LLM)
  What to do / Must NOT do: Implement `DeclarationGenerator::generate_accepted_types()` that, for every accepted CanonicalTypeHypothesis (post todo 38), emits/updates an `include/recovered/<type>.hpp` source artifact under `generated/openvb/`. Use upstream IDA type-snapshot artifact + accepted LayoutConstraintReconciled fields → C struct/class decl. Repeat for every accepted CanonicalVtableHypothesis → emits a `vtable` decl with virtual method slots in canonical slot order. Updateter updates via `RegisterArtifact{kind=generated-declaration}` and `RegisterGeneratedSourceMapping`, transitioning prior `Stubbed` source-mappings to `Replaced`. `RunBuild` (already in the todo 2 command list) drives Wave-9's patch control (todo 41 / 42 — generation invokes validation+build then accept-or-roll back).
  Must NOT do: do NOT call LLM in this todo (§11.6 stubs are deterministic; declarations are derived). Do NOT generate methods' bodies — only declarations. Do NOT generate conflicting layouts; if two accepted hypotheses inter-related by AcceptedInheritanceRelation imply different sizes for the same struct → emit a new `BuildFailure` work item.
  Parallelization: Wave 8 | Blocked by: 38 | Blocks: 41
  References: spec §11.4 (`llm.generation.declaration`); §10.4 (verification split reuses here)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction types::declaration_gen` runs `declaration_generator_emits_hpp_for_accepted_type` (asserts output dir, file present), `declaration_generator_replaces_stub_on_accept` (stub mapping marked `Stubbed` → `Replaced`), `declaration_generator_emits_vtable_in_canonical_slot_order`, `conflicting_accepted_layouts_create_build_failure_work`. Clippy clean.
  QA scenarios: happy — one accepted type → 1 hpp file persisted under artifacts, mapping updated. failure — accept two conflicting types simultaneously → expected `BuildFailure` work item not emitted (asserting zero such work items). Evidence: `.omo/evidence/auto-re-stage-1/task-39-declaration-gen.txt`
  Commit: Y | feat(reconstruction/types): deterministic declaration generator from accepted hypotheses

- [ ] 40. Wave-8 exit slice: shared type/class coherent evolving model end-to-end (Wave 3 + Wave 8)
  What to do / Must NOT do: Integration test `tests/wave8_shared_model.rs`: (1) ingest small fixture binary (Wave 3); (2) issue a sample of `LayoutConstraint` evidences for two differing types via `AddEvidence`; (3) run `reconciler` (todo 36) → some yield accepted hypotheses, some flow to `ConflictResolution`; (4) for one conflict, dispatch `llm.analysis.conflict` against mock LLM (`providers/openai-compatible` with mock server); (5) accept the resolution via `AcceptHypothesisPolicyDriven`; (6) run `DeclarationGenerator` (todo 39); (7) assert the generated `recovered/types.hpp` reflects the accepted hypothesis (single-source-of-truth evidence). Run `VerifyGeneratedDeclarations` — assert the entire function skeleton still tamper-not-modified EXCEPT the declaration side; build is still green (via Wave 6's `BuildProvider`).
  Must NOT do: do NOT use a real LLM in the test path. Do NOT skip the conflict-resolution step. Do NOT accept the LLM output without `AcceptHypothesisPolicyDriven`.
  Parallelization: Wave 8 | Blocked by: 39 | Blocks: 41 (Wave 9 generation), 45-48 (Wave 10 verification consumes accepted types)
  References: spec §10.6 (exit criterion), §23 #1 (future backend reuse same typed hypotheses)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test wave8_shared_model -- --nocapture --ignored` ends with `[OK] shared types recovered, declaration artifacts up-to-date; build green`.
  QA scenarios: happy — generation passes and build still compiles. failure — emit conflicting layout in test setup without going through conflict-resolution → build fails (raw-text change); detect via an immediate build attempt + diagnostic check. Evidence: `.omo/evidence/auto-re-stage-1/task-40-wave8-exit.txt`
  Commit: Y | test(reconstruction/types): wave-8 shared type/class model coherent evolving end-to-end

### Wave 9 — Function and cluster generation (Spec §18 Phase 9; §11.4, §12.4)

- [ ] 41. Implement LLM generation capabilities (`llm.generation.declaration`, `llm.generation.type`, `llm.generation.function`, `llm.generation.cluster`, `llm.generation.test`, `llm.generation.repair`) + provider wiring
  What to do / Must NOT do: Append to `providers/openai-compatible/` six new capability handlers per spec §11.4. Each handler accepts an `InvestigationBundle` (todo 22) plus a `GenerationContext` (accepted types, accepted specs, generated stubs map, prior generated candidate if regeneration, compiler diagnostics if repair). Capability schemas (committed under `autore-reconstruction/schemas/generation/*.schema.json` per capability): `generation.declaration.schema.json` (headers + declarations side); `generation.type.schema.json` (full struct/union decl); `generation.function.schema.json` (entity_target_id, candidate_source_bytes, declarations_required, assumptions, dependencies, unsupported_behavior (string[]), proposed_tests (test_id list), source-to-evidence references); `generation.cluster.schema.json` (extends function for cluster-coordinates cluster member ordering, init order); `generation.test.schema.json` (target_unit, test_kind (unit|differential|scenario), naive expected observations); `generation.repair.schema.json` (prior_candidate_bytes, diagnostics, recommended_repair_bytes, new_candidate_source_bytes, handoff_followups). Provider emits `ObservationProduced{kind=llm.raw-response}` for raw, plus `ArtifactProduced` with stage_inbound deliverable for the candidate. Prompts under `providers/openai-compatible/prompts/generation/*.handlebars`.
  Must NOT do: do NOT bundle artifacts into the response — only references + candidate bytes staged via ArtifactTransport. Do NOT bypass per-capability result schema. Do NOT collapse `llm.generation.cluster` into `llm.generation.function` (separate schema/task per §11.4). Do NOT allow `\n` arbitrary newline, breaking content negotiation contract.
  Parallelization: Wave 9 | Blocked by: 21, 27, 40 | Blocks: 42
  References: Wave-5 LLM provider crate pattern; spec §11.4 (capability list + inputs + result fields)
  Acceptance criteria (agent-executable): `cargo test -p openai-compatible-provider generation::` runs `provider_advertises_six_generation_capabilities`, `generation_function_schema_rejects_missing_entity_target_id`, `generation_test_schema_rejects_unsupported_test_kind`. Use mock LLM for end-to-end. Clippy clean.
  QA scenarios: happy — 6 capabilities advertised via `DiscoverCapabilities`, schemas parse via jsonschema 0.33. failure — drop required-field validation on `generation.function` → test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-41-generation-providers.txt`
  Commit: Y | feat(providers/openai-compatible/generation): 6 generation capabilities with JSON-Schema results

- [ ] 42. Implement controlled staged source-patching pipeline (validate targets, parse, syntax-check, patch-and-rollback)
  What to do / Must NOT do: Module `autore-reconstruction::generation::patch`. Pipeline per spec §11.5: `model_candidate -> validate_file_targets` (reject blank / outside `generated/openvb/` paths / undeclared file deletion / binary output > 16 MiB / changes to auto-re repo / changes unrelated to work item) `-> stage_candidate_artifacts (via ArtifactTransport stage_inbound)` `-> parse_or_syntax_check` (call `clang -fsyntax-only -std=c++98` against the tmp file via the build provider's `compile_units` with a dry-run flag; OR if clang unavailable on this machine, use an embedded syntactic validator from a tiny crate; resolution: include `tree-sitter-cpp` workspace dep OR a simpler lighter `nom` based surface — either way, parsing must catch malformed output) `-> construct_controlled_patch` (compare against the prior generated file via a unified-diff builder; produce one patch per file) `-> apply_through_generated_project_manager` (the operation is wrapped in `ApplicationCommand::ImportGeneratedSourceCandidates` — already in the todo 2 canonical command list; handler executes with full transactional artifact+mapping update) `-> build` (Wave-6 build-provider compile+link via Wave-9 todo 28's `BuildProvider`) `-> accept_or_roll_back` (success → `accept` finalizes mapping; fail → `discard` the staged patches per §11.5 "Reject: changes unrelated to the assigned work item"). Implement explicit Acceptance policy: candidate must satisfy Traits: (a) builds (passes base compile+link), (b) passes lint if asked (`/ ayant/cmake --target lint` operator-configured), (c) source-evidence references verify referenced entities exist (also Todo 23 wave 5; see Wave 10 verification).
  Must NOT do: do NOT let the LLM edit the working tree directly (spec §11.5 absolute). Do NOT bypass transactional boundaries (changes roll forward as one atomic commit through ImportGeneratedSourceCandidates). Do NOT commit before build success (always `Build first, accept-or-rollback on diagnosed outcome`).
  Parallelization: Wave 9 | Blocked by: 41, 28 | Blocks: 43, 44
  References: spec §11.5 (pipeline literal); tree-sitter-cpp via Context7 (resolve-library-id `tree-sitter-cpp`); existing Stage 0 artifact/operation infrastructure
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction patch::` runs `apply_rejects_paths_outside_generated_openvb` (candidate modifies `auto-re/Cargo.toml` → rejected), `apply_rejects_undeclared_deletion` (candidate removes an existing declared file via staged patch without deletion-request flag; rejected), `apply_rolls_back_on_build_failure` (candidate compiles alone but breaks other functions → build fails → staged patches discarded and prior source map remains), `atomic_accept_on_build_pass` (mapping transitions Stubbed→Replaced, build recorded as Pass). Clippy clean.
  QA scenarios: happy — patch applied successfully + accept → mapping goes `Stubbed → Replaced`. failure — remove the path-validation step → `apply_rejects_paths_outside_generated_openvb` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-42-patch-control.txt`
  Commit: Y | feat(reconstruction/generation): controlled staged patch pipeline with atomic accept/rollback

- [ ] 43. Implement leaf-first stub-replacement scheduler ordering + repair loop
  What to do / Must NOT do: Module `autore-reconstruction::generation::orchestrator`. Priority order: leaf functions first (functions with no remaining Stubbed callees), then small cluster-mates (cluster's members all leaf-stub-free at cluster level), per spec §11 priority. Per work-item kind: For a `Function` work item, request `llm.generation.function`; the InvestigationBundle includes existing accepted declarations+function-skeleton stub-state; build via Wave-6 provider; parse diagnostics via Wave-6 todo 30 classify; deterministic-repair (declaration / unknown-type) via `BuildFailure → CreateWorkItems{Generation, kind=MissingDeclaration}` (via Todo 30's `select_repair_strategy`); on `GeneratedCodeDefect` kind diagnoses the LLM via `llm.analysis.failure` (existing Wave 5 LLM provider capability); gets back a fix → attempt patch+build again. Bounded retry per spec §12.4: max 3 repair attempts per work item (configurable). Detect repeated-equivalent failures via a TODO memory: 3 diagnostic-by-code-occurrences (same MSVC code, same line/col) → mark `BlockWorkItem{kind=RepeatedEquivalentFailure}` → Wave 11 detects via no-progress detector.
  Must NOT do: do NOT include test generation/repair covariant with code (it is done later separately at Wave 9's leaf-repair step). Do NOT skip Spec heal rule that syntaxed code repairs eligible for deterministic paths must go deterministic FIRST (§12.3 explicit: "Use deterministic repairs where possible"). Do NOT progress past Wave-6 build attempt without a result-record (`RecordBuildAttempt` via Wave 1 command).
  Parallelization: Wave 9 | Blocked by: 42, 30, 36 | Blocks: 44, 51
  References: spec §11 priority leaf-first; §12.4 build loop; §12.3 deterministic-then-LLM; existing `BuildFailure` work kind from Wave-1
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction generation::orchestrator` runs `leaf_first_ordering_priority_max_when_no_stubs_remaining_in_callees`, `deterministic_repair_paths_covered_before_llm_repair_invoked` (one MissingDeclaration → 1 deterministic work-item created; verify no LLM request dispatched by RecordingAutoReClient dispatch count for LLM provider), `llm_repair_invoked_on_generated_code_defect_after_deterministic_exhaustion`, `bounded_retry_max_3_creates_blocked_work` (4 attempts of same error → `BlockWorkItem` recorded with `kind=RepeatedEquivalentFailure`). Clippy clean.
  QA scenarios: happy — sequence `[deterministic-repair-on-C2065, acceptedStub→Replaced]` for one function completes without LLM intervention. failure — drop bounded retry → assertion `bounded_retry_max_3_creates_blocked_work` fails seeing max-3 attempts then coordinator loops. Evidence: `.omo/evidence/auto-re-stage-1/task-43-generation-orchestrator.txt`
  Commit: Y | feat(reconstruction/generation): leaf-first ordering + bounded repair loop + no-progress-on-tripleted-failure

- [ ] 44. Wave 9 exit slice: progressive stubs→compiling candidate implementations for small-fixture
  What to do / Must NOT do: Integration test `tests/wave9_stub_replacement.rs` progresses at least 3 functions from stub to replaced; uses mock LLM for clarity. Setup: ingest small fixture binary → skeleton generated (Wave 6) → wait that all accepted types from Wave 8 are settled. Schedule work-item for each function. For 3 functions, arrange ordered mocks: f_a with simple body `return f_b();` requiring f_b declared; f_b with body `return RUNTIME_DATA[0];` requiring `RUNTIME_DATA` global addressed; f_c idempotent identity. Assert: f_a initially issued by scheduling ruled-out due to f_b not-yet-replaced; f_b leaf-first issued → `llm.generation.function` mock-lm response; patch applied; build passed; mapping stubbed→replaced; dps and downstream unblocked include f_a. Asserts with every mutation going through ApplicationCommand (RecordingAutoReClient invariant).
  Must NOT do: do NOT use real LLM. Do NOT skip build invocation (Wave 6 build-provider must run on each candidate). Do NOT skip downstream unblocking check.
  Parallelization: Wave 9 | Blocked by: 43 | Blocks: 51
  References: spec §11.4 (declaration follow-up), §11.6 (stub explicit), §12.4 (build loop)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test wave9_stub_replacement -- --nocapture --ignored` ends with `[OK] 3 functions: stubbed→replaced, build green, downstream unblocked`. 
  QA scenarios: happy — mappings seen for 3 functions with last_change_hash of candidate artifact. failure — force f_a generational relaxation before f_b replaced → f_a dispatched early → f_b stub; f_a candidate fails build → require waiting/order-violation log. (the leaf-first ordering must prevent this; assert the early-dispatch attempt blocks via `BuildFailure` work item with `MissingDeclaration` of f_b.) Evidence: `.omo/evidence/auto-re-stage-1/task-44-wave9-exit.txt`
  Commit: Y | test(reconstruction/generation): end-to-end stubs-replaced leaf-first for small fixture

### Wave 10 — Differential verification and repair (Spec §18 Phase 10; §13)

- [ ] 45. Scenario capture/replay/comparison model — typed, normalized, differential
  What to do / Must NOT do: Module `autore-reconstruction::verification`. Define `Scenario` per spec §13.2: `initial_state{env, argv, working_dir, seed}`, `inputs`, `executable_artifact_id (original)`, `candidate_artifact_id (generated)`, `execution_steps`, `comparison_policy`, `normalization_rules[].kind` (per spec §13.2: RelocatedAddress, Timestamp, RandomSeed, EnvSpecificHandle), `comparison_level` (Function, Cluster, Whole-program). Implement `ScenarioExecutor::execute_original(scenario) -> ObservationSet` (invokes Wave 7 dynamic provider — absolute source is `original-executable artifact`), `ScenarioExecutor::execute_candidate(scenario, generated_binary_artifact)` (uses TM TargetRunner abstraction — for Stage 1, also Wine+GDB at slight different entry point). Implement `comparator::compare(obs1: ObservationSet, obs2: ObservationSet, normalization_rules) -> VerificationComparison` per spec §13.3 typed differences: `Equal`, `EquivalentUnderNormalization`, `Different`, `Inconclusive`, `NotObserved`, `ExecutionFailed`. Per `VerificationComparison` counts (equal_count, equivalent_count, ..., equal/values/failed). Persist via `RecordVerificationComparison` Wave-1 command (Wave 4 migration V33).
  Must NOT do: do NOT equate test-success with full program equivalence (§13.3 explicitly). Do NOT do raw bytes comparison — only typed observation comparisons. Do NOT enable unnormalized comparisons — verify via `normalization_rules` per spec §13.2.
  Parallelization: Wave 10 | Blocked by: 33, 44 | Blocks: 46, 47
  References: Wave-7 dynamic pipeline; spec §13.2 (scenario shape), §13.3 (comparison kinds)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction verification::` runs `scenario_normalize_relocated_addresses` (same function at address-origin X that shifted by 0x10000 → equal after normalization), `scenario_normalize_timestamps`, `scenario_normalize_random_seeds_before_comparison`, `equal_under_normalization_stored_as_equivalent_under_normalization`, `different_diff_boosted_to_repair`, `execution_failed_exited_with_code_139_classified_execution_failed_with_diagnostic`. Clippy clean.
  QA scenarios: happy — two identical except shifted addresses → eventual comparison kind `EquivalentUnderNormalization`. failure — omit normalization → assertion sees `Different`; test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-45-scenario.txt`
  Commit: Y | feat(reconstruction/verification): scenario capture/replay + typed normalization + comparison model

- [ ] 46. Verification-driven repair + LLM failure analysis
  What to do / Must NOT do: Implement spec §13.4 8-step flow: On `Different`/`Inconclusive`/`ExecutionFailed`:
1. Record VerificationComparison (todo 45 commit). 2. Associate with affected entities (`related_entity_id` from scenario). 3. Determine likely cause category (Implementation|Type|Environment|Scenario) — via per-level rules (Wave-6 dialect, Wave-8 layout mismatch, etc.). 4. Create `CreateWorkItems{kind=Investigation}` for Static OR Dynamic investigation depending on cause — Dynamic refers to Wave-7 IDA provider; Static refers to Wave-5 LLM analysis OR TODO-43 generation repair. 5. Run `llm.analysis.failure` (Wave 5 capability) on bounded diff (optimizing tokens); LLM returns recommended_repair + recommendations. 6. Generate revised candidate via `llm.generation.repair` (Wave 9 capability). 7. Rebuild via Wave 6 provider. 8. Re-run failed scenario AND regression scenarios to ensure no reintroduced bugs.
  Must NOT do: do NOT skip step 8 (the regression guard per spec §13.4 — "Re-run the failed scenario and relevant regression scenarios"). Do NOT auto-pass based on equivalent_under_normalization if any failing test exists in regression list. Do NOT use the LLM to write any canonical records directly.
  Parallelization: Wave 10 | Blocked by: 45, 38 | Blocks: 47
  References: spec §13.4 8-step flow; Wave-5 analysis failure; Wave-9 generation repair
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction verification::repair::` runs `mismatch_creates_investigation_work`, `llm_analysis_failure_invoked_with_bounded_diff`, `repair_candidate_rebuilds` , `invocation_re_runs_scenarios_and_regression`. Clippy clean.
  QA scenarios: happy — `[Record, AddEvidence, LLM-analysis-failure, LLM-generation.repair, Build, Re-run-scenario]` via mock LLM counting tokens. failure — skip regression rerun → test fails on step-8 assertion. Evidence: `.omo/evidence/auto-re-stage-1/task-46-verification-repair.txt`
  Commit: Y | feat(reconstruction/verification): mismatch-driven repair + regression guard

- [ ] 47. Regression selection on dependency change
  What to do / Must NOT do: Module `autore-reconstruction::verification::regression`. For each accepted implementation (state == Compiled/Verified-function OR Verified), retain regression set per spec §13.5:
- scenarios that verified it (list of scenario_ids)
- dependency fingerprints (transitive accepted hypotheses fingerprints)
- affected-type dependencies (supplied types used in this entity)
- build dependencies (which build profiles used).
When a dependency changes (build config change, fingerprint change in callees per §7.5) via `InvalidateWorkItem` triggered, also issue `ScheduleVerificationRegression{entity_id}` (in the todo 2 command list): this re-runs verification scenarios for the affected entity. Match dependencies via `work_dependencies` where edge_kind is `BuildDependency` or `VerificationDependency`. Implement explicit maximal cost-bound: regression only on `GeneratedDeclRequirement` & `Verification` edges (Spec §13.5 explicit).
  Must NOT do: do NOT regress on every prior scenario — only relevant regression set. Do NOT skip regression on dependent-type-changes (§13.5 explicit: "Changes to shared types or callees must rerun dependent verification").
  Parallelization: Wave 10 | Blocked by: 46, 18 | Blocks: 48
  References: spec §13.5 (regression selection criteria); Wave-4 fingerprint propagation; Wave-8 generation-induced propagations
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction verification::regression::` runs `change_to_shared_type_modifies_regression_set_of_dependent_entities`, `stale_fp_of_dependencies_triggers_regression`, `regression_contains_only_specific_callee_scenarios`, `non_build_verification_dep_not_in_regression_set`. Clippy clean.
  QA scenarios: happy — change `SomeSharedType` fingerprint → dependent function regression count matches set size ≤ config max. failure — include non-`BuildDependency`/non-`VerificationDependency` edges in regression set → assertion `non_build_verification_dep_not_in_regression_set` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-47-regression.txt`
  Commit: Y | feat(reconstruction/verification): regression selection keyed on dependency fingerprints/edges

- [ ] 48. Wave 10 exit slice: function-level + cluster-level differential test success
  What to do / Must NOT do: Integration test `tests/wave10_differential.rs` verifies 2 implemented functions from Wave-9 (from small fixture) together with a fixed scenario (`Run f_a then f_b with input seed X`): original execution captured as observation set; candidate executed under Wine+GDB; compared; passing §13.1 function-level + cluster-level. Force a regression: Wave-8 todo 39 path replaces stub mapping with a generated candidate (likely returns different bytes); Wave-10 todo 47 schedule triggers regression to re-run; assert no reintroduced failure (mock LLM keeps consistent behavior). Terminal state = function verified.
  Must NOT do: do NOT skip the regression step (Wave-10 todo 47 invariant). Do NOT use real LLM. Do NOT pass on `ExecutionFailed` (must trigger repair).
  Parallelization: Wave 10 | Blocked by: 46, 47, 44 | Blocks: 51
  References: Wave-7 dynamic; Wave-9 generation; spec §13.1 (three verification levels)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test wave10_differential -- --nocapture --ignored` ends with `[OK] function-verified + cluster-verified + regression-passed`.
  QA scenarios: happy — verification variants all-green with mock LLM. failure — drop the regression guard → regression counter doesn't run after stub escalation; assertion `regression_ran == true` fails. Evidence: `.omo/evidence/auto-re-stage-1/task-48-wave10-exit.txt`
  Commit: Y | test(reconstruction/verification): wave-10 end-to-end differential verification with regression guard

### Wave 11 — Whole-executable autonomous run (Spec §18 Phase 11; §14, §15, §16)

- [ ] 49. Implement durable coordinator loop with no-progress detection + completion-policy gate
  What to do / Must NOT do: Module `autore-reconstruction::coordinator`. Implement spec §14.1 as a `Coordinator::tick()` async fn: `reconcile_interrupted_operations` (Wave 11 todo 55), `refresh_provider_health`, `refresh_program_structure_if_requested` (Wave-3 IDA refresh), `update_work_dependencies`, `invalidate_stale_work` (Wave-4 fingerprint), `promote_ready_work`, `select_ready_work` (Wave-4 scheduler), match work kind → handler: StaticInvestigation → invoke Wave-3 IDA provider; DynamicInvestigation → invoke Wave-7 IDA-GDB scenario; SemanticAnalysis → Wave-5 LLM; ConflictResolution → Wave-8 pipeline; Generation → Wave-9 generation; BuildFailure → Wave-9 repair orchestration; Verification/VerificationFailure → Wave-10 pipeline. Import result atomically (Wave-12/atomicity tests). `atomic_import_per_iteration` invariant — all coordinator tick outputs come through ApplicationCommand. Per §14.2 false-progress paths: detect "repeated identical model output" comparing raw responses hash to last-3 attempts (per entity); emit BlockWorkItem with kind tag per spec §14.2 list.
  Must NOT do: do NOT introduce an LLM-controlled scheduler (§7.4 still applies). Do NOT exit complete-with-blocked as success. Do NOT skip reconciliation at startup (§17 explicit). Do NOT loop without async cancellation — coordinator loop must check `CancellationToken::is_cancelled` always-between ticks.
  Parallelization: Wave 11 | Blocked by: 19, 25, 32, 44, 48 | Blocks: 50, 51, 52
  References: spec §14.1, §14.2, §14.3, §17 (restart procedure)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction coordinator::` runs `coordinator_tick_executes_expected_handlers_per_work_kind`, `no_progress_on_repeated_identical_model_output_triggers_blockwork`, `coordinator_respects_cancellation_token_between_ticks`, `coordinator_refresh_reconciles_interrupted_operations`, `coordinator_completeness_does_not_pass_with_terminal_blocked`. `cargo clippy` clean.
  QA scenarios: happy — 3 identical LLM responses → block event then stops. failure — remove cancellation-token check → assert tick not checking it breaks. Evidence: `.omo/evidence/auto-re-stage-1/task-49-coordinator.txt`
  Commit: Y | feat(reconstruction/coordinator): durable tick loop with no-progress detection and atomic per-iteration import

- [ ] 50. CLI surface — `auto-re reconstruct {start,status,pause,resume,stop,validate}`, `auto-re provider {refresh,list,show,start,stop,restart,health}`, `auto-re work {list,show,blockers,retry,dependencies}`, `auto-re generated {status,files,entity}`, `auto-re build latest`, `auto-re verification coverage`
  What to do / Must NOT do: Edit `autore-cli/src/cli.rs` and handlers. Add to existing `enum Command` (file read needs inspection at implementation time, similar pattern to `OperationCommand` shown in autore-cli/src/cli.rs:322-349). `auto-re reconstruct start --binary <path> --output <path> --analysis-provider <provider_spec> --model-provider <provider_spec> --build-profile <profile_id>` registers the binary with ArtifactId and registers a `ReconstructionCampaign`, launches the coordinator (or pins to current session). Status/pause/resume/stop issue the right ApplicationCommand/Query. 增强 `auto-re provider refresh` reads installed provider roots, refresh caches OptionList. `auto-re {provider,work,generated,...}` — all read commands support `--output json` per Stage 0 json pattern. Make all read queries synchronous (non-blocking), no-spin.
  Must NOT do: do NOT introduce new binary-  `auto-re` is Stage 0's `auto-re` (crate `autore-cli`) — Stage-1 verbs append. Do NOT remove Stage-0 commands. Do NOT change `--output json` payload format (type-stable per Stage 0 json discipline).
  Parallelization: Wave 11 | Blocked by: 49, 12 | Blocks: 51
  References: autore-cli/src/cli.rs (existing command structure); Stage 0 `--output json` discipline; spec §15 (full CLI list); Stage 0 plan §3.6
  Acceptance criteria (agent-executable): `cargo test -p autore-cli --help-output` runs `reconstruct_start_help_has_all_flags`, `provider_show_help_has_all_flags`, `work_retry_help`, `generated_files_stable_json_output` (serializes and re-parses identical). `auto-re --help` lists the new subcommands. Clippy clean.
  QA scenarios: happy — JSON output round-trip preserves schema. failure — omit `--output json` option on one subcommand → test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-50-cli.txt`
  Commit: Y | feat(cli): Stage 1 reconstruct / provider / work / generated / verification commands

- [ ] 51. TUI Ratatui extension — campaign coverage + work queue + providers + diagnostics + verification diffs
  What to do / Must NOT do: Edit `autore-tui/src/tui.rs` (read at impl time; existing module structure confirmed). Add new panes per spec §16: campaign state / overall entity coverage / functions discovered/stubbed/generated/compiled/verified/blocked / type and class progress / unresolved conflicts / current build status / verification coverage / active providers / active operations / current work queue / recent compiler failures / recent verification failures / stagnation reasons. Keys: `o` start; `p` pause; `r` resume; `X` stop campaign; `n` cancel active op; `R` retry blocked work; `e` inspect entity; `H` inspect evidence; `y` inspect hypothesis; `g` inspect generated source; `D` inspect build diagnostics; `V` inspect verification diff; `P` provider start/stop/restart dialog. Reuse `AutoReClient` trait + `RecordingClient` test double pattern (autore-tui/src/tui.rs:2174-2279). Provider/storage operations run on tokio worker task; never block render thread — follow existing pattern in autore-tui/src/runtime.rs (referenced in pre-Wave-1 plan content). Use existing `ProjectEventSubscription` for live updates; no polling.
  Must NOT do: do NOT replace existing 7 panes (extend per spec §4.1: "Adapt existing TUI panes"). Do NOT block render loop on `execute` calls — dirty pattern async-spawn the command as existing pattern in autore-tui/src/tui.rs:230 possibly. Do NOT change from `TuiState` projection-only pattern (Stage 0 invariant).
  Parallelization: Wave 11 | Blocked by: 50, 49 | Blocks: 52
  References: autore-tui/src/tui.rs (existing 7-pane + event loop); README.md:188-218 (existing keybindings); spec §16 (full pane + action list)
  Acceptance criteria (agent-executable): `cargo test -p autore-tui` runs `tui_renders_campaign_coverage_pane`, `tui_renders_work_queue_pane`, `tui_provider_start_action_issues_command` (uses RecordingClient double — presses `P`, asserts ApplicationCommand issued), `tui_cancel_op_action_emits_cancel_command`, `tui_no_blocking_render_in_storage_ops` (mock storage with sleep; assert render tick still progressing).
  QA scenarios: happy — new panes all render under `TestBackend`. failure — call `execute` synchronously on render thread → `tui_no_blocking_render_in_storage_ops` detects render IntMutexGreater-Than. Evidence: `.omo/evidence/auto-re-stage-1/task-51-tui.txt`
  Commit: Y | feat(tui): Stage 1 coverage/work/providers/diagnostics/verification panes + actions

- [ ] 52. Whole-executable autonomous run on small fixture + restart-recovery scenario
  What to do / Must NOT do: Integration test `autore-reconstruction/tests/coordinator_autonomous_run.rs` (tagged `#[ignore]` for slow path). All deterministic mocks. Steps: (1) register a small-fixture binary's `core.binary` artifact; (2) arrange the three provider manifests (IDA provider, OpenAI-compatible LLM provider with mock-deterministic responses, build-provider with mock docker); (3) run `auto-re reconstruct start --binary <fixture> --output /tmp/coordinator-output --analysis-provider ida --model-provider openai-compatible --build-profile msvc-docker`; (4) the coordinator loop (todo 49) is invoked synchronously with a real `tokio::runtime::Runtime`, max-1000-tick budget, real Wave-3/5/6/7/9/10 pipelines backed by mock providers; (5) assert the loop reaches a terminal state (every required work item is Verified | Blocked | ExplicitlyExcluded per §14.3) WITHOUT exceeding the tick budget; (6) throughout, assert zero mutations outside ApplicationCommand (RecordingAutoReClient weekly CTP-five-zero Glob invariant); (7) now induce interruption: between tick `k` and `k+1`, invoke `auto-re reconstruct stop`, drop the in-memory coordinator process state SIMulately-restart from disk; (8) invoke `auto-re reconstruct resume`; (9) assert §17 restart procedure: old local provider instances marked Unavailable by the bootstrap launch-replacement test (query `auto-re provider list` — fixture/ida providers shown Unavailable), expired leases recovered (no orphan leases), uncommitted staging data swept (`StagingReconciler::sweep` ran on resume), committed artifacts preserved; non-idempotent operations not repeated (RecordingAutoReClient dispatch did not re-issue `CreateWorkItems`/`CreateReconstructionCampaign` since those would have been already-completed-equivalent now); (10) wait for terminal state via resumed loop; assert conclusive end-state.
  This todo is implementation-success proof-of-end-state — the Van Buren-real-binary run (the §20 campaign-success criterion) is a separate manual operator activity, NOT a Wave-11 todo (huge binary + real LLM; asserted by an integration report only, not an automated test). Substitute `samples/hello.exe` or a sanitized 8-function fixture generated via IDA on an `int main(){return factorial();}` source. The coordinator must process the ENTIRE fixture binary without manual function selection (proven because no individual `auto-re work retry` was issued by the test).
  Must NOT do: do NOT use real LLM (provider-run windows are bound but the underlying model is mocked per Wave-5 e2e pattern). Do NOT use real Van Buren binary inside the test — Van-Buren-real-binary is operator-driven manual acceptance per §20 and is out of scope as an AUTOMATED commit-blocking test. Do NOT loop beyond max-1000-tick budget (a no-progress detector should kick in well before — Wave-11 todo 49 owns that invariant). Do NOT save or commit any Van Buren binary bytes (only the operator-supplied path + content_hash commit per user-resolved binary-acquisition answer). Do NOT allow providers to open project SQLite throughout the test (Wave-2 isolation invariant asserted at runtime).
  Parallelization: Wave 11 | Blocked by: 51, 49 | Blocks: 56 (Wave 12 hardening exercises coordinator)
  References: spec §11 (Phase 11 exit criterion — whole executable processed without manual selection), §14 (coordinator loop), §17 (restart procedure explicit), §20 (Van Buren manual acceptance — clearly scoped out of this automated test)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test coordinator_autonomous_run -- --nocapture --ignored` ends with `[OK] autonomous full cycle on fixture; restart recovery honored per §17; at edges N orchestrates itself through provider-runtime`. Adds the `RecordingAutoReClient` asserting no `CreateReconstructionCampaign` re-issue on resume; asserts staging dir is empty after reconciler sweep on restart; asserts no `ProviderInstance` from a prior process run remains `Available` on resume. `cargo clippy` clean.
  QA scenarios (name the exact tool + invocation): happy — the test prints its 10 phase markers + final terminal-state count: `[OK] phase 10/10 terminal; verified=X blocked=Y explicitly-excluded=Z; X+Y+Z == total_work_items`. failure — disable the staging reconciler on resume → assert `staging_dir is empty after reconciler sweep` fails (orphan directories persist); test reports the violation. Evidence: `.omo/evidence/auto-re-stage-1/task-52-autonomous-run.txt`
  Commit: Y | test(reconstruction/coordinator): autonomous small-fixture end-to-end + restart recovery

### Wave 12 — Hardening (Spec §18 Phase 12)

- [ ] 53. Provider crash / coordinator restart fault injection — canonical state integrity after every fault
  What to do / Must NOT do: Add fault-injection harness `autore-reconstruction/tests/faults.rs` (slow, `#[ignore]`). Per spec §19 #2, #5, #32: (a) inject `kill -9` on the fixture provider mid-RPC → assert provider-instance marked Unavailable by the coordinator channel state (todo 7's `ChildEvent::Signal` path); (b) restart the coordinator mid-campaign → assert `mark_old_local_provider_instances_unavailable` ran on startup (autore-app command via Wave-1 `StopProviderInstance` on resume sweep); (c) inject `kill -9` on the IDA provider pre-`Completed` → assert the in-flight operation is reconciled to `Interrupted` (Wave 17 §17 step 2), no leaked leases, no orphan staging; (d) inject `kill -9` on the LLM provider post-`ArtifactProduced` pre-`Completed` → assert the partial artifact is discarded ("Incomplete artifacts are discarded" per §5.4); (e) reset the `Cargo.lock`/`project.sqlite3` mid-write — these would normally not happen but likely-tests sanitize behaviour: assert atomic SQLite transaction fails closed and SQLite integrity_check returns `ok`. Assert every recovery path goes through ApplicationCommand ( RecordingAutoReClient invariant).
  Must NOT do: do NOT skip the canonical-integrity assertion post each fault. Do NOT use `SIGTERM` instead of `kill -9` — use `SIGKILL` to simulate sudden crash specifically. Do NOT bake the SQLite path into the test environment outside the project.auto-re SQLite.
  Parallelization: Wave 12 | Blocked by: 52 | Blocks: 54, 55, 56, 57, 58
  References: spec §5.4 (artifact discard on incomplete), §17 (restart procedure), §19 #2/#5/#32 (completion criteria)
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test faults -- --nocapture --ignored` runs the 5 fault cases; ends with `[OK] all 5 fault scenarios recovered; canonical integrity intact`.
  QA scenarios: happy — each fault path completes the recovery sequence. failure — drop the partial-artifact discard logic → fault (d) leaves an orphan artifact visible via artifact list; test asserts no orphan present, fails. Evidence: `.omo/evidence/auto-re-stage-1/task-53-fault-provider-crash.txt`
  Commit: Y | test(reconstruction/faults): provider-crash + coordinator-restart canonical integrity

- [ ] 54. Invalid LLM output / repeated repair failures / corrupted artifacts fault tests
  What to do / Must NOT do: Cover spec §19 #35 ("Fixture and fault tests pass") fully. Suite of 4 cases in `autore-reconstruction/tests/faults-llm.rs`: (1) `llm_response_garbage_bytes_after_json_starts` → Wave-5 todo 23 schema validation rejects, bounded-repair runs once; still invalid → `FailWorkItem` retryable + `BlockedReason{InvalidOutput}`; (2) `llm_timeout_on_first_attempt_then_succeeds_second_attempt` emits `Diagnostic{Warning,timeout}` then on retry, returns valid → asserts `CompleteWorkItem{status=Passed}`; (3) `repeated_identical_compiler_failure_within_bounded_retry_limit_triggers_blocked_work` (LS compilation broke twice same code/line) → `BlockWorkItem{RepeatedEquivalentFailure}` per Wave-9 todo 43; (4) `corrupted_artifact_rejected_by_import` (corrupt one staged artifact's bytes by truncating to <content-hash> math mismatch) → `ArtifactError::HashMismatch` and `ImportProviderRunResult` atomic roll-back. Assert no canonical state mutated pedaling the failure.
  Must NOT do: do NOT bypass the atomic-rollback guard (Wave-3 todo 12 invariant). Do NOT auto-repair with the LLM after `BlockedReason{InvalidOutput}` (Wave-5 todo 23 explicit). Do NOT use real LLM.
  Parallelization: Wave 12 | Blocked by: 53 | Blocks: 55, 56, 57, 58
  References: Wave-5 import boundary, Wave-9 repair loop, spec §19 #35
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test faults-llm -- --nocapture --ignored` ends with `[OK] all 4 LLM/build/artifact-fault cases handled; BlockedReason or RetryableFail recorded; no canonical state mutated during failure`.
  QA scenarios: happy — schema-reject → `Insert evidence` count 0 after fail. failure — Slack a tailbyte into Markdown commit → background corruption test doesn't run. Evidence: `.omo/evidence/auto-re-stage-1/task-54-faults-llm.txt`
  Commit: Y | test(reconstruction/faults-llm): invalid-llm/repeated-failure/corrupted-artifact fault coverage

- [ ] 55. Debugger timeout + stale-work invalidation + build-tool failure coverage
  What to do / Must NOT do: Spec §19 completion-criteria completion cross-cutting #5 + #6 + #7 + #29: (1) `debugger_timeout_returns_diagnostic_and_terminates_target` (Wave-7 provider respects `StopAfterTimeout` from §9.2 — emulate IDA debugger not progressing for 10 s; assert `Diagnostic{Warning,timeout}` plus terminated target); (2) `stale_work_invalidation_rebuilds_on_refresh` (Wave-3 ingestion gets a re-save .idb compiled in test → `program_refresh_emits_only_deltas` plus stale entities marked → todo 18 fingerprintable state invalidation → Wave-9 generation_Grid rebuilds affected candidates); (3) `build_tool_failure_creates_build_environment_defect_work` (kill docker daemon mid build-attempt → diagnostic classified as `BuildEnvironmentDefect` per Wave-6 todo 30, and `BlockWorkItem` issued — the coordinator cannot auto-fix this); (4) `cancellation_token_propagates_to_provider_streams` (apply per-RPC CancellationToken 200ms after start; assert stream ends; `Completed{status=Cancelled}` or `Diagnostic{Warning,cancelled}`).
  Must NOT do: do NOT escalate build-tool failures to LLM (they are environment-defect; §12.3 explicit). Do NOT bypass stale-marking on refresh — stale-marking IS REQUIRED per §6.6. Do NOT skip build-tool recovery skipping by treating as eternal fail (must record the BlockWorkItem; operator gets an actionable reason per §14.3).
  Parallelization: Wave 12 | Blocked by: 53, 54 | Blocks: 56, 57, 58
  References: spec §19 #29 (repeated non-progress detected), §14.2 false-progress probes
  Acceptance criteria (agent-executable): `cargo test -p autore-reconstruction --test faults-coverage -- --nocapture --ignored` ends with "[OK] 4 additional fault cases covered (debugger to / stale-work / build-tool-fail / cancellation)".
  QA scenarios: happy — debugger timeout emitted Expected diagnostic. failure — remove debugger timeout check → assertion fails. Evidence: `.omo/evidence/auto-re-stage-1/task-55-faults-coverage.txt`
  Commit: Y | test(reconstruction/faults): debugger-timeout / stale-work / build-tool-fail / cancellation

- [ ] 56. PTY TUI tests for Stage 1 panes/keybindings
  What to do / Must NOT do: Adapt existing Stage 0 `autore-tui/tests/pty_integration.rs` per README.md:87 (run isolated `cargo test -p autore-tui --test pty_integration -- --ignored --nocapture`). Add 3 new PTY-driven tests for Stage 1 interactions: (1) press `o` opens start-campaign dialog → enter → asserts ApplicationCommand `CreateReconstructionCampaign` issued; (2) press `X` confirms stop-campaign dialog → asserts `StopProviderInstance{provider_instance_id}` loop shutdown all and `PauseCoordinator` invoked; (3) press `j/k` + Enter to start a provider line item → asserts ApplicationCommand issued was `RegisterProviderInstance` for selected installation; the live render+state remains consistent across a full refresh cycle. Existing PTY harness reused. After every test, terminal-restore verified.
  Must NOT do: do NOT use real providers/clis in PTY tests (use RecordingClient or mock backend from existing pattern). Do NOT skip terminal-restore-on-panic (Stage 0 invariant). Do NOT collapse new panes into existing 7; assertions reflect the new panes per Wave-11 todo 51.
  Parallelization: Wave 12 | Blocked by: 51, 55 | Blocks: 57, 58
  References: Stage 0 plan §29.15 (PTY harness invariant); existing autore-tui/tests/pty_integration.rs; spec §19 #34 completion criteria about Ratatui responsiveness
  Acceptance criteria (agent-executable): `cargo test -p autore-tui --test pty_integration -- --ignored --nocapture` runs all 3 new PTY tests + existing ones; all green. Each test executes in <5s under real pty; terminal sanity check final-state stdout normal.
  QA scenarios: happy — `o` dialog opens and closes cleanly with ArtifactId in campaign record. failure — remain in block-on-render thread (slipped regression) → PTY takes >5s timeout. Evidence: `.omo/evidence/auto-re-stage-1/task-56-pty.txt`
  Commit: Y | test(tui): Stage 1 PTY-keybinding integration tests

- [ ] 57. Workspace tests + fmt + clippy with `--exclude autore-stage1` discipline
  What to do / Must NOT do: Final-cross crate quality gate per Stage 0 plan §32 #44/#45/#46 invariants. Run in order: `cargo fmt --all --check`, `cargo clippy --workspace --exclude autore-stage1 --all-targets -- -D warnings` (this excludes `autore-stage1` only, since `autore-provider-protocol`, `autore-provider-runtime`, `autore-reconstruction` ARE marked `members` and clippy walks them when invoked with `-p` explicitly — the wave pattern below does that), `cargo test --workspace --exclude autore-stage1` (the 7 new crates are all off `default-members` so `cargo test --workspace --exclude autore-stage1` still runs their unit tests by workspace-membership, not by default-members surface), `cargo build` (no flags — must succeed for the 7 default-member Stage-0 crates only, no `protoc` requirement), `cargo build -p autore-stage1 --no-default-features`, `cargo build -p autore-provider-protocol --no-default-features`, `cargo build -p autore-provider-runtime --no-default-features`, `cargo build -p autore-reconstruction --no-default-features`, `cargo build -p fixture-provider --no-default-features`, `cargo build -p ida-provider --no-default-features`, `cargo build -p openai-compatible-provider --no-default-features`, `cargo build -p build-provider --no-default-features`. Assert no new compiler warnings. Stage 1 explicit clippy sweep on new crates: `cargo clippy -p autore-provider-protocol -p autore-provider-runtime -p autore-reconstruction -p fixture-provider -p ida-provider -p openai-compatible-provider -p build-provider --all-targets -- -D warnings`. PTY test still separated (`cargo test -p autore-tui --test pty_integration -- --ignored --nocapture`).
  Must NOT do: do NOT run `cargo test --workspace` without `--exclude autore-stage1` (Stage 0 invariant carried forward — autore-stage1 needs external SDK binding). Do NOT suppress clippy with `#[allow]` ... except for a documented wax Room exception with reason annotation. Do NOT ignore `cargo fmt`. Do NOT ignore target binary breakage warnings.
  Parallelization: Wave 12 | Blocked by: 55, 56 | Blocks: 58, 59, 60
  References: Stage 0 README.md:84-91 (commands pattern); spec §19 #36/#37/#38
  Acceptance criteria (agent-executable): all listed commands exits 0, zero warnings, all tests green. `_count warnings` via `cargo build 2>&1 | grep -c warning` is 0 across each. `cargo fmt --all --check` shows 0 differences.
  QA scenarios: happy — green across the board. failure — discourage — Leave an unwrap() in fresh code → clippy warning, exit nonzero. Evidence: `.omo/evidence/auto-re-stage-1/task-57-workspace-gates.txt`
  Commit: N | (gate-only todo, no source change)

- [ ] 58. Required final implementation report (spec §22) — comprehensive structured report
  What to do / Must NOT do: Author `docs/stage1-report.md` with all §22 headings: Existing code (Retained/Adapted/Replaced/Removed/Added), Provider substrate, IDA integration, Debugger integration, LLM integration, Scheduler, C++ generation, Build & verification, Recovery, Deferred work, Test results. Each section structured from spec §22 (not paraphrased). Pull quantitative numbers from `.omo/evidence/auto-re-stage-1/` artifacts (test counts, fixtures). Deferred work section itemizes every §21 / per-cut defer (e.g. Ghidra / Binary Ninja / symbolic execution etc.). Include final `§23 Architectural test` 10 yes/no answers bar-documenting each invariant. Lighting Leavedcan coordinate: the report becomes the implementation closest callback for the §23 architectural test acceptance.
  Must NOT do: do NOT hand-wave the "deferred" section — every item from §21 must appear. Do NOT falsify test results — extract from evidenced files. Do NOT overwrite `docs/stage0-report.md` (separate doc).
  Parallelization: Wave 12 | Blocked by: 57 | Blocks: 59, 60
  References: spec §22 (full report structure), §23 (10 architectural test questions)
  Acceptance criteria (agent-executable): `test -f docs/stage1-report.md` and `grep -c '^## ' docs/stage1-report.md` ≥ 11 (one per §22 heading). Deferred-work section contains each of the 23 spec §21 exclusions verbatim. Test results section contains evidence file paths. Architectural-test section contains 10 yes/no answers with rationale.
  QA scenarios: happy — file present with required sections. failure — omit one §21 deferral → test fails. Evidence: `.omo/evidence/auto-re-stage-1/task-58-stage1-report.txt`
  Commit: Y | docs(stage1): comprehensive final implementation report per spec §22

- [ ] 59. §23 Architectural-test answer the 10 yes/no questions with explicit proof
  What to do / Must NOT do: For each of the 10 §23 questions, write 1-5 sentences proving the answer is "yes" by referencing the actual CRATE/Trait/struct PATH in the shipped code, NOT by quoting the spec. Examples: "Can a future Ghidra provider implement the analysis capabilities without changing canonical storage? YES — trait `AnalysisProvider` in `autore-reconstruction/src/provider/analysis.rs:NN-NN` declares 9 capability method signatures independent of IDA; storage routes only through `ApplicationCommand::ImportProviderRunResult`; a Ghidra impl needs only `impl AnalysisProvider for GhidraProvider` + a manifest.toml." Same shape for each of the 10. Save to `docs/stage1-architectural-test.md`. If any answer is "no" — STOP and report; the plan (per spec §23 final) requires correction before completion.
  Must NOT do: do NOT answer "yes" without a code-path citation. Do NOT skip a question. Do NOT continue work past a "no" — sign-off fixes[ __must land first].
  Parallelization: Wave 12 | Blocked by: 57, 58 | Blocks: 60
  References: spec §23 (10 questions); all previously-built trait surfaces
  Acceptance criteria (agent-executable): `test -f docs/stage1-architectural-test.md` AND file has exactly 10 sections. EXPLICIT ENUMERATION (each question must be answered literally — restate the spec §23 question verbatim as a `## Q1.` heading, then the proof): (Q1) Can a future Ghidra provider implement the analysis capabilities without changing canonical storage? (Q2) Can a future direct GDB provider implement the debugger scenarios without changing the coordinator? (Q3) Can a future non-OpenAI model provider implement the LLM capabilities without changing the scheduler? (Q4) Can remote artifact transfer replace local staging without changing capability semantics? (Q5) Can a second build backend be added without changing generated-entity ownership? (Q6) Can symbolic execution later produce observations through the same import boundary? (Q7) Can package dependency resolution later wrap the existing local provider-package identity? (Q8) Can stronger sandboxing later wrap provider and target execution without changing provider capabilities? (Q9) Can additional verification techniques produce records through the existing verification model? (Q10) Can every current shortcut be identified as an implementation behind a stable interface rather than a semantic assumption embedded in the core? Each section contains a literal `YES` AND a code-path citation (file:line or trait:method within the shipped tree). `grep -c '^## Q' docs/stage1-architectural-test.md == 10`. `grep -c 'NO' docs/stage1-architectural-test.md == 0` (or, if any answer is NO, leaves a halt-marker file under `.omo/` and stops the worker before Wave 12 completes).
  QA scenarios: happy — 10 yes answers with code-path citations. failure — replace one with "yes (assumed)" → "literal token 'YES'" regex fails. Evidence: `.omo/evidence/auto-re-stage-1/task-59-architectural-test.txt`
  Commit: Y | docs(stage1): §23 architectural proof file with code-path citations

- [ ] 60. Spec §19 38-item completion gate cross-check + §20 Van Buren criterion reminder report
  What to do / Must NOT do: Build a 38-row markdown table `docs/stage1-completion-gate.md`: row per §19 criterion, columns: # | Spec-text | Evidence pattern | Pass marked "Y" | Note. Each row's "Pass" cell filled by parsing the corresponding `.omo/evidence/auto-re-stage-1/task-<N>-<slug>.txt` file and checking for the "OK" present or marked-as-completed flag (the evidence file convention established by every previous todo's QA section). For any criterion whose evidence can't be checked, FAIL — go back to the corresponding todo with a follow-up edit-in-place; do NOT declare Wave 12 done with outstanding items. Also include a separate one-page addendum reminding the operator that the §20 "Van Buren reconstruction completion" is a campaign-success bar (not implementation): it requires a manual whole-Van-Buren real-LLM autonomous run; the campaign's exit terminal state must report no required work items stubbed | blocked | omitted, etc. The campaign's blocked-count report becomes the operator-accepted "honest partial reconstruction" output that §20 explicitly endorses — the implementation does NOT enforce this manually.
  Must NOT do: do NOT mark a criterion pass without an evidence file path. Do NOT collapse §19 and §20. Do NOT run whole Van Buren automatically (operator activity).
  Parallelization: Wave 12 | Blocked by: 58, 59 | Blocks: F1, F2, F3, F4
  References: spec §19 (38 criteria), §20 (11 Van Buren), spec §22 (final report)
  Acceptance criteria (agent-executable): `test -f docs/stage1-completion-gate.md` AND `grep -c '^| [0-9]' docs/stage1-completion-gate.md == 38`. EXPLICIT ENUMERATION — the file's 38 rows each carry the spec §19 verbatim text plus the row number from §19 #1..#38, plus the mapping todo-numbers from this plan plus the evidence file path. Spec §19 #1 "All canonical mutations use `autore-app`" → todos 2, 4, 12, 17, 19, 21, 23, 33, 49 (any state-mutating path). #2 "Real providers run externally" → 10, 13, 21, 28, 32. #3 "Provider requests are versioned and typed" → 6, 7, 22. #4 "The entire Van Buren executable can be ingested" → 13, 15, 52 (52 proves on small fixture; Van Buren real-binary run is operator-acceptance, §20). #5 "Every discovered relevant entity has a canonical identity" → 14, 15. #6 "Every relevant entity has an explicit work or terminal state" → 17, 18, 19, 36, 41, 45, 49. #7 "The work graph includes dependency cycles correctly" → 17 (SCC collapse test). #8 "Scheduling proceeds without manual function selection" → 20. #9 "IDA static snapshots can be refreshed on demand" → 13, 14, 55 (stale-marking test). #10 "IDA debugging uses GDB as its backend" → 32, 35. #11 "Structured debugger scenarios produce dynamic observations" → 31, 32, 33, 34. #12 "The OpenAI-compatible provider supports both analysis and generation" → 21, 41. #13 "LLM outputs are schema-constrained" → 22, 23, 24. #14 "Raw model responses are preserved" → 23 (raw-llm-response artifact commit), 25 (e2e). #15 "LLM claims enter as hypotheses, not facts" → 23, 38 (Wave-8 conflict), §3.3 invariant throughout. #16 "Generated source enters as candidate artifacts" → 27, 42 (stageedInbound), 44. #17 "The core independently validates provider results" → 23, 33 (observation importer). #18 "Import is atomic" → 12, 33, 42, 53 (fault tests). #19 "A complete C++ project is generated" → 27. #20 "Every discovered function has a generated-source mapping" → 27 (skeleton; mappings registered). #21 "Temporary stubs are explicit and measurable" → 27, 39 (stubbed→replaced), 44. #22 "The complete source tree can build using the configured toolchain" → 29. #23 "Compiler failures create repair work" → 28, 30, 43. #24 "Shared types are reconciled globally" → 36, 37, 38, 40. #25 "Original and generated scenarios can be compared" → 45, 48. #26 "Verification failures create investigation or repair work" → 46, 47. #27 "Verified code retains its supporting scenarios and evidence" → 47 (regression set), 13 (artifact storage invariant). #28 "Dependency changes invalidate affected verification" → 18, 47. #29 "Repeated non-progress is detected" → 43 (bounded-retry + RepeatedEquivalentFailure), 49 (no-progress detector), 54 (faults-llm). #30 "Blocked work includes a structured reason" → 12 (BlockWorkWithReason), 23 (InvalidOutput), 43 (RepeatedEquivalentFailure), 49 (no-progress paths), 55 (BuildEnvironmentDefect). #31 "Coordinator restart preserves committed progress" → 49 (reconcile interrupted ops), 52 (autonomous + restart recovery), 53 (provider-crash). #32 "Provider failure cannot corrupt canonical state" → 53, 54. #33 "CLI exposes the complete campaign" → 50. #34 "Ratatui remains responsive during provider and model operations" → 51, 56 (PTY no-block-render test). #35 "Fixture and fault tests pass" → 10, 53, 54, 55. #36 "Workspace tests pass" → 57. #37 "Formatting passes" → 57. #38 "Clippy passes with warnings denied" → 57. Every §19 row has all four columns filled (row#, verbatim text, todo-nums, evidence path). Addendum: §20 reminder addendum asserts the Van-Buren-real-binary real-LLM campaign is operator-manual acceptance (separate from the automated gate), with explicit reminder that `Blocked` items are an honest partial reconstruction per §20 not a failure state.
  QA scenarios: happy — 38 rows all green. failure — mark a criterion "Failing" and assert "no F remains" check picks it up and returns nonzero exit. Evidence: `.omo/evidence/auto-re-stage-1/task-60-completion-gate.txt`
  Commit: Y | docs(stage1): 38-item completion-gate cross-check + §20 Van Buren addendum

## Final verification wave
> Runs in parallel after ALL todos. ALL must APPROVE. Surface results and wait for the user's explicit okay before declaring complete.
- [ ] F1. Plan compliance audit
- [ ] F2. Code quality review
- [ ] F3. Real manual QA
- [ ] F4. Scope fidelity

## Commit strategy
- One commit per todo that has `Commit: Y` in its Commit line. Each such todo's Commit line names the conventional-commit prefix (feat / refactor / test / docs / chore) and a scope.
- Todos with `Commit: N` (only task 5 and 57) leave no source diff — they are agent-executed gates; their evidence artifacts live under `.omo/` not the project tree.
- Atomic verbs verified the design: every commit references its task by separate conventional type since per Stage-0 plan discipline.
- Per-wave convention: `Wave N` commits tag via separate conventional type e.g. `feat(proto)`, `feat(providers/runtime)`, `feat(reconstruction/...)`, `test(...)`, `docs(stage1)` — embedded into each Commit line. No `chore(merge)` merges — linear history preserved per Stage-0 plan §32 / workspace invariant.
- Every commit is **bi-directional volumetric exit** — flag broken realges and in-cd lives at worker-side discretion: a `git revert <commit>` is the canonical fallback. Atomic per-todo commits make every todo REVERTIBLE INDEPENDENTLY — backup rule for the hardening phase (Wave 12) fault tests.

## Success criteria
This plan is shipped ONLY and IF-AND-ONLY-IF all the following gate items are green AND all four `F1-F4` final-verification-wave reviewers approve. The criteria list is the explicit pass-bar for the executor; anything below is failure.

### A. Per-todo Completion (60/60)
All 60 todos have their Acceptance criteria met by an agent-executable command (no human walk-throughs). The evidence file for each todo contains the asserted `[OK]` line. Stub-only todos marked `Proof out-of-band`. Every Commit line crashed correct conventional-type. Use the dependency matrix as the ordering constraint — no todo starts before its `Blocked by` list is fully satisfied.

### B. Stage 1 §19 Completion Gate Cross-check (38/38)
`docs/stage1-completion-gate.md` has 38 rows; every row has `Pass = Y`; all 38 spec §19 criteria across the 12 vertical cuts are green. Implementer asserts the cross-check at todo 60 — this report is the **status-of-truth** file shipped to the user at delivery.

### C. Spec §23 Architectural Test (10/10 yes)
`docs/stage1-architectural-test.md` exists with 10 sections, each containing a literal `YES` and a code-path citation. If any answer is `NO` the spec §23 requires correction before completion — the worker MUST fix the cited dead end and re-run todo 59 before re-entering the final verification wave.

### D. Spec §22 Final Implementation Report
`docs/stage1-report.md` exists with all 11 §22-mandated section headings. Deferred-work section lists all 23 §21 exclusions verbatim. Test-results section contains test counts and evidence file paths.

### E. Workspace Gates
- `cargo fmt --all --check` clean
- `cargo clippy --workspace --exclude autore-stage1 --all-targets -- -D warnings` clean — AND `-D warnings` for every new Stage-1 crate (autore-provider-protocol, autore-provider-runtime, autore-reconstruction, fixture-provider, ida-provider, openai-compatible-provider, build-provider)
- `cargo test --workspace --exclude autore-stage1` passes (existing 614 Stage-0 tests + new Stage-1 unit tests)
- `cargo build -p autore-stage1 --no-default-features` builds clean
- `cargo build -p fixture-provider --no-default-features`, `-p ida-provider --no-default-features`, `-p openai-compatible-provider --no-default-features`, `-p build-provider --no-default-features` all build clean
- `cargo test --workspace --exclude autore-stage1 -- --include-ignored` runs the previously `#[ignore]`-tagged Wave-11 slow integration tests (autonomous run; whole-program work graph) and they pass
- `cargo test -p autore-tui --test pty_integration -- --ignored --nocapture` passes (3 new Stage-1 PTY tests + Stage-0 ones)
- Stage 0 614-test baseline preserved unchanged (no regressions in retained Stage-0 modules)

### F. Final Verification Wave (F1-F4 ALL APPROVE)
- **F1. Plan compliance audit** — re-read this plan; confirm every §1-§23 spec section is mapped to ≥1 todo; confirm every `Must NOT do` is preserved in the shipped code; flag any deviation as a stop-item.
- **F2. Code quality review** — clippy `-D warnings` clean on all crates per workspace gate E above; no `unwrap()` panic paths in non-test code; no panicking newtons allowed; no `unwrap_or_else(|| panic!())` inside a coordinator/codegen loop; no `todo!()` / `unimplemented!()` outside of the explicit Stage-1 command details list and tasks that are explicitly deferred (with a `BlockWorkItem` test specimen proving they fail closed).
- **F3. Real manual QA** — operator runs `auto-re reconstruct start --binary ./samples/<small.exe> --output /tmp/gen-out --analysis-provider ida --model-provider openai-compatible --build-profile msvc-docker` against a real local LLM endpoint (or `mock-mode` flag dedicated to manual QA). Reports `auto-re reconstruct status --output json`; confirms campaign progresses through ingestion → generation → build → verification; cancels mid-run via `auto-re reconstruct stop`; restarts via `auto-re reconstruct resume`; confirms `auto-re work list` shows matching reconcile state. Operator confirms TUI panes leading about feasibility coordinates.
- **F4. Scope fidelity** — current visible diff vs. spec §21 exclusions: no Ghidra / Binary Ninja / package-solver / remote-workers / sandbox / symbolic-execution / multi-language generation committed. Cross-check `docs/stage1-architectural-test.md` for the 10 §23 yes answers; verify each individual citation path exists in the shipped tree. The user (not the worker) decides whether the Van Buren exe itself qualifies per §20 — the §20 criterion is a campaign-success bar, OUTSIDE this implementation-completion scope; the implementation plan delivers the platform capable of running a Van Buren campaign to that bar.

### G. Stage 0 Backward-Compatibility
- Schema version still `2.0`.
- Existing `project.auto-re/` projects created at Stage-0 still open successfully at Stage-1 (no migration regression).
- Existing CLI commands preserved verbatim per Stage-0 README §CLI usage.
- `cargo build` (no flags, default members) still succeeds in a clean dev environment WITHOUT `protoc` installed — the 7 Stage-0 default members compile cleanly. The 7 new Stage-1 crates (`autore-provider-protocol`, `autore-provider-runtime`, `autore-reconstruction`, `fixture-provider`, `ida-provider`, `openai-compatible-provider`, `build-provider`) are added to workspace `members` but NOT `default-members`, matching the existing discipline for `autore-stage1`. `cargo build --workspace` compiles them too (requires `protoc` + IDA SDK), but `cargo build` default does not.

### H. Default-Members Disposition (explicit ledger — Metis/Momus round-2 fix)

| Crate | `members` | `default-members` | Public? | Reason |
| --- | --- | --- | --- | --- |
| `autore-provider-protocol` | YES | NO | lib | Requires `protoc` build-time → `cargo build` must NOT pull it in |
| `autore-provider-runtime` | YES | NO | lib | Transitively depends on `autore-provider-protocol` (`protoc`) |
| `autore-reconstruction` | YES | NO | lib | Transitively depends on `autore-provider-runtime` (`protoc`) |
| `fixture-provider` | YES | NO | bin | Requires provider runtime + `protoc` build-time |
| `ida-provider` | YES | NO | bin | Requires `idax` SDK |
| `openai-compatible-provider` | YES | NO | bin | Requires provider runtime + `protoc` build-time |
| `build-provider` | YES | NO | bin | Requires provider runtime + `protoc` build-time |
| `autore-stage1` | YES | NO (existing) | bin+lib | Already excluded per Stage 0 plan due to IDA/llama/gdb features |

The Wave-summary `--exclude autore-stage1` modifier on `cargo test --workspace` / `cargo clippy --workspace` only excludes the existing scaffold crate. The 7 new Stage-1 crates remain in `members` (testable by explicit `-p <name>` or by workspace-membership in a CI environment with all build tools available).