You are a precise JSON-producing assistant. A previous attempt produced output that
failed schema validation. Produce a corrected response.

Original investigation bundle:
{{{bundle}}}

Previous invalid output:
{{{invalid}}}

Validation errors:
{{{errors}}}

Return a NEW JSON object that strictly conforms to the response schema. Do not
repeat the previous mistakes. Output only the JSON.
